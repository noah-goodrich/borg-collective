## 1. Goal
Orchestration session: fix borg-link permission prompts, advance ingle frontend migration, and
move reveal-site forward.

## 2. Accomplished

**borg-collective v0.7.14 — shipped**
- `hooks/bash-guard.sh` Layer 1.5: added pre-approval for `for f in */docs/plans/*` and
  `for f in */.borg/checkpoints/*` patterns — eliminates "Unhandled node type: string"
  prompts during `/borg-link`
- `skills/borg-link/SKILL.md`: added `## Command Patterns` section with grep/awk/ls
  alternatives to for-loops + `$()` inside echo
- Tagged v0.7.14, pushed main + tag, updated `Formula/borg-collective.rb` with new sha256
- Committed: `f6d3a40` (bash-guard + SKILL.md), `014e1df` (version bump), `bee12b8` (formula)

**ingle — frontend migration foundation**
- Audited all 15 Supabase query files: 9 need changes, zero new schema adoption yet
- Created `/Users/noah/dev/ingle/PROJECT_PLAN.md` — 6-phase test-driven plan (type regen →
  store_chains → purchase_transactions → category_id → canonical_item_id → typecheck clean)
- Created `/Users/noah/dev/ingle/supabase/tests/schema_migration.smoke.ts` — 14 tests in the
  existing `pg` + `tsx` smoke pattern; red against old schema, green after migration

**reveal-site — film stock naming directive**
- Rewrote `filmStock.el10` in all 7 `src/content/archetypes/*.md` files: qualities first,
  named stock as example (matches photographer naming rule from last session)
- Committed `f2dcdf6` on `main` — not yet pushed

**reveal drone — diagnosed**
- All processes zombie (npm run dev, node ×2, headless_shell ×10); only `sleep infinity`
  alive. Needs `drone restart reveal`.

**Loops setup guidance**
- Explained full setup: Loops API key → CF Pages env var `LOOPS_API_KEY` → redeploy
- Recommended dedicated sending subdomain (mail./send.) for reputation isolation

## 3. Ready to Commit

Nothing uncommitted in borg-collective (all v0.7.14 work is committed and pushed).

Manual actions needed:
- Sync installed borg-link skill:
  cp /Users/noah/dev/borg-collective/skills/borg-link/SKILL.md ~/.claude/skills/borg-link/SKILL.md
- Push reveal-site film stock commit:
  git -C /Users/noah/dev/reveal-site push

## 4. Blockers

- borg-link skill sync — installed copy at ~/.claude/skills/borg-link/SKILL.md is stale;
  requires manual cp (bash-guard correctly blocks rm -rf ~/.claude from Claude sessions)
- reveal drone crashed — run `drone restart reveal` before any reveal work
- Loops LOOPS_API_KEY — not set in CF Pages; waitlist form returns devMode: true
- ingle devcontainer — Phase 0 type regen must run inside container:
  drone exec ingle -- supabase gen types typescript --local > /workspaces/ingle/src/lib/supabase/database.types.ts

## 5. Next Session

**reveal-site:** Execute docs/plans/directives/2026-05-06-photographer-naming-audit.md (reveal
app, not reveal-site) — fix historyEl10 possessive framing in src/lib/archetypes.ts, remove
dead rootedIn field, update prompt design principle #1 in prompts.py (~line 850).

**ingle:** Start Phase 0 — regenerate database.types.ts, run npm run typecheck, use the
error list as the full migration hit list. Then Phase 1: grocery_lists.store → chain_id
with smoke test green as the gate.
