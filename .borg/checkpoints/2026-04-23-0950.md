## 1. Goal

Three interleaved pieces of work this session:
1. Detangle reveal/ingle workspace collision (every drone used `/workspace`, causing Claude to write
   all drones' session history into one shared `~/.claude/projects/-workspace/` dir).
2. Add layout hotkeys + default side-by-side 2-pane layout for new drone windows.
3. Audit/fix coverage gaps: briefs references (none found — already clean), `docs/plans/severed/`
   coverage in skills + CLI (real gap), and redesign the session-lifecycle around checkpoints
   (kill the LLM debrief system).

## 2. Accomplished

**Workspace collision fix:**
- `drone.zsh cmd_scaffold`: default workspace is now `/workspaces/<project_name>` instead of
  `/workspace`; legacy `/workspace` symlink created in postStartCommand when they differ
- `templates/supabase/devcontainer.json` gained `__WS_SYMLINK__` placeholder
- `~/.config/dotfiles/devcontainer/docker-compose.base.yml` example comment updated
- Applied surgical fix to reveal (`.devcontainer/{Dockerfile,docker-compose.yml,devcontainer.json}`)
  and ingle (same four files plus `CLAUDE_PLUGIN_ROOT` env var)
- Reveal container restarted; confirmed `pwd` inside is `/workspaces/reveal`, `/workspace` symlink
  resolves correctly, `.borg-project` + `PROJECT_PLAN.md` visible via both paths

**Tmux + drone layout:**
- `~/.config/dotfiles/tmux/tmux.conf`: added `bind = select-layout even-horizontal` (side-by-side
  50/50) and `bind _ select-layout even-vertical` (stacked 50/50); removed the dead 70/30 3-pane
  resize hook
- `drone.zsh create_2pane_window`: default is now `split-window -h -p 50` (side-by-side), focus
  goes to the right pane (Claude pane)

**Session-lifecycle redesign (link-up/link-down + kill debriefs) — BUT NAMING IS INVERTED:**
- Renamed `hooks/borg-start.sh` → `hooks/borg-link-up.sh` (this is wrong — should be
  `borg-link-down.sh` per "download FROM collective")
- Renamed `hooks/borg-stop.sh` → `hooks/borg-link-down.sh` (this is wrong — should be
  `borg-link-up.sh` per "upload TO collective")
- Renamed `skills/borg-checkpoint/` → `skills/borg-link-down/` (wrong — should be `borg-link-up/`)
- Deleted `skills/borg-debrief/` (redundant with checkpoints)
- Removed the Sonnet LLM debrief path entirely from the Stop hook
- SessionStart hook now reads newest `<project>/.borg/checkpoints/*.md` instead of a debrief file
- `borg.zsh` deep-dive, morning briefing, and LLM briefing payload all swapped from debriefs to
  checkpoints; `borg-link` SKILL.md gained severed/ coverage
- Live envs updated: `~/.claude/settings.json`, `~/.snowflake/cortex/settings.json`, installed
  `~/.claude/hooks/` and `~/.snowflake/cortex/hooks/`, installed `~/.claude/skills/borg-link-down/`
- Canonical template `config/claude/settings.base.json` updated
- `README.md`, `CLAUDE.md`, `install.sh`, `tool-count-nudge.sh`, `borg-review` SKILL, tests, memory
  all point at new names
- `borg.zsh cmd_setup` has migration code to clean up old names on next `borg setup`

**Other:**
- Confirmed: no stale "briefs" references anywhere (only `debriefs` substring matches, which are
  historical record dirs)
- Severed-coverage gap documented in `borg-link` SKILL.md data contract

## 3. Ready to Commit

**Extensive uncommitted changes.** Before any commit, run `/simplify` on:
- `borg.zsh` (many blocks swapped; mostly mechanical but worth a sweep)
- `hooks/borg-link-up.sh` (pending rename), `hooks/borg-link-down.sh` (pending rename)
- `drone.zsh create_2pane_window` + `cmd_scaffold` (workspace-defaulting logic)
- `skills/borg-link-down/SKILL.md` (pending rename) and `skills/borg-link/SKILL.md`

**Do NOT commit yet** — the link-up/link-down names are semantically inverted and need a swap
pass first. Otherwise git history will capture the wrong names, then a re-rename, which is noise.

Modified (pre-rename-swap):
- `.gitignore` (pre-existing, unrelated)
- `CLAUDE.md`, `README.md`, `install.sh`, `borg.zsh`, `drone.zsh`
- `config/claude/settings.base.json`, `templates/supabase/devcontainer.json`
- `hooks/tool-count-nudge.sh`, `lib/borg-hooks.sh`
- `skills/borg-link/SKILL.md`, `skills/borg-review/SKILL.md`
- `tests/cairn.bats`, `tests/lifecycle.bats`
- `~/.config/dotfiles/tmux/tmux.conf`
- Reveal `.devcontainer/*`, Ingle `.devcontainer/*`

Renames (all direction-flipped, will be renamed AGAIN in the swap pass):
- `hooks/borg-start.sh` → `hooks/borg-link-up.sh` (should be `borg-link-down.sh`)
- `hooks/borg-stop.sh` → `hooks/borg-link-down.sh` (should be `borg-link-up.sh`)
- `skills/borg-checkpoint` → `skills/borg-link-down` (should be `skills/borg-link-up`)

Deletions:
- `skills/borg-debrief/SKILL.md` (intentional, keep)

## 4. Blockers

**CRITICAL: link-up/link-down semantic is inverted.** The naming convention should be:
- `borg-link-down.sh` (SessionStart hook) = downloads latest checkpoint FROM collective INTO session
- `/borg-link-up` (manual skill) = uploads session state TO collective (writes a checkpoint)

I built the opposite. Every file referencing these names needs a second swap pass before commit.

## 5. Next Session

1. **Swap the link-up/link-down names end-to-end.**
   a. File renames (use intermediate names to avoid collision, e.g. `*.tmpswap`):
      - `hooks/borg-link-up.sh` → `hooks/borg-link-down.sh`
      - `hooks/borg-link-down.sh` → `hooks/borg-link-up.sh`
      - `skills/borg-link-down/` → `skills/borg-link-up/`
   b. Update SKILL.md internal `name:` field in the renamed skill to `borg-link-up`
   c. Update content inside each renamed hook (comment header explaining direction)
   d. `borg.zsh cmd_setup`: flip the `_borg_register_hook` event types — the hook registered for
      SessionStart should be `borg-link-down.sh`; the hook registered for Stop should be
      `borg-link-up.sh`. Also flip the CoCo ones. Keep the migration-cleanup blocks.
   e. Live env: flip `~/.claude/settings.json` and `~/.snowflake/cortex/settings.json` hook paths,
      copy the renamed hooks into installed dirs, rename installed skill dir
   f. Canonical: flip `config/claude/settings.base.json` paths
   g. Docs: `README.md` hook table, `CLAUDE.md` hook list, `install.sh` help text,
      `tool-count-nudge.sh` nudge text (uses `/borg-link-up` now), `skills/borg-review/SKILL.md`
      "fading → run /borg-link-up", memory `feedback_simplify_checkpoint.md` ref
   h. Tests: `tests/cairn.bats`, `tests/lifecycle.bats` — swap `BORG_START` / `BORG_STOP` bindings

2. **Run `/simplify` across the changed files**, then commit. Single commit for the whole
   session-lifecycle redesign (post-swap) — don't split into rename-churn commits.

3. **Docs sweep (follow-up directive):** `docs/architecture.md`, `skills-guide.md`,
   `devcontainer-coco.md`, `boris-workflow.md`, `cheatsheet.md`, `six-pager.md` still reference
   old names in prose.

4. **`borg regenerate` rename** — still queued. Separate PR. Covers: `drone sever <project>` +
   `/borg-sever` skill + rename top-level `borg down` → `borg regenerate`.

5. **Historical cleanup (optional):** existing `.borg/debriefs/` dirs in reveal, borg-collective,
   snowfort, ingle; `~/.config/borg/debriefs/` global file. These are no longer written to. Delete
   or archive at your discretion.
