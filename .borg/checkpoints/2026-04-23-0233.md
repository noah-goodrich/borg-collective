## 1. Goal
Reduce permission-prompt friction by allowlisting Supabase MCP tools that have been pestering Claude sessions in `reveal` and `ingle`.

## 2. Accomplished
- Scanned `~/.claude/projects/-Users-noah-dev-reveal/*.jsonl` and `-workspace/*.jsonl` (ingle in-container) for actual `tool_use` invocations — found 26 real Supabase MCP calls, dominated by `execute_sql` (7), `apply_migration` (4), and read-only `list_*`/`get_*` ops.
- Added 17 read-only Supabase MCP tools to the global allowlist in `~/.claude/settings.json` (alphabetically placed between `Grep` and `Read`): `generate_typescript_types`, all `get_*` and `list_*` ops, and `search_docs`.
- Verified valid JSON via `jq` post-edit.
- Deliberately left mutating ops (`execute_sql`, `apply_migration`, branch/project lifecycle) behind a prompt.

## 3. Ready to Commit
Nothing in this repo. The only change was to `~/.claude/settings.json`, which is outside `borg-collective` and not tracked here. No `borg-collective` files were modified.

Pre-existing untracked files in working tree (unrelated to this session, do not commit as part of this work): `.borg/checkpoints/2026-04-14-1600.md`, `.borg/checkpoints/2026-04-20-1100.md`, `.claude/`, `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md`, `templates/supabase/.borg/`.

## 4. Blockers
No blockers.

## 5. Next Session
If Supabase prompts return despite the allowlist, grep for the actual tool name in the triggering session (`jq -r '..|.name? // empty' ~/.claude/projects/<slug>/*.jsonl | grep -i supabase`) — the offender is probably `execute_sql` or `apply_migration`, which were intentionally left out. Decide then whether to promote them to the allowlist (option 2 from this session) or keep the prompt. Separately: `ingle` is registered in the borg registry but has no Claude session history on the host because it runs inside a drone container under `/workspace` — worth noting if you ever wonder why `borg ls` shows it idle.
