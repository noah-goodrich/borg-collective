## 1. Goal
Finish the ssh-agent fix sweep, split the borg/dotfiles boundary so future borg changes don't bleed into personal dotfiles, and propagate the ssh-auth-socket permission fix across every devcontainer so SSH works end-to-end from inside containers.

## 2. Accomplished
- **Closed the first commit sweep** (started pre-0900 checkpoint): committed the selective `~/.ssh` mount fix in `reveal b8065df`, `pytest-coverage-impact 46c415b`, `snowfort d10781f` using `--no-verify` (authorized — devcontainer-only changes).
- **Borg/dotfiles boundary split (borg-collective 7b4365b, dotfiles e35737c):**
  - Created `borg-collective/config/{claude,cortex}/` as the canonical home for borg-required baseline.
  - `config/claude/CLAUDE.md` — the Permissions / Bash Tool Rules / Subagent Rules sections, now injected into `~/.claude/CLAUDE.md` as a delimited `<!-- BEGIN borg-managed --> ... <!-- END borg-managed -->` block.
  - `config/claude/settings.base.json` and `config/cortex/settings.base.json` — canonical permission lists.
  - New `_borg_merge_claude_md()` helper in `borg.zsh` (strips old block + trailing blanks, re-appends; verified byte-identical across consecutive runs).
  - `cmd_setup` now reads from `$BORG_HOME/config/*` instead of `$DOTFILES_DIR/*`; personal dotfiles CLAUDE.md used only as a first-run seed.
  - Dotfiles shrunk: `~/.config/dotfiles/claude/code/CLAUDE.md` trimmed to personal-only content; `~/.config/dotfiles/claude/code/settings.json` + `~/.config/dotfiles/cortex/settings.base.json` deleted.
  - Also fixed the stale token-cost `@import` path in the dotfiles CLAUDE.md as a drive-by.
- **Devcontainer-base image rebuild** with the new `install -d -o dev -g dev -m 700 /home/dev/.ssh` layer.
- **Ingle container brought up on the new config.** Verified: `/home/dev/.ssh/` is 700 dev-owned, `config` + `known_hosts` are `:ro` bind-mounts with correct perms, agent socket reachable, `ssh -T git@github.com` authenticates as `noah-goodrich`.
- **Socket-perm fix propagated** (7 commits across 5 repos + 2 templates):
  - `borg-collective 3a18811` — drone.zsh scaffold template + `templates/supabase/devcontainer.json`
  - `claude-plugins 064ee1c` — `bootstrap-project/references/devcontainer.template.json`
  - `ingle a590b66`, `snowfort-scaffold-bak 43d562b`, `reveal b8065df`, `pytest-coverage-impact 46c415b`, `snowfort d10781f` — each project's `devcontainer.json`.
  - Pattern: stripped `chmod 600 /home/dev/.ssh/*` from `postCreateCommand` (files are `:ro` now — chmod returned "Read-only file system"); added `sudo chmod a+rw /run/host-services/ssh-auth.sock 2>/dev/null || true` to `postStartCommand`.

## 3. Ready to Commit
Nothing in scope. All session work is committed across 13 commits in 8 repos. Pre-existing noise remains (unchanged from the 0900 checkpoint):
- `borg-collective`: `M .gitignore`, `?? .claude/`, `?? docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md`
- `~/.config/dotfiles`: `M git/config`, `?? .borg-project`
- Per-project untracked files in reveal, pytest-coverage-impact, ingle, snowfort, snowfort-scaffold-bak (unrelated to this session)

No `/simplify` run. The two functions worth reviewing before they set convention: `_borg_merge_claude_md()` in `borg-collective/borg.zsh` (new, ~25 lines) and `heal_ssh_agent_dir()` in both `install.sh` copies (new, ~15 lines). Both work as-designed; simplify would check for obvious quality issues. Low priority.

## 4. Blockers
- **cairn and snowflake-projects are not git repos** — their devcontainer.json + docker-compose.yml fixes are on disk only. Same as before; low priority.
- **`~/.ssh/agent/` dir still exists on host** — now unused by any container mount. Safe to `rmdir` but not blocking.
- **borg-collective/dotfiles/ orphan subtree** — still undeleted. Partially overlaps with `~/.config/dotfiles/` but was also the target of the ssh-agent devcontainer fix commits (`dbbd5cd`). Needs a diff-and-decide pass: is any of that subtree meant to stay? Likely fully redundant with `~/.config/dotfiles/` after this session's work.

## 5. Next Session
**Start here:** switch to ingle (container is up on the new config, SSH verified). The session's original backlog item was Phase 1 of `2026-04-17-hosted-connector-architecture.md`: context abstraction refactor for the MCP server.

Exact first edits:
1. Create `/Users/noah/dev/ingle/mcp-server/src/context.ts` with a `getContext(extra): Promise<{householdId: string}>` helper. Env-var path reads `process.env.INGLE_HOUSEHOLD_ID`; JWT path is stubbed with a TODO. Fails loudly if both are missing.
2. For each file in `/Users/noah/dev/ingle/mcp-server/src/tools/*.ts` (pantry, lists, meals, family, shopping, budget, household, predict), replace direct `process.env.INGLE_HOUSEHOLD_ID` reads with `const { householdId } = await getContext(extra)` inside each tool handler.
3. Create `/Users/noah/dev/ingle/mcp-server/tests/` with a smoke test that asserts the context helper throws when both env var and JWT are absent.
4. Acceptance check: `grep -R "process.env.INGLE_HOUSEHOLD_ID" mcp-server/src/` returns zero hits outside `context.ts`.

After Phase 1 lands, the narrower sibling directive `2026-04-17-plugin-distribution.md` (rename `wayfinderai-waypoint` → `ingle`, fix `.mcp.json` to bundle the custom server, update skills to MCP tool names, add marketplace.json) can happen any time.
