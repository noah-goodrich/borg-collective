## 1. Goal

Finish the executable remainder of the cairn-decommission directive so no cairn artifacts linger in borg, and
run `/borg-plan` on the link-unification directive — two independent tracks opened in one instruction.

## 2. Accomplished

**Root-caused "why are you still referencing cairn?"** — not the stray `pipx` binary. `~/.claude/skills/` was
never redeployed after the decommission, so the *deployed* `borg-link/SKILL.md` still listed `cairn search P`
in its data-contract table (which is why this session opened by running it) and `borg-collective-review/SKILL.md`
still instructed `cairn convene` (which is why the Collective Review ended in a failed call). Redeployed via
`install.sh`; orphaned `borg-search` skill removed; zero cairn references remain in `~/.claude/skills/`.

**Four PRs merged. `main` at `736886d`.**

- **#122 `ea2f5a0`** — cairn Phase 1: item 1.2 (checkpoint truncation → awk sections 4+5 with a legacy
  byte-cap fallback, 3 tests), 1.3 (the missing CLAUDE.md pointer), 1.4-partial, 1.5 (four platform plugins),
  and 1.6 below.
- **#123 `411333b`** — usage-watch probe-transcript prune, 5 tests.
- **#126 `28a47c3`** — collapsed the prune's double `find`; a `/simplify` fix that was made and tested but not
  committed before #123 merged.
- **#127 `736886d`** — `PROJECT_PLAN.md` + resequenced layout directive, now on `main`.

**Phase 1.6 had never once run.** `bin/borg-memory-gate:47` used `${0:A:h}` — a zsh expansion — in a bash script
under `set -euo pipefail`. Bash evaluated `A` as arithmetic and aborted with `A: unbound variable` before the
`||` fallback could fire. It reached that line only because `command -v memory-hits-report` always failed
(`borg setup` installs it to `~/.claude/bin`, on neither PATH). Invisible because **every test in
`memory_gate.bats` exports `BORG_MEMORY_GATE_REPORT_BIN`, so the resolution block never executed under bats** —
the same shape as the usage-watch blindness bug, in a new component. Fixed with portable symlink-following
resolution + 2 tests that fail against the old line. `launchctl` now reports exit 0 on a real launchd fire.

**First verdict delivered: `FAIL, ratio=0.027`** vs the pre-registered `< 0.2 reads/session`. By the directive's
own criterion, auto-memory is a second write-only store — the cairn replacement failing cairn's own test, ~7x
under the null. Also fixed the causal doc bug: CLAUDE.md said `borg setup` installs launchd jobs; only
`install.sh` does, which is exactly why 1.6 sat undeployed.

**`~/.claude/projects/-` was not dead-poller residue** as the directive assumed — `usage-watch` runs
`claude -p "/usage"` every 120s with no `WorkingDirectory`, persisting a transcript per probe (15,108 files /
60MB since Jul 12). Added age-based self-pruning. Rejected the plist `WorkingDirectory` change on evidence:
`/` is a *declared quarantine* (`borg-link-down.sh:39`), so relocating would abandon a guard that already exists.

**Link port planned.** A 5-agent scoping workflow corrected three of my own claims — **L4's `---` bug does not
reproduce**, L5's baseline is 160 not 83, and `_borg_read_assimilated:147` has a real `(NOm)` bug listing
*oldest* plans as "Recently assimilated." Adversarial challenge raised the estimate to 30-40h and found the
governing conflict: parity cannot be proven against a target being simultaneously redesigned. Split accordingly.

## 3. Ready to Commit

Nothing. All four PRs merged, `main` synced, no session work uncommitted. `/simplify` ran (one finding, shipped
as #126). Untracked `.borg/checkpoints/*` and `docs/research/*` are pre-existing and untouched.

## 4. Blockers

- **Two host commands blocked by the auto-mode classifier**, needing Noah's hand:
  `rm -f /Users/noah/.config/borg/plan-promote-debug.log && pipx uninstall cairn`. The 15k-file delete is no
  longer needed — the merged prune reclaims it automatically on first poll.
- **Phase 1.6's proof obligation is calendar time, not work.** Re-check on/after **2026-08-19**; the signal is
  whether `~/.local/state/borg/memory-gate.log` has entries nobody triggered.
- **CI has no pinned Python on either bats lane.** `test` (ubuntu) installs `zsh jq fzf`; `contract-macos`
  installs `jq`; neither runs `setup-python`. Survivable today (only low-traffic arms dispatch to Python) but
  **must be fixed before port Phase 2**, when the daily path starts dispatching. Not a Phase 0 blocker.
- **Flaky test**: `contract: watch dispatches into the live-refresh loop (background+kill)` polls a 5-second
  window for first output. Failed once on #122, passed on re-run with no code change.

## 5. Next Session

**Start at Phase 0 of `PROJECT_PLAN.md` (now on `main`, the hook will surface it). Switch to `/model sonnet` —
planning is done and the plan is locked.**

Phase 0 is the parity harness and **nothing else** — 14-18 cases in `tests/cli_contract.bats` written against
today's zsh, green on zsh, committed before any port work begins. They must later pass **unchanged** against
Python; an edited assertion is not a parity proof. Cover all four output modes (overview, deep dive,
`--porcelain`, `--all`) plus the three external consumers:

- `drone.zsh:963` — greps `Status:` out of the human-readable deep dive (undeclared cross-CLI API, no test today)
- `drone.zsh:1405` — `drone link` execs into `borg link` with the CWD basename as an implicit project arg
- `borg.zsh:689` — `cmd_switch`'s fzf `--preview "borg link {1}"`, which runs on a pipe, not a tty

Also pin: unknown-flag tolerance (`borg link --bogus` and `--help` both render the overview and exit 0 via
`-*) shift ;;` at `borg.zsh:226`), `--llm` as an accepted alias for `--brief`, and `--porcelain` winning over a
positional project.

**viz-1 is NOT a constraint — verified, do not re-raise.** The layout directive warns "viz-1 should land first…
must not undo its tier," implying a shared surface. PR #125 (`feat/viz-1-awaiting-you-tier`, open) touches
**only** `merge-tree/` — `curate.py`, `render_graph.py`, `test_curate.py`, a fixture. Nothing in `borg.zsh` or
`cmd_link`. viz-1 is infoviz-program work and cannot move the parity harness underneath Phase 0.

**Optional, unfiled — do not file unprompted:** a directive on the recurring "instrument ships green, never
actually runs" pattern. Four instances now (usage-watch ×3, memory-gate ×1), identical root cause every time: a
test injects the env var that skips the broken path.
