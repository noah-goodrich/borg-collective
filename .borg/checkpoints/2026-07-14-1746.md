## 1. Goal
Finish the cairn-pollution plugin-guard deploy — fix the pre-existing red claude-plugins CI (the
borg-link-down project-mode JSON bug), rebuild the plugin, merge, deploy, and verify the synthetic
`query='/'` pollution stopped.

## 2. Accomplished
- **Root-caused** the weeks-old CI red: `borg-link-down.sh` appended cairn-hits.log via
  `printf ... >> "$BORG_DIR/cairn-hits.log" 2>/dev/null` — bash opens the redirect target *before*
  applying `2>/dev/null`, so a missing `$BORG_DIR` leaked a stderr open-error that `bats run` spliced
  into the JSON stdout, breaking `jq` ("Invalid numeric literal at line 1, column 88" = 87-char CI
  hook path + `: line N:`). `$BORG_DIR` was absent because the bats setup overrode `HOME` but not
  `XDG_CONFIG_HOME`, and the hook recomputes `BORG_DIR` from `${XDG_CONFIG_HOME:-$HOME/.config}/borg`.
  Reproduced locally by setting `XDG_CONFIG_HOME` to a missing dir (Ubuntu 24.04 / bats 1.10).
- **Fixed** by brace-grouping the redirect: `{ printf ... >> "$BORG_DIR/cairn-hits.log"; } 2>/dev/null`.
  Added a `tests/lifecycle.bats` regression test (fails on the old form with the identical `jq` error).
- **Shipped:** borg-collective #77 (fix + test) and claude-plugins #33 (rebuilt 0.8.8, CI green — the
  BATS suite was red since #14, merged-over-red at #30/#31) — both merged to `main`, v0.8.8 tagged.
  Verified zero build-drift from merged `main` before merging #33.
- **Deployed:** `borg setup` (deployed hook carries the fix; poller sets
  `BORG_NO_SPEND_RECORD=1 BORG_NO_SESSION_HOOKS=1`) + `claude plugin update borg-collective@noah-local`
  0.8.7 → 0.8.8, applied live via `/reload-plugins`.
- **Verified:** cairn `call_log` = 0 `project='/'` rows in the last 24h; `/stats/usage` shows no `"/"`
  project (last 2h only real projects: stillpoint, ingle, cairn, troth).
- Assimilated the tracking directive (`docs/plans/assimilated/2026-07-13-finish-plugin-guard-deploy.md`);
  captured the bash redirect-open gotcha in CLAUDE.md **Learned** + memory
  `reference_bash_redirect_open_stderr_leak`.

## 3. Ready to Commit
Nothing — working tree is clean, everything committed and pushed to `main`.

## 4. Blockers
No blockers.

## 5. Next Session
This thread is fully closed. Pick from the standing queue (highest-value first):
- **Noah's review queue** (the bottleneck): ingle **#269/#270**, troth **#33**, reveal **#7**.
- **Open borg-collective directives:** `docs/plans/directives/2026-07-08-usage-guardian-build.md` and
  `docs/plans/directives/2026-07-12-agent-roster-source-drift.md`.
- Cosmetic-only: commit `3f5adfc` bundled several legit-but-unrelated files (two `.borg/checkpoints`,
  the `2026-07-12` directive, `docs/research/`) under a directive-assimilation message — left
  intentionally; splitting would need a `main` history rewrite for zero content benefit.
