## 1. Goal

Answer "what are the open directives, and do we have a current PROJECT_PLAN?" — which turned into verifying whether
two specific directives are still real work, and then triaging the whole backlog into what an agent can clear versus
what only Noah can do.

## 2. Accomplished

**No PROJECT_PLAN.md exists.** `borg link --json borg-collective` returns `plan: null`. That is the correct
post-ship state — `2026-08-12-port-borg-link-to-the-python-core.md` was assimilated 2026-08-14 at `a9fdb45`. The board
is between plans; any code work needs `/borg-plan` first. **14 open directives** enumerated.

**`2026-08-11-link-unification-and-layout` is NOT complete — 3/3 adversarial skeptics refuted closure, high
confidence.** The directive's own 2026-08-12 revision header reassigned L1/L2/L6 to the port plan. Only **L3
(bottom-anchored layout)** and **L5 (re-measure + record line count)** remain, and both have **zero work**. Scoped to
its post-revision charter it is 0-for-2, not "partially satisfied."

- L3 fails its own verify command live: `borg link | tail -5` ends on `▸ 5 sessions need attention (limit: 3)` — no
  next action, no command. `borg_core/link/render.py:250-303` is a deliberate byte-for-byte transcription of the
  pre-port zsh branch order (parity was the port's whole acceptance criterion).
- L5 regressed: `borg link | wc -l` = **169** vs the corrected 160 baseline, 86 worse than the original 83. No PR in
  `#129..#143` touches layout.

**Three findings not captured by any directive:**

1. **127 of the 169 lines are an uncapped directive dump** — `render.py:215-220` prints one bullet per pending
   directive with no cap. ~75% of the output. **No criterion anywhere covers it**, so the "fits one screen" goal (D1)
   is arithmetically unreachable as written. L3 only reorders; L4's idle-collapse (~15 lines) was withdrawn.
2. **L6 is vacuous and will need re-establishing.** "Regression: stays green" is relative to a change; with no change,
   651/651 proves nothing. Four byte-exact goldens under `tests/fixtures/link/` pin the exact output L3 must rewrite —
   L3 needs a deliberate golden-regeneration strategy that does not exist yet.
3. **L1's actual sentence is false.** It says "mirroring the `borg recon --json` contract." The two documents share
   exactly **one** top-level key (`generated_at`). What shipped is a JSON assembly seam — useful, but not that.

**`2026-07-08-usage-guardian-build` reconstructed.** Prevention half of the 5-hour-cap problem (`borg-resume` is the
recovery half). launchd poller samples `claude -p "/usage"` every 120s; at **85%** it checkpoints every live Claude
pane via two-step `tmux send-keys /borg-link-up`, at **92%** a `PreToolUse` hook vetoes new `Agent`/`Workflow`
dispatch. Design spine: *fail closed on reading, fail open on acting*; two-step send-keys (bundled `Enter` lands as a
literal newline); `USER` in the plist or the poller goes silently blind.

- **Built, green (57/57 bats), and completely inert.** Both acting halves default-OFF
  (`bin/borg-usage-watch:55`, `hooks/borg-dispatch-guard.sh:29`) and **nothing arms them** — no reference in
  `launchd/`, `install.sh`, or `scripts/`. `~/.config/borg/usage-guardian.json` has never been written.
- **The July blocker has dissolved.** Tuning was blocked on one near-cap episode; the samples file now holds
  **19,170 rows with >=85% readings on 14 distinct days** (07-09 → 08-13).
- Three small mechanical gaps: adaptive 60s cadence at >=70%, `week_pct >= 90` warn, alert-after-3-consecutive-UNKNOWN.

**Exact arming commands handed to Noah**, with the wiring reason: the two halves live in **different environments**.
The sweep runs under launchd (plist passes only `USER`/`HOME`/`PATH`, so a shell export is inert); the guard is a hook
spawned by Claude Code (so the var must be in the environment *at launch*, not set mid-session).

**Cross-cutting defect:** PR links in the port plan resolve under `github.com/noah-goodrich/borg-collective`, not
`stillpoint-labs`. Already filed against `2026-08-14-link-port-simplify-followups.md` for one file — it is wider than
that.

## 3. Ready to Commit

**Nothing from this session.** It was pure orchestration and analysis — no files were created or edited. Every
untracked path in `git status` predates this session (8 checkpoints, the `docs/research/` tree from the
clean-architecture research pass). `/simplify` is not applicable; there is no session-touched code to review.

## 4. Blockers

**The backlog triage workflow was still in flight when the session checkpointed.** Run ID `wf_368574ec-d46`, 14 agents
(12 per-directive triage → sequence → adversarial critic), covering every open directive except the two analyzed above.
Resume with:

```
Workflow({scriptPath: "/Users/noah/.claude/projects/-Users-noah-dev-borg-collective/515f81de-f3c4-49fd-b78c-8e3a39fa812a/workflows/scripts/backlog-triage-wf_368574ec-d46.js", resumeFromRunId: "wf_368574ec-d46"})
```

Completed agents replay from cache. **Read `journal.jsonl` in the transcript dir before assuming a cached result is
non-empty.** Without it, the dispatch plan for 12 of 14 directives does not exist.

**Two live verifications are blocked on Noah and cannot be delegated.** Bats can prove the guard *emits* exit 2; it
cannot prove Claude Code *honors* it, and no agent can observe a live cap approach. Until both pass, arming the
guardian permanently is premature — a veto the harness ignores is worse than no veto.

**Capacity is over limit:** 4-5 active/waiting against a limit of 3, and the backlog is 14 directives. The backlog size
is itself a finding; the triage was told to bias toward closing and parking rather than working.

## 5. Next Session

**Start by resuming `wf_368574ec-d46`** (command above) — it is the input to every dispatch decision and re-running it
from scratch wastes ~14 agents.

**Then run the two verifications, in this order.** They are cheap and they gate the guardian directive:

```bash
tail -1 /Users/noah/.local/state/borg/usage-samples.jsonl
BORG_USAGE_SWEEP_ENABLED=1 BORG_USAGE_CHECKPOINT_PCT=5 /Users/noah/dev/borg-collective/bin/borg-usage-watch --once --debug ; cat /Users/noah/.config/borg/usage-guardian.json
```

Sends `/borg-link-up` to every live Claude pane — that is the mechanism under test. Idempotent per window; reset with
`rm -f /Users/noah/.config/borg/usage-guardian.json`. Then, in a throwaway session, `BORG_USAGE_HALT_ENABLED=1
BORG_USAGE_HALT_PCT=5 claude` and ask it to spawn a subagent — expect a denial. The guard ignores samples older than
300s, so confirm the `ts` in that first command is fresh.

**Do not re-run a defect search on the link port.** Closed explicitly by the reviewer after three rounds; the fourth
found zero live defects. Every remaining finding is latent and has a directive.

**Before working `link-unification-and-layout`, rewrite it.** Cut it to L3 + L5, mark L1/L2/L6 "carried by the port,"
and **add a criterion capping the directive dump** — without that the D1 one-screen goal cannot be met by any
combination of the existing criteria. Pin the baseline to a fixture, not the live board: 169 grows with the registry.
It is `needs_plan_first`, not dispatchable — layout is a taste call plus a golden-regeneration strategy.

**Still unfiled:** the deployed-artifact drift check. Nothing verifies that what `borg setup` writes to
`$HOME/.claude/skills/` and `/Users/noah/dev/claude-plugins/borg-collective/skills/` matches repo source; both pinning
tests (`tests/skill_borg_link.bats:17`, `tests/bash_guard.bats:654`) read repo source only. It covers all 16 skills and
12 hooks and is the generalizable fix for the A6 failure — verified, ticked, then invalidated 40 minutes later by
[#139](https://github.com/noah-goodrich/borg-collective/pull/139) editing the verified artifact.

The transferable lesson from this session: **a directive's own checkboxes are not evidence, and neither is a green
suite.** Both directives examined were misread from their paperwork — one looked done because a *different* plan
shipped its criteria, the other looked shipped because 57/57 tests pass on code that has never once executed its own
acting path.
