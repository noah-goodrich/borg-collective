## 1. Goal

Decide how to close the cairn-decommission directive, then continue Part 3 of the python-core-and-toolchain
migration, then commit/push/merge/assimilate whatever was completed.

## 2. Accomplished

**Cairn directive** — left open (user's explicit call, not closed). Header corrected: the memory-gate job
(`bin/borg-memory-gate` + its launchd plist) exists in the repo but was never actually deployed via `borg setup`
— no plist in `~/Library/LaunchAgents`, `launchctl list` shows nothing, no log/state file. The directive's own
7-day unattended-proof clock hasn't started. Committed as `12527cf`.

**`add`/`rm` migrated to Python** — `borg_core/registry/{core,shell,cli}.py`, 97% coverage, mirroring the
`recon` module's established core/shell/cli split. `cmd_add`/`cmd_rm` deleted from `borg.zsh`, dispatch rewired.
An independent 3-lens adversarial review (Workflow: parity/bugs/scope, each finding separately re-verified)
found 7 distinct real issues; fixed 6:
- **Blocker fixed**: `file_mtime_iso` first draft computed genuine UTC where zsh's `stat -f %Sm` actually wrote
  local time mislabeled as UTC — and `lib/reaper.sh`'s `-u`-forced read-side parse had been silently cancelling
  that out. Reverted to `time.strftime(..., time.localtime(mtime))` to preserve the (load-bearing) bug, with
  the reasoning documented in `shell.py`'s docstring and pinned by a TZ-forced test.
- Dropped `argparse` for direct `argv` indexing in `cli.py` — zsh's `cmd_add`/`cmd_rm` had zero flag surface
  (only ever read `$1`), so argparse's extra-positional-arg rejection and auto `-h`/`--help` interception were
  both real divergences. Fixed by matching zsh's permissive `$1`-only reads exactly.
- `read_registry()` now raises a clean `ValueError` on malformed JSON instead of an unhandled traceback
  (deliberately does NOT silently degrade to an empty registry — would risk overwriting a corrupted-but-
  recoverable file on the next write).
- `claude_encode_path` now strips exactly one trailing slash (was `rstrip("/")`, stripping all).
- TOCTOU race in `claude_latest_session_id`'s `max()`/`stat()` fixed with a `_mtime_or_epoch` helper.
- Left unfixed (accepted precedent, not a regression): bare/unstyled `_die`/print output — matches the
  already-shipped `recon` migration's own `cli.py`.

**PROJECT_PLAN.md accuracy pass** — Part 1 (T1-T6) and Part 2 (P1-P4) were substantively complete but never
checked off; corrected with inline verification evidence. T5's coverage-threshold deviation (90% floor set
before a baseline existed, contrary to the item's own instruction) documented rather than silently passed.
Post-C5 sequencing revised: `link` deferred (blocked on the independent, unstarted
`2026-08-11-link-unification-and-layout.md` directive), `scan` un-bundled from `add`/`rm` (it's a multi-source
discovery engine with unported dependencies, not "registry CRUD, pure logic" as originally framed).

**Shipped**: committed (2 commits: `12527cf` docs fix, `4c637f2` migration), pushed, PR #120 opened and
squash-merged to `main` as `c760514` (CI: `lint`/`python`/`contract-macos` green; ubuntu `test` job's 6
failures confirmed pre-existing/unrelated — same failures on `main` before this branch started). Ran
`/simplify` (flagged one deferred cross-module duplication — `borg_dir()` now identical in
`borg_core/recon/shell.py` and `borg_core/registry/shell.py`, correctly left unextracted per rule-of-three),
evaluated all 17 acceptance criteria with evidence, ran The Collective Review (verdict: ship), archived the
plan to `docs/plans/assimilated/2026-08-11-python-core-toolchain-and-enforced-clean-architecture.md` with an
"Additional Work Shipped" section, removed `PROJECT_PLAN.md` from root, committed and pushed (`3b3013a`).

Final verification: bats 585/585 (local macOS), pytest 147/147 @ 97% coverage, ruff/mypy/pylint 10.00/10.

## 3. Ready to Commit

Nothing — everything from this session is committed and pushed to `main` (`3b3013a`). Working tree is clean
of tracked changes. `/simplify` was run on this session's files as part of the `borg-assimilate` flow.

Untouched, pre-existing untracked files from earlier sessions remain in the working tree (not part of this
session's work, left alone deliberately): `.borg/checkpoints/2026-08-10-0000.md`,
`docs/research/2026-08-12-clean-architecture-for-ai-agents/`, and the `docs/research/sources/*.md` files.

## 4. Blockers

No blockers on the shipped work. Three known follow-ups recorded in the archived plan (not blocking, not
started):
1. Extract `borg_dir()` to a shared `borg_core` module if a third command duplicates it (currently duplicated
   verbatim in exactly two places — `recon/shell.py` and `registry/shell.py`).
2. `borg add -h`/`borg rm -h` no longer print help text (reverted to zsh's literal-data treatment for parity).
   If help output is wanted, that's a deliberate, separately-shipped change.
3. The ubuntu `test` CI job has 6 pre-existing, unrelated failures (`nanoprobes`/`nanoprobe-log`/`regenerate`
   fixtures) — confirmed present on `main` before this session, not introduced or worsened here, but still open.

## 5. Next Session

Pick up Part 3's migration track at `next` (the plan's revised sequencing: `recon` → `add`/`rm` done →
`next` → `scan` (own pass) → `nanoprobes`/`spend`/`watch` → `link` once link-unification ships →
`switch`/`focus`/`init` last). Mirror the established pattern: `borg_core/next/{core,shell,cli}.py`, contract
tests already exist in `tests/cli_contract.bats` for `next`, adversarial-review the diff before merging same as
this session.

Separately, three untouched items worth a decision:
- The cairn-decommission directive (`docs/plans/directives/2026-08-08-cairn-decommission-and-unconditional-block.md`)
  is still open — re-check in 7+ days once (if) `borg setup` is rerun to actually install the memory-gate
  launchd job, per its own corrected header.
- The pre-existing ubuntu CI `test` job failures (6 tests, `nanoprobes`/`nanoprobe-log`/`regenerate`) are worth
  a dedicated fix pass — likely GNU vs BSD tool differences given the fast-lane's own documented limitations.
- Untracked research deliverables from an earlier session (`docs/research/2026-08-12-clean-architecture-for-ai-agents/`
  and related source files) are still uncommitted — decide whether to commit them or if they're intentionally
  staged elsewhere.
