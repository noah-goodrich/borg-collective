## 1. Goal

Execute the Fable-5 "zero-hour" plan — direct Fable's reasoning at durable framework/harness artifacts across
borg-collective / claude-plugins / cairn before Fable leaves the subscription tier — while cleanly handing off
the parked portfolio-bet research (reveal/ingle/troth) to Opus/Sonnet for tomorrow.

## 2. Accomplished

**Cost/routing fix (the live pain — WS3):**
- Root-caused the credit burn: the main loop runs on Fable 5 AND Workflow `agent()` calls silently inherit it,
  so ~40 workflow agents/session ran on Fable 5 (tripped 3 session limits + 1 weekly limit this project).
- `claude-plugins/borg-collective/agents/ROUTING.md`: added a "Model routing inside Workflow scripts" section
  (every `agent()` must pass explicit `model:`) + refreshed the cost table (Fable 5 $10/$50 = priciest tier).
- `~/.claude/settings.json`: flipped default model `claude-fable-5[1m]` → `claude-opus-4-8` (next session).
- Drafted (NOT applied) a CLAUDE.md "Workflow model routing" rule at
  `borg-collective/docs/research/2026-07-07-fable5-zero-hour/01-claude-md-workflow-rule.md`.
- Audit writeup: `.../00-cost-and-routing-audit.md`. Tracker: `.../EXECUTION-PLAN.md`.

**fable-reviewer skill (WS4):** authored `borg-collective/skills/fable-reviewer/SKILL.md` (5-gate discipline
with an emittable ledger; grounded in the real `pytest`/`bats` commands). Mirrored to
`claude-plugins/borg-collective/skills/fable-reviewer/` so it loads.

**Orchestration architecture (WS2):** `borg-collective/docs/orchestration-architecture.md` — frozen spec
(orchestrator-worker only, Conductor role, handoff state-machine, 10 frozen decisions, open questions).

**Security audit (WS1):** `.../03-security-audit-triaged.md`. Cheap Sonnet reviewers found 23 issues; Fable
triaged. Headline: bash-guard has a CRITICAL — any command containing the string `.borg-project` is
unconditionally pre-approved (`hooks/bash-guard.sh:66`) + ~12 matcher-gap bypasses. cairn is mostly
design-boundary EXCEPT `CAIRN_API_HOST` defaults to `0.0.0.0` (K1) + write-path integrity bugs. Fixes
specified, NOT applied (queued as nanoprobe jobs gated by new bats/pytest tests).

**Skill distillation (WS3b — bonus):** `research-tools/skills/research/SKILL.md` distilled 1001→550 lines
(45% cut) via a Fable-authored rubric + preserve-verbatim list (`claude-plugins/docs/SKILL-DISTILLATION-RUBRIC.md`).
Independent grep gate (23 literals) + blind Sonnet operational review caught 6 dropped clauses the self-check
missed → Haiku grunt restored → re-verified → SWAPPED live (backup `SKILL.md.pre-distill.bak`).

**Research handoff:** evidence phase COMPLETE (51 blind-verified cards, 0% failure); synthesis + decision-design
parked. Canonical resume doc: `/Users/noah/dev/docs/research/2026-07-03-portfolio-bet/STATUS.md`. Two memory
files written: `project_portfolio_bet_research.md`, `project_fable5_zero_hour_plan.md`.

## 3. Ready to Commit

Two git repos have my changes. **Commit only the files listed — both repos also contain pre-existing untracked
files from earlier sessions (do NOT sweep those in).**

**`claude-plugins/`:**
- `M borg-collective/agents/ROUTING.md` (my edit)
- `M research-tools/skills/research/SKILL.md` (distilled — verified; a human diff-read vs `SKILL.md.pre-distill.bak`
  is advised before commit since it is load-bearing behavioral config)
- `?? borg-collective/skills/fable-reviewer/` (mine)
- `?? docs/SKILL-DISTILLATION-RUBRIC.md` (mine)
- `?? research-tools/skills/research/SKILL.md.pre-distill.bak` (backup — gitignore or delete after commit; do
  not commit the backup)

**`borg-collective/` (commit ONLY these — the other untracked files predate this session):**
- `?? docs/orchestration-architecture.md`
- `?? docs/research/2026-07-07-fable5-zero-hour/` (00 audit, 01 CLAUDE.md rule, 03 security audit, EXECUTION-PLAN)
- `?? skills/fable-reviewer/`

`/simplify` not run and largely N/A — changes are markdown docs/skills, not code. The one behavioral file worth a
human review before commit is the distilled `research-tools/skills/research/SKILL.md` (already passed an
independent grep gate + blind operational review this session).

Global (not a project repo, already applied): `~/.claude/settings.json` model flip; two memory files under
`~/.claude/projects/-Users-noah-dev/memory/`.

## 4. Blockers

No hard blockers. Notes: (a) the research pipeline hit repeated session/weekly usage limits — resolved by
resume-after-reset and by parking synthesis for Opus. (b) ROUTING.md source-of-truth ambiguity: the
borg-collective source repo has NO `agents/ROUTING.md` — it lives only in `claude-plugins/borg-collective/agents/`,
so my edit is not at mirror-overwrite risk, but confirm whether `agents/` is native to claude-plugins or expected
to sync from somewhere.

## 5. Next Session

Restart onto **Opus 4.8** (now the default) for everything below — none of it needs Fable.

1. **Commit today's work** per section 3 (two repos; only the listed files). Consider gitignoring `*.pre-distill.bak`.
2. **Resume the parked portfolio research** — read `/Users/noah/dev/docs/research/2026-07-03-portfolio-bet/STATUS.md`
   first, then run: `Workflow({ scriptPath: ".../portfolio-synthesis-wf_d1b1baf7-2be.js", resumeFromRunId:
   "wf_d1b1baf7-2be" })` (cached t5 returns instantly; 5 synth agents + bibliography re-run on Sonnet). Then
   Phase 6 analysis.md → decision-design D2–D6. Task list #3–#6 track this.
3. **Security follow-ups** (from `borg-collective/docs/research/2026-07-07-fable5-zero-hour/03-security-audit-triaged.md`):
   nanoprobe to apply the bash-guard normalization fix — start with the CRITICAL `.borg-project` pre-approval
   (`hooks/bash-guard.sh:66`) — WITH new `bats tests/bash_guard.bats` regression cases (none exist today). Then
   cairn K1: default `CAIRN_API_HOST` to `127.0.0.1` (`cairn/src/cairn/api.py:264`).
4. **Apply the CLAUDE.md workflow rule** (`.../01-claude-md-workflow-rule.md`) via the update-config skill.
5. Optional: distill the next-heaviest skills (consistency-audit 236, borg-collective-review 214, borg-plan 204)
   using `claude-plugins/docs/SKILL-DISTILLATION-RUBRIC.md` — Opus/Sonnet can do this; no Fable needed.
