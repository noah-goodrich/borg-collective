## 1. Goal

Clear the PR review queue and push the merged work all the way live — dotfiles sync, gated merges, and the
deploys behind them (bash-guard hardening, cairn transport attribution, observability standard).

## 2. Accomplished

- **dotfiles** — synced. gh credential helper + PR #4 (GH_TOKEN stale-export) merged; **TCC-popup self-heal shipped**
  (`claude-tcc-heal` + `DISABLE_AUTOUPDATER` export, commit 65db927) so any machine repairs itself after a Claude
  update. Verified idempotent no-op on this (fixed) machine.
- **cairn #15** merged (attribute every ledger call to its transport). Service **rebuilt from main**; recovered a
  down **`dev-postgres`** (was missing on host — brought up against existing volume `devcontainer_dev-postgres-data`);
  attribution **verified live** — a REST search now logs `source=rest` (historical 513 rows stay `unknown`, expected,
  no backfill).
- **borg-collective #74** merged — but only after the **verify gate caught a real Layer-1 security regression**. The
  normalization rewrite opened wrapper-prefix (`sudo`/`env`), bare-`&`/subshell, and wrapper-with-arg (`sudo -u root`,
  `nice -n 5`) bypasses of the unappealable hard-block. Fixed across 2 iterations (final design: scan for a dangerous
  `rm`/`chmod` token anywhere in a segment, over-block in the safe direction). Verified 31/31 probes + 419/419 bats +
  shellcheck clean.
- **claude-plugins #31** merged — synced the #74 hardening into the plugin mirror, bumped `borg-collective` 0.8.6→0.8.7,
  deployed locally. **This session's live hook is now hardened** (31/31). Foreign `borg-link-down.sh` work on the
  `fix/skip-cairn-for-synthetic-sessions` branch preserved untouched.
- **stillpointlabs-site #9** merged (OBSERVABILITY / GlitchTip error-tracking standard).
- **herdr.dev** rapid research brief produced (`borg-collective/docs/research/2026-07-10-herdr-dev/` + epub). herdr = an
  off-the-shelf agent multiplexer that overlaps borg's tmux orchestration — worth a build-vs-borrow look; shares borg's
  state-detection fragility.
- **Memories:** saved `dev-postgres-recovery`; rewrote `claude-autoupdate-tcc-popup` with the full two-part fix.

## 3. Ready to Commit

Nothing pending from the orchestrator — all shipped work is merged/pushed. Two notes, both intentionally left alone:
- `claude-plugins` working tree (branch `fix/skip-cairn-for-synthetic-sessions`) still has uncommitted
  `borg-link-down.sh` (foreign work) + a now-**redundant** `bash-guard.sh` delta (that change also landed on main via
  #31). Belongs to that branch's owner — do not clobber.
- cairn image was rebuilt but `__version__` is still `0.4.0` (no bump commit), so `/health` under-reports the build.

## 4. Blockers

- **#9 next-phase wiring is gated on DSN provisioning** — the GlitchTip acceptance checklist can't pass until each
  project has a `GLITCHTIP_DSN` (create the project in GlitchTip at localhost:8101 → store secret). That's a
  browser/Noah step before code wiring is verifiable.
- `borg link --refresh` does NOT write registry summaries in this version (only re-renders the overview) — cosmetic.
- Restart Claude Code to fully apply the plugin-manager 0.8.7 update (the hook file is already live and passing).

## 5. Next Session

1. **#9 GlitchTip wiring** — provision per-project DSNs first, then wire the SDK per
   `stillpointlabs-site/docs/standards/OBSERVABILITY.md`. Recommended order **ingle → troth → reveal** (reveal last,
   EXP-0-gated). Per project: SDK init at entrypoint + `environment`/`release` tags + PII `beforeSend` + a test-throw
   that reaches the dashboard.
2. **Noah's review queue** (still the bottleneck): ingle **#269** + **#270** (write-path D2 — #269 needs a *product*
   decision, not a patch), troth **#33**, reveal **#7**.
3. Optional cleanup: bump cairn `__version__` past 0.4.0; tidy the `claude-plugins`
   `fix/skip-cairn-for-synthetic-sessions` branch (its bash-guard delta is now redundant with main).
