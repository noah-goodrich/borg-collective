---

# Session Debrief: borg-collective
*Date: 2026-04-16 17:21*

## Objective
Create a cross-project portfolio directive capturing the Supabase + Fly.io MVP pivot decision for Waypoint and wallpaper-kit, and wire up project-level Claude settings for borg-collective.

---

## Outcome

**Files created/modified:**

| Path | Status | What |
|---|---|---|
| `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md` | Created (untracked) | Strategic directive: Waypoint + wallpaper-kit pivot off Snowflake → Supabase + Fly.io |
| `.claude/settings.json` | Created (untracked) | Enables `supabase@claude-plugins-official` plugin for this repo |
| `.claude/settings.local.json` | Created (untracked) | Local permission allowlist (WebFetch, Read, Bash overrides) |
| `.gitignore` | Modified | Added `.borg/` to ignore list |

**Nothing shipped** — all three changed items remain uncommitted.

---

## Decisions Made

**1. Snowflake/Cortex/SPCS fully out of MVP path for both apps.**
Cortex Code evaluation (`~/dev/cortex-handoffs/`) confirmed Snowflake Postgres costs ~$10/mo with zero user benefit at Stage 1. SPCS adds $50/mo warm-pool floor and 10–30s cold starts. Both apps use Supabase free tier instead, with a revisit gate at ≥1K paying users.

**2. Two separate Supabase projects (not shared).**
Clean kill/sell boundary. Either app can exit independently.

**3. MCP server demoted for Waypoint.**
Reversed the v0.2.0 Snowflake/MCP-first plan. Mobile-first Next.js PWA is now primary surface; MCP is an optional power-user integration. Cancelled plan preserved at `wayfinderai-waypoint/docs/plans/severed/`.

**4. Combined infra cap: ≤ $25/mo pre-launch.** No Stripe/pricing/checkout in MVP — free closed beta, pricing validated by user conversation.

**5. `.borg/` excluded from git.**
Ensures local borg runtime state doesn't pollute the repo.

---

## Blockers and Observations

**Malformed permission entry in `.claude/settings.local.json`** (line 22):
```json
"Bash(matters most\" — all research citations preserved, narrative shifted from deficit framing:*)"
```
This is clearly an accidental injection — looks like prose from a content-writing task leaked into the allowlist. It is syntactically valid JSON but semantically a no-op permission that will never match. Low severity, but should be cleaned up before committing.

**Directive is dated 2026-04-14** but was created/tracked this session (2026-04-16). Two days of lag — the decision was made earlier and the artifact was formalized now. Not a problem, but means the related external-repo plans (`wayfinderai-waypoint/PROJECT_PLAN.md`, `wallpaper-kit/docs/plans/directives/`) may already be ahead of this borg-collective record.

**Wallpaper-kit rename is a hard blocker for Stream B Session 0.** Not owned by this directive, but nothing in Stream B can start until the branding decision lands.

---

## Next Steps

1. **Fix the corrupted permission in `.claude/settings.local.json`** — remove line 22 (`"Bash(matters most\"..."`) before committing. It's harmless but looks like a bug.

2. **Commit the three changes together** with a clear message:
   ```
   git add .gitignore .claude/ docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md
   git commit -m "chore: add portfolio MVP pivot directive + claude project settings"
   ```

3. **Verify external repo plans are in sync with the directive** — spot-check that `~/dev/wayfinderai-waypoint/PROJECT_PLAN.md` and `~/dev/wallpaper-kit/docs/plans/directives/2026-04-14-supabase-flyio-mvp-pivot.md` exist and match the stack decisions documented here.

4. **Resolve wallpaper-kit rename** before opening Stream B — it is the only thing blocking Session 0 of that work stream.

---

> ⚠️ **Note on transcript quality:** The provided "last 8KB" was entirely the session initialization skill-list attachment — no actual conversation turns were visible. This debrief was reconstructed from git diff, untracked file contents, and the directive document itself.
