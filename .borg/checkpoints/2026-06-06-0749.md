## 1. Goal

Turn the `deep-research` + `brainstorm` skills on themselves — research the state of the art in research/
brainstorming/invention, adversarially audit our own skills + 21-artifact corpus, and surface a thesis (plus
buildable directives) for the next-gen evolution of the research-tools plugin.

## 2. Accomplished

All work landed in **`/Users/noah/dev/claude-plugins`** on branch **`meta-research-review`** (NOT borg-collective —
borg-collective has no code changes this session). Two multi-agent workflows ran (dogfooding deep-research +
brainstorm on themselves).

**Workflow 1 — evidence + audit** (7 research tracks, 5 audit lenses, blind verification):
- `research-tools/docs/research/2026-06-05-meta-review/analysis.md` (1404L) — state-of-the-art report, full §1–§7
  manifest. Core framework: **Generate → Critique → Ground/Verify loop**; both humans and machines are strong at
  generate, weakest at verify. Key cited findings: AI citations 47–97.5% unsupported (DeepTRACE); AI ideation
  homogenizes ~10.7% unless diversity is designed for; self-consistency is the cheapest reasoning win; TRIZ
  contradiction matrix near-random (abandoned by Altshuller); A-Lab's "41 new materials" → ~0 on re-analysis.
- `.../audit.md` (474L) — right/wrong/ugly, file:line-cited. Headline: **our Phase 3.5 "blind" verification is
  self-certification in every shipped report, has never failed across 7+ runs, and is gameable** (reveal s11
  retroactive `cached/partial`; troth shipped 65 cards with 0 quote sections + no verification report). Root cause:
  **honor-system enforcement — the design can't tell honored from skipped.** Highest-leverage fix: convert the prose
  manifest into an executable post-check.
- `.../verification-report.md` — blind citation check, 16 verified / 0 failed / 3 inaccessible (0%, ≤5% band).
- `.../sources/` — 63 source cards (note: verifier tallied 62 vs 63 on disk — a live source-reconciliation gap,
  exactly what directive 01 catches).

**Workflow 2 — brainstorm + council** (3 edge-axes, 5 distinct options, 9-voice council with 9 real dissents):
- `research-tools/docs/brainstorms/2026-06-05-next-gen-research-invention.md` (791L).
- **Council pick: Option C — Fail-Closed Ground Gate**, ship HALF 1 MVV first, architected as a ground-ledger-shaped
  artifact so it later becomes Option E (the unifying "Ground Engine"). Axis balance is sequential: lead with
  (b) depth (make verify real) because distribution amplifies whatever ships and invention on a self-certifying
  substrate manufactures pseudo-inventions; then (a) invention (Contradiction Forge + executed probe), then
  (c) open distribution (the fail-closed gate IS the distribution asset).
- 6 directives in `research-tools/docs/plans/directives/`: **01 (spec-grade, ready)** fail-closed ground gate;
  02 card-integrity + real cuts; 03 evidence-floor + confirmation-skew (banner first); 04 de-dup skills + triage
  tier; 05 council dissent + contradiction forge; 06 scholarly-source adapter (OpenAlex/Semantic Scholar).

Workflow 2 first run failed (9 council agents returned prose instead of StructuredOutput); fixed by adding a
compact inline context + explicit "respond only via StructuredOutput" instruction and resumed from cache.

## 3. Ready to Commit

Nothing committed (user did not ask). All deliverables are **uncommitted on branch `meta-research-review` in
`/Users/noah/dev/claude-plugins`** (untracked `research-tools/docs/`). `/simplify` is N/A — these are markdown
research/strategy artifacts, not code. The plan file is at
`/Users/noah/.claude/plans/we-ve-run-a-bunch-structured-twilight.md`. No voice/AI-scoring pass was run on the
internal docs (template-bound dogfooding artifacts) — reserve that for a Ring-1 public showcase if pursued.

## 4. Blockers

No blockers. Directive 01 is spec-grade and self-contained; it uses the existing troth + reveal corpus as test
fixtures (both MUST fail the gate on replay).

## 5. Next Session

Per Noah: after `/clear`, **pick up the first thing in the line to fix — build Directive 01**:
`/Users/noah/dev/claude-plugins/research-tools/docs/plans/directives/01-fail-closed-ground-gate.md`.

Build (on branch `meta-research-review`): `hooks/deep-research-verify.sh` (no-model, 6 falsifiable assertions:
report exists · §6 has sample N + failure count + canonical band · band canonical · verifier-id ≠ synthesis-id ·
every card has `Access status:` enum + `## Verified Quote(s)` heading · corrected-during-verification = FAILURE) +
`hooks/deep-research-stop.sh` (Stop hook, blocks PASS, injects "NOT fact-checked") + register in the
`.claude-plugin/` manifest + `deep-research/SKILL.md` Phase 3.5 verifier-ID recording + honest fallback. HARD-cap
rejection to genuine integrity facts — NEVER reject on cosmetic enum nits (trains permanent bypass). Badge must say
"a distinct agent ran and the files prove it," explicitly NOT "blind"/"true" (context-blind, not model-blind).
Test via `drone exec` (devcontainer rule): troth → FAIL, reveal → FAIL, synthetic compliant → PASS, cosmetic-nit →
NOT rejected. Est 1.5–2 sessions. Then sequence 02→06; do not start a later directive until its prerequisite ships.
