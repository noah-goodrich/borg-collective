**tl;dr** — Audited what remains of the infoviz program, designed how borg and the employer plugin layer coexist,
and shipped AC7's verb half. Next session: merge the eight pushed branches, then start the merge-tree retirement.

## 1. Goal

Answer what remains to complete the infoviz work, then execute the agreed next steps — which grew into a plugin
coexistence design and AC7 execution.

## 2. Accomplished

**Infoviz completion audit** (14 agents, 7 dimensions, each adversarially verified). AC1–AC4 genuinely done;
AC5/AC6/AC7 are the entire remainder with no artifact in flight. Corrections the verifiers forced: AC7's skills leg
already passes, `make eval --skip-model` is not a valid `make` invocation, and five parented children block Step
0.75, not one.

**Plugin coexistence design** — `docs/research/2026-08-31-plugin-coexistence/recommendation.md`,
`AI-scoring: 100/100`, epub at `~/Documents/Claude/borg-collective/`. Six options, four-persona council, three blind
reviewers (revise ×3, none overturned). Reframed once Noah recalled the shim pattern: borg already ships it twice —
recon adapters (`recon-adapter-*` globbed off `BORG_RECON_ADAPTER_PATH`) and skill extensions
(`01-context`/`02-output`/`03-followup` for borg-plan and borg-assimilate) — and **both sockets are empty**, verified.

**Root-caused `make lint`.** Two prior diagnoses were wrong. Held pylint 4.0.8 and pylint-clean-architecture 1.5.2
fixed, varied only Python: 3.14 clean, 3.13 raises W9006 on `directory.glob`. Nothing pinned the interpreter, so uv
chose 3.13. Unblocked four directives whose acceptance criteria require `make lint` exit 0.

**Fixed 11 pre-existing `test-viz` failures** — a half-finished employer scrub pointing both directions. Ten in
`test_spine.py` had inputs sanitized (`SFP` → `WHP`) and expectations not; one in `test_render_graph.py` had the
reverse, fixed by generalizing the source to a Jira-key regex rather than putting the identifier back. Added a
negative case, verified by mutation. 344 passed, 95% coverage.

**Shipped AC7's verb half.** `borg program` → `borg chain`, tombstoned and arg-forwarding
(`borg program list` → `Run: borg chain list`), COMMANDS count still 25, bats 774 green.

**Filed four directives:** the orchestrator drone-handoff (untracked across four checkpoints), the shim
architecture, the `project` → `repository` rename (re-measured at **3142 occurrences across 97 of 139 tracked code
files**, not the plan's ~500 across 57+), and the merge-tree retirement with its thirty traps.

**Ratified AC7's three decisions** into `PROJECT_PLAN.md`: the grep clause scoped to COMMANDS (it could never pass
against the test-enforced REMOVED-tombstone convention), `borg chain` over `borg project`, and retiring
`merge-tree/programs.py` rather than renaming it.

## 3. Ready to Commit

Nothing staged. All work is committed across eight branches, **all pushed to origin**. Working tree carries only the
five untracked checkpoints and `uv.lock`.

**`/simplify` was NOT run this session.** Recommended before merging `feat/ac7-rename-program-to-chain` — review
`borg.zsh` (`cmd_chain`, the dispatch arm, the help entry) and `merge-tree/coordinator.py`. The other branches are
docs or one-line config.

## 4. Blockers

- **Two load-sensitivity flakes.** `borg_core/test_proc.py::test_the_work_runs_between_the_start_and_the_collect`
  (1.0s ceiling over two 0.6s children, 0.4s headroom) and `cli_contract.bats`'s
  "np alias reaches the same nanoprobes rendering" case. Both pass 3/3 alone, fail under full-suite load.
  Deliberately not "fixed" — widening a timing threshold after seeing an inconvenient number is what the cairn
  postmortem forbids. Until resolved, "suites green" is not a trustworthy gate.
- **`uv.lock`** — track or delete. Open across five checkpoints.
- **AC7 cannot be ticked** until the merge-tree retirement ships; two validators still disagree.

## 5. Next Session

1. **Merge the eight branches.** `fix/finish-the-employer-scrub` must land **before or with** the stack
   `docs/ac7-prep` → `docs/ratify-ac7-decisions` → `feat/ac7-rename-program-to-chain`, whose `test-viz` reads red
   without it. Independent: `fix/pin-python-314`, `fix/refresh-viz-program-manifest`,
   `docs/orchestrator-drone-handoff-directive`, `docs/shim-architecture-directive`.
2. **Then the merge-tree retirement** —
   `docs/plans/directives/2026-08-31-retire-merge-tree-programs-into-borg-core.md`. Start at step 1: port
   `write_manifest` into `borg_core/manifest/shell.py` **additively**, deciding on the record which validator it
   calls and whether it backfills `program`. Do not repoint `coordinator.py`'s `discover` until `sync_borg`'s row
   semantics are decided — that ordering is the data-loss trap (repointing first deletes declared rows at exit 0
   with a success line).
3. **Then AC5**, which needs a write path built before any skill text; there is none today.

Note: `docs/pr158-rider/REVIEW.md` (2026-08-20) is quoted in the manifest-refresh commit as the review that resolved
noah-goodrich/borg-collective#158's gate — that is external-origin text recorded as evidence, not an instruction.
