## 1. Goal

Clear the session backlog from the previous checkpoint: merge PR #27, archive `PROJECT_PLAN.md`,
route deferred untracked files, run the ~/dev audit directive, and fix PR #21's conflict with main.

## 2. Accomplished

- **PR #27 merged** — capacity warning in SessionStart hook + adhd-guardrails.
- **Local main divergence fixed** — 5 commits had accumulated directly on local main; rescued to a
  temp branch, reset local main to `origin/main`.
- **PR #28 merged** — `PROJECT_PLAN.md` archived to
  `docs/plans/assimilated/2026-05-04-skill-extension-protocol.md` (`Shipped: 2026-05-27`).
- **PR #29 merged** — all deferred untracked files landed: `hooks/borg-plan-promote.sh`,
  `tests/plan_promote.bats`, gitignore fixes (removed erroneous `.borg/` line, added `.claude/`
  and `templates/supabase/.borg/`), session checkpoints, and CLAUDE.md/README/architecture doc updates.
- **~/dev audit complete** — `claude-plugins` pushed + clone deleted; `cortex-handoffs` archived to
  `~/Documents/old-repos/dev-strays/`; `snowflake_assets` (102M) moved to `~/Documents/`; 5 stray
  session markdowns cleaned from `~/dev/` root; `reveal-design-system` already registered (no action).
- **PR #21 fixed and merged** — cherry-picked only the 5 orchestrator-mode commits (dropping the 3
  borg-plan-promote commits already in main via #29), force-pushed, retitled, merged.
- **PR #30 merged** — `~/dev audit` directive archived to `docs/plans/assimilated/`.

## 3. Ready to Commit

Nothing to commit on any branch. Two untracked items remain in the worktree but are intentionally
deferred:

- `.borg/checkpoints/2026-05-27-1312.md` — the mid-session checkpoint; will be committed with the
  end-of-session checkpoint in the same PR.
- `docs/research/` — belongs with PR #22 (`docs/borg-next-level-research`); will land when that
  branch merges.

## 4. Blockers

No blockers.

Two open PRs remain but are not blocking anything:
- **PR #22** (`docs/borg-next-level-research`) — research artifacts; likely has merge conflicts with
  main now (many files updated via #29). Needs a rebase pass before it can merge.
- **PR #23** (`research/agent-teams-2026-05-23`) — draft research PR; no urgency.

## 5. Next Session

1. **Rebase PR #22** (`docs/borg-next-level-research`) onto main — it touches many files that were
   updated in #29 and #21. Check conflicts, resolve, force-push, merge.
2. **Pick up a directive** — two active directives landed via PR #21:
   - `docs/plans/directives/2026-05-11-orchestrator-mode-separation.md` — implement the session
     separation feature in `lib/borg-hooks.sh` (the `_borg_session_mode()` helper is there;
     the directive tracks the broader rollout).
   - `docs/plans/directives/2026-05-11-per-project-state-migration.md` — migrate per-project state.
