## 1. Goal

Resolve the Clean-Architecture-for-AI-agents research deliverable's citation-verification gate failures,
finalize borg_core/'s actual architecture via full Decision-Design mode, then implement Part 3 of the
python-core-and-toolchain directive (scaffold borg_core/, migrate `recon` from shell to Python).

## 2. Accomplished

**Research deliverables (both gate-clean, committed as untracked files, not yet committed to git):**
- `docs/research/2026-08-12-clean-architecture-for-ai-agents/analysis.md` — evidence-mode report;
  `verification-report.md` fixed to pass all 12 hook assertions (added real verifier/synthesis agent
  IDs pulled from the nanoprobe log, relabeled Round 4 same-actor rechecks "confirmed" not "verified"
  to close a corrected-then-verified loophole). `deep-research-verify.sh` now reports a clean PASS.
- `docs/research/2026-08-12-clean-architecture-for-ai-agents/recommendation.md` (+ epub at
  `~/Documents/Claude/borg-collective/`) — full D1-D6 decision-design pass. Went through 3 rounds of
  independent blind review (capped by design): Round 1 found the chosen option's cross-command
  isolation claim was unverified (confirmed false via direct plugin source read: `module_map` resolves
  by bare filename, `DependencyChecker` unconditionally allows intra-layer imports); Round 2 demanded
  proof, so I built a real two-command scratch package and ran both `pylint-clean-architecture` and
  `import-linter` against it directly, confirming the fix (`import-linter`'s `Independence` contract)
  actually works and finding a second bug (the `allowed_prefixes` fix needed to make the plugin usable
  also silently disables its only cross-command signal); Round 3 upheld the corrected recommendation
  with two named limitations (bare-builtin I/O like `open()` bypasses both tools — confirmed directly;
  the isolation contract isn't wired in yet, nothing to isolate `recon` from until a second command
  exists). Final pattern: one folder per command, `core.py` (pure, `module_map`-tagged) + `shell.py`
  (all I/O), `allowed_prefixes` required, `import-linter` Independence contract deferred to the second
  command, complexity-gate principle documented from day one.

**Part 3 implementation (committed, 3 commits on `feature/part3-borg-core-recon`):**
- `86e29e9` — scaffolded `borg_core/` package, wired `pyproject.toml`/`Makefile`.
- `c1709d3` — ported `recon` (since-mark resolution, adapter fan-out, contradiction detection, JSON/
  digest output) to `borg_core/recon/{core.py,shell.py,cli.py}` + tests.
- `488010d` — fix cycle from an adversarial review: replaced `typer` (dev-only, no fallback, would
  `ModuleNotFoundError` on a real install) with stdlib `argparse`, actually deleted `lib/recon.sh`/
  `lib/recon.zsh`/`tests/recon.bats` (were left dormant, violating C5's own criterion), added
  `coverage --fail-under=90`, wrote the C6 migration ledger
  (`docs/plans/assimilated/2026-08-12-recon-migration-ledger.md`), updated `PROJECT_PLAN.md` C1-C6.
  I independently re-verified everything myself after the automated workflow (bats 585/585, pytest
  82/82 at 96% coverage, ruff/mypy/pylint clean) rather than trusting agent reports, and caught two
  things the agents missed: C4's checkbox was never ticked despite the work landing in an earlier
  session, and a leftover comment in `borg.zsh` still contained the literal string `cmd_recon`,
  technically failing C5's own `grep -c 'cmd_recon' borg.zsh` acceptance criterion.

**Process note worth remembering:** my own Workflow script had a bug — a naive `/FAIL/i` regex gate
matched the substring inside `--fail-under=90` and treated a clean PASS as a failure, stopping the
workflow one step short of finalizing. Caught by reading the actual agent output instead of trusting
the gate, not by luck.

## 3. Ready to Commit

Nothing uncommitted on `feature/part3-borg-core-recon` among tracked files (`git status` shows a clean
tree for everything except the pre-existing, unrelated research docs below). `/simplify` was not run
as a separate pass this session — the equivalent scrutiny happened via the workflow's own adversarial
review + fix cycle + my independent re-verification, but a dedicated `/simplify` pass over
`borg_core/recon/*.py` before the branch is merged would still be worth doing, since it hasn't had a
readability-focused pass specifically.

**Still untracked, not part of this session's commits, needs a decision:**
- `docs/research/2026-08-12-clean-architecture-for-ai-agents/` (analysis.md, recommendation.md,
  verification-report.md, drafts/, .gate-armed) and `docs/research/sources/*.md` (21 new source cards)
  — the completed, gate-clean research deliverables. Not yet committed anywhere; live only in the
  working tree. Should be committed (to this branch or a separate one) before they're at risk of loss.
- `.borg/checkpoints/2026-08-10-0000.md` — pre-existing untracked file from before this session,
  not created by this session, not touched.

## 4. Blockers

No blockers. Two open items recorded as known limitations in `recommendation.md`, not blockers:
1. Bare-builtin I/O (`open()`, no import statement) bypasses both `pylint-clean-architecture` and
   `import-linter` — confirmed directly. Option G (a pytest fixture monkeypatching `open`/`subprocess`/
   `os.system`/`socket` at test time) is recorded as the concrete next spike to close this gap.
2. The `import-linter` `Independence` contract is proven to work (direct test) but not yet wired into
   the real repo — nothing to isolate `recon` from until a second `borg_core/<command>` exists.

## 5. Next Session

1. **Decide on the untracked research docs** (see §3) — commit them (this branch or a dedicated one)
   before anything risks losing that work.
2. **Push `feature/part3-borg-core-recon` and open a PR** — implementation is done and independently
   verified, but not pushed; Noah asked to hold off on that as a separate step.
3. **Sequencing after C5**, per `PROJECT_PLAN.md`: migrate `link` next (biggest win), then `next`, then
   registry CRUD, then `nanoprobes`/`spend`/`watch`; `switch`/`focus`/`init` last (tmux-interactive).
   Each new command should follow the `core.py`/`shell.py` pattern `borg_core/recon/` now sets, with
   `allowed_prefixes` already covering the whole `borg_core` prefix — no new config needed per command.
4. **Wire the `import-linter` Independence contract** once the second command (`link`) lands — this is
   the concrete trigger condition recommendation.md already named for adding it.
5. **Consider prototyping Option G** (runtime I/O interception) at some point — it's the only mechanism
   found that closes the bare-builtin-I/O gap, and `link`'s migration may be a good testbed if it turns
   out to read/write files directly.
