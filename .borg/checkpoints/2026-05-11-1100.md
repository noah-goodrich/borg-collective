## 1. Goal

Drive the five-track multi-project plan (reveal beta hardening, ingle to prod with data, troth scaffold + auth,
persona/archetype UX, reveal-site GTM refinements) under "full auto" mode, ship a borg release, and close the
long-running ~/dev cleanup arc.

## 2. Accomplished

**Multi-track parallel agent dispatches (all shipped):**
- **Reveal beta Code Session 1** — `ffa1d06` directives, `5117548` BB-1 per-user free render quota (3 lifetime,
  bypassed by `is_beta_tester=true`), `98421cd` BB-2 items 1/2/4 (error sanitization, UUID guards, Cloudflare-
  edge regen rate limit). +22 tests (169 → 191). Cost-pinned files untouched. Migration
  `20260510000001_regenerate_rate_limit.sql` pushed to prod via `supabase db push --include-all`.
- **Ingle prod** — `4a5f531` directive, `d92ca6a` seed-store-items.ts schema fix, `4e9abbd` auth redirect
  config restored. Migrations 0001–0019 confirmed on prod (no orphans). Data import via re-run
  `scripts/import-orders.ts` + `scripts/seed-store-items.ts`. Final prod counts: 228 trips / 2,611 price_history
  / 1,029 purchase_history / 4 store_chains / 1,025 store_items. **Working state tables (pantry, lists,
  purchase_transactions, household_store_item_prefs, etc.) NOT yet migrated — flagged.**
- **Troth scaffold** — full ingle-mirrored Next.js 14 + Supabase scaffold (`dceae63` directive, `4a639bd`
  pattern notes, `2320905` config, `ae55b0a` devcontainer, `50d1905` migrations, `00ed093` PWA + auth,
  `4e02aa3` cleanup). 4 migrations applied to shared Stillpoint project `jmgjznimsrlcgyevefqm` via docker psql
  (renamed `20260510000001-04` → `20260510010001-04` to dodge reveal collision). Brand tokens applied (Vellum/
  Pitch/Heartwood/Stillwater, Crimson Pro + IBM Plex Sans). Stillpoint schema (identities/households/
  household_members/access_tokens) + RLS + `handle_new_user()` trigger + `troth.accounts` all live.
- **Troth deploy to prod** — `troth-app` CF Pages project + custom domain `app.troth.money` end-to-end:
  - CF zone added; CNAME `app.troth.money` → `troth-app.pages.dev` (proxied).
  - GitHub repo `stillpoint-labs/troth` (private) created + 4 secrets set.
  - 5 fix commits (`eeffafb` lockfile, `c7e920c` pin next-on-pages 1.13.15 + .npmrc, `55fc34a` edge runtime,
    `499bb08` Node 22, `8d7b680` workflow_dispatch).
  - Porkbun NS swap to `ashley/merlin.ns.cloudflare.com` via API (Noah enabled API access).
  - Supabase Auth allowlist updated to include `app.troth.money/**`, `troth-app.pages.dev/**`.
  - Final: `https://app.troth.money/sign-in → HTTP 200`. Full brand-styled page rendering.
- **Persona / archetype** — directives + docs + POC across both ingle (`6b943bd` directive, `5e81d2a` 3
  personas + 4 archetypes + triage + architecture, `0bb4f6c` POC: triage → archetype-aware home tiles +
  migration `0020_household_archetype.sql`) and troth (`80c2353` directive, `2689d42` 3 personas + 4 finance
  archetypes + triage).
- **Reveal-site GTM** — P0 items 1–4 (`02b5c2a`), brand identity spike (`5a13114` recommends horizon-line/
  sightline mark; 3 open questions for Noah), P1 batch (`33b68c3` closing CTA, hero CTA, persona segmentation,
  BeforeAfter tap, gallery card hierarchy, footer "Gallery", Mira hook).

**Borg v0.7.16 released:**
- Directive Orphan Prevention shipped earlier in session (`5a28733` impl, `b4018d0` assimilated, `c0101de`
  housekeeping).
- `9435349` simplify skill + session reviews + 2026-05-07 checkpoint.
- `3b2ffe2` BORG_VERSION bump, `129af39` Formula update (sha256
  `05bfd758eff27ab38f1c908279a1b1ac27fcb263db9c364ecdf39e2e100033a1`).
- Tag `v0.7.16` pushed.

**~/dev cleanup arc — closed:**
- 33 → 13 active directories. ~9.78G reclaimed (9.3G sfquickstarts + 195M excelsior-architect + 143M snowarch +
  49M SnowDDL/snowflake-examples + 5.3M prototypes tarball + scattered scaffolds + cursor transcripts).
- Archived to `~/Documents/old-repos/`: snowfort-old (60K), snowfort-scaffold-bak (260K), wayfinderai-waypoint
  (2.2K), wallpaper-kit (2.6K), snowflake-projects-prototypes (5.3M), snowarch dirty-state patch (7,444 lines),
  dev-strays (13 loose files).
- GitHub archived: noah-goodrich/SnowDDL, noah-goodrich/snowflake-examples.
- Cairn/engram design docs relocated to `~/dev/cairn/docs/legacy/` (`530a973`), bootstrap.yml relocated to
  `~/dev/snowfort/` (`d40860b`), snowflake-projects retired.
- Symlink `dev.sh` removed (we use borg now).

**Final audit directive filed** (`245ea38`) — captures the remaining 4 unaudited directories (`claude-plugins`,
`cortex-handoffs`, `reveal-design-system`, `snowflake_assets`) for a future session.

**Memory updates:** `feedback_per_project_directives.md` (orchestrator-side aggregate plan files are a smell) +
`feedback_full_auto_mode.md` (when Noah says full auto, dispatch immediately, only escalate hard blockers).

## 3. Ready to Commit

**borg-collective:** clean. All commits this session pushed to origin/main. Latest:
- `245ea38` docs(plans): final ~/dev audit directive
- `129af39` release: v0.7.16
- `3b2ffe2` bump BORG_VERSION
- `9435349` simplify skill + reviews + prior checkpoint
- `b4018d0` assimilate orphan-prevention
- `5a28733` orphan-prevention impl
- `c0101de` housekeeping

**snowfort:** `d40860b chore: import bootstrap.yml` — committed locally, NOT pushed (held for review).

**cairn:** `530a973 docs: relocate legacy design docs` — committed locally, NOT pushed (held for review).
There's also an untracked `docs/plans/directives/2026-05-01-host-first-running-things.md` — predates session,
left alone.

**reveal-site:** all session commits pushed. Two untracked draft docs from earlier in the session: `docs/icp.md`
and `docs/plans/reviews/2026-05-07-gtm-persona-review.md`. Worth committing as session artifacts on the next
visit.

**reveal:** all session commits pushed.

**ingle:** session work committed. Local Supabase still running; nothing more to commit here.

**troth:** all 11 commits pushed to `stillpoint-labs/troth`.

`/simplify` was NOT run this session against the orchestrator's own commits. Worth a pass on borg-collective
before next release; not urgent.

## 4. Blockers

- **Ingle: working-state tables not yet migrated to prod.** Local has 3,699 `purchase_transactions`, 1,008
  `household_store_item_prefs`, 77 `grocery_items`, 3 `grocery_lists`, 5 `pantry_items`, 2 `budget_targets` —
  prod is at 0 / 0 / 0 / 0 / 1 / 1. The CSV-derived history is in prod; the user's actual working state is not.
  Needs a pg_dump-and-remap migration with `household_id` remap from `11111111-...` → `234b71c5-ad0c-...`.
- **Troth: legacy budgeting data import deferred.** Per the handoff, troth's legacy budgeting-app files will be
  uploaded by Noah; nothing to migrate yet.
- **Reveal: pre-existing tsc errors in `e2e/gallery.spec.ts` (Set iteration) and `GalleryGrid.test.tsx`
  (ByRoleOptions.exact)** — predate this session; flagged but not addressed.
- **Reveal: BetaFeedbackForm reducer split** — planned for Code Session 2 (BB-3 + BB-4 trigger + form split),
  not yet started.
- **Reveal: pre-commit hook runs Python tooling on host** — known violation of the devcontainer rule; cleanup
  is a separate directive worth filing.
- **INGLE_SUPABASE_DB_PASSWORD leaked into chat transcript** by an earlier Track B agent. Recommend rotation in
  Supabase Dashboard → Project Settings → Database; update env var afterward. Not urgent (local env only) but
  durable in cloud-stored session transcript.

## 5. Next Session

**First action: pick one of three priority tracks based on Noah's bandwidth:**

1. **Ingle working-state migration (1 session).** Write `scripts/migrate-local-to-prod.ts` (new): `pg_dump
   --data-only --table=...` from local (port 54322) for `pantry_items`, `grocery_lists`, `grocery_items`,
   `purchase_transactions`, `household_store_item_prefs`, `family_members`, `budget_targets`, filtered to
   `household_id='11111111-1111-1111-1111-111111111111'`. Stream through `sed` to remap to prod household_id.
   Apply via `psql` with `ON CONFLICT DO NOTHING`. Verify final counts match local. Idempotent.
2. **Reveal beta Code Session 2 (1 session).** BB-3 (`PhotoConsultation.tsx` Zod schema for ExecutionBrief +
   server-action sign-out) + BB-4 trigger (BEFORE INSERT auth.users allowlist migration) + BetaFeedbackForm
   reducer split. Cost-pinned files still off-limits.
3. **The ~/dev final audit (30-45 min).** Per the just-filed directive
   `docs/plans/directives/2026-05-11-dev-dir-final-audit.md`. Audit `claude-plugins`, `cortex-handoffs`,
   `reveal-design-system`, `snowflake_assets`. Mechanical once decisions are made.

**Manual user-side tasks still outstanding (from prior plan):**
- BB-7: equity-lawyer engagement on reveal/troth/ingle privacy + ToS.
- BB-8: Resend/Postmark + Supabase SMTP swap (kills the 3/hr rate limit).
- **Ingle shopping smoke test** — manual Walmart/Target run via Claude Desktop + ingle MCP at
  `https://app.ingle.kitchen/sign-in`. Unblocks Item Canonicalization planning. Instructions at
  `ingle/docs/plans/directives/2026-05-07-shopping-smoke-test.md`.
- **Brand identity 3 open questions** from `reveal-site/docs/brand-identity-rethink.md` (parent-architecture
  conflict, 16px legibility, stance vs. plate anchor).

**Open commits to push before next major work:** `snowfort:d40860b`, `cairn:530a973`. Or leave them for review.
