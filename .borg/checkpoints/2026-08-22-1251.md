## 1. Goal

Evaluate the rider branch for PR #158 (independent review + spine fix + communication program), verify
rider-vs-PR compatibility on real multi-source data, and close what the review surfaced.

## 2. Accomplished

- **Rider evaluated + collective review run.** All six REVIEW.md merge blockers independently re-verified
  (findings 2/4/5 by execution, 1/3 by code mechanism). Verdict posted to **PR #159** (created it:
  rider → `feat/program-manifests`): take the fix, split-merge with backfill.py extracted, two silent decisions
  to resolve first (contested refs, home-project view) — both since resolved by the personal machine (`c8de33d`)
  and verified here.
- **Branch compatibility proven**: rider × PM6/PM7 head merges clean — 343 tests, 97% coverage, ruff+mypy green;
  `desc` survives the full write/discover/sync round-trip; coordinator is regrouping-safe.
- **Live multi-source run** (13 projects, 184 items + synthetic second adapter) exposed 3 integration findings;
  **all three closed**: §4.3 dropped-items health + duplicate-ref guard on the rider branch (`a4cf00e`);
  §4.1 cross-source dedup as **PR #160** (`fix/recon-cross-source-dedup`, 95 tests, gates the first employer
  adapter); §4.2 staged window contract reconciled into comms-delivery-surfaces (`470d8a5`, AC8 v1 banner /
  AC9 S2-final fetch).
- **Security audit** of all deps/hooks/launchd on this machine → directive SA1–SA6 filed as **PR #161**
  (gh CVE + doctor floor, bash-guard allowlists, untrusted-quotation labeling, tmp/argv/pins hardening).
- **Infoviz program**: status answered (Phases 0–2 done, no Phase 3 filed); resume order recorded in
  `docs/infoviz/README.md` as **PR #162** (Track 6 first — its DAG trigger fired); Phase 1+2 review brief
  generated at `~/.local/state/borg/infoviz/phase-1-2-brief.html`.
- **All five PR bodies reworked** in the K3 `pr-description` four-block format (read from the claude-plugins
  drone branch) + Kelly ORCHESTRATOR-CHAIN block stamped into #158/#159/#160; stamping manifest committed at
  `docs/plans/directives/2026-08-18-program-manifests-stack.json` (`2fc2e75`).
- **Persistent PR watcher armed** (Monitor task `bechui47e`, 2-min poll on #158–#162 + new-PR detection) —
  session-scoped; it dies with this session.

## 3. Ready to Commit

Nothing staged. Untracked: two checkpoint files (this skill commits nothing), and **`uv.lock` at the repo root —
my test-run artifact, `rm` it or track it deliberately**. All real work is committed and pushed on
`rider/pr-158-review`, `fix/recon-cross-source-dedup`, `docs/security-audit-directive`,
`docs/infoviz-resume-order`. /simplify not run this session; the code worth a pass if you want one:
`borg_core/recon/core.py` (`dedup_cross_source`) and `merge-tree/gather.py` (health builder).

## 4. Blockers

- **#158 merge is gated on the agreed merge unit**: #159 merges into `feat/program-manifests` first, then
  backfill.py is extracted (findings 1–3 + the `edges_from` merged-mid-chain inversion rework there). Human
  decision — both machines have signed off on the shape.
- ScheduleWakeup + some env-inspection commands were classifier-blocked; watcher fell back to Monitor (works,
  but session-scoped, not durable).
- `bats` is not installed on this machine — `borg.zsh`'s test path is non-functional here (noted in audit).

## 5. Next Session

1. **Merge the train**: `gh pr merge 159` (into `feat/program-manifests`), extract `backfill.py` to its own
   branch, then merge #158 to main. Re-stamp after each:
   `python3 ~/.claude/plugins/cache/ai-data-engineer/data-engineer/1.8.1/skills/stacked-pr-program/scripts/stamp_stack.py docs/plans/directives/2026-08-18-program-manifests-stack.json`
   (update `status` fields in that manifest first).
2. **Merge #160 before any employer adapter is written** — that ordering is the whole point of the PR.
3. Backfill rework branch: fix `backfill.py` findings 1–3 **and** `programs.edges_from`'s `order: "–"`
   sort-inversion (same bug, other side — see #159 review comment).
4. If starting SA work: SA2 first (`hooks/bash-guard.sh:338-344` blanket allows → subcommand allowlists).
5. Re-arm the PR watcher if still wanted — the Monitor died with this session.
