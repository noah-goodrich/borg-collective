## 1. Goal

Run `/borg-link` and start Phase 0 of the link Python-core port — which grew, on instruction, into Phases 0
and 1 plus three live-bug fixes found along the way.

## 2. Accomplished

**Five stacked PRs, all open, all green locally.**

- **[#129](https://github.com/noah-goodrich/borg-collective/pull/129) → `main` — Phase 0 parity harness (A1
  done).** 25 link cases in `tests/cli_contract.bats`; four byte-exact goldens under `tests/fixtures/link/`
  (`link-porcelain`, `link-overview`, `link-overview-all`, `link-deep`), compared with `diff`, ANSI and column
  padding included. Regenerate with `BORG_UPDATE_GOLDEN=1` — never during the port.
- **[#130](https://github.com/noah-goodrich/borg-collective/pull/130) — `borg recon` had been dead on every
  real machine.** `borg_core/recon/cli.py` read `BORG_REGISTRY` with no fallback; `borg.zsh` assigns it
  without `export`, so the child got `""` and died with `no registry at ` on every invocation except
  `--adapters`. `/borg-recon` has been failing since the migration. Fixed both sides: `_borg_py` (a dispatch
  wrapper in `borg.zsh`, just above the `case` block) hands the child the whole config surface — this is also
  **A2, now done** — and `borg_core/paths.py` was extracted so borg_core resolves its own defaults.
- **[#131](https://github.com/noah-goodrich/borg-collective/pull/131) — one bad `state.json` blanked the whole
  registry.** The guard in `borg_registry_with_state` was `result=$(…) || result="$result"`, a no-op: command
  substitution assigns before the `||` runs. `borg link` printed "No projects registered" with eleven
  registered; nine call sites went blind together.
- **[#132](https://github.com/noah-goodrich/borg-collective/pull/132) — a tmux window name is data, not a
  regex.** `grep -qx` without `-F` at four sites; `troth.site` matched a live `troth-site`.
- **[#133](https://github.com/noah-goodrich/borg-collective/pull/133) — Phase 1: leaves + chokepoint.**
  `borg_core/link/{core,shell}.py` + colocated tests. **Zero tracked files modified** — pure addition, so
  every Phase 0 golden passes unchanged, which is what A4 needs later.

**The shared case table is the load-bearing artifact.** `tests/fixtures/reaper-cases.tsv` (22 rows) is read
by *both* `tests/reaper_cases.bats` (live `lib/reaper.sh` under zsh) and `borg_core/link/test_core.py`
(`core.should_reap`), asserting the same expected column; plus a 5-row block for `borg_reap_overlay`'s window
resolution. Both green, so the implementations demonstrably agree.

**Phase 0 was hardened after a blind adversarial pass** (reuse / vacuity / portability). It found that the
tertiary sort key was invisible (no fixture tied a bucket), the human `--all` path was never rendered, and my
own `borg.zsh:689` consumer claim was **wrong** — fzf reads `cmd_ls --porcelain` (`:685`). Non-vacuity then
verified by six mutations, each caught by exactly the goldens that should see it.

**Two rendering bugs pinned as deliberate deviations**: `(NOm)` lists the *oldest* three assimilated plans
(kept out of the golden so fixing it flips one substring test rather than forcing a regeneration), and
`grep -c … || echo 0` makes `Progress:` render across two lines on every fresh plan.

`make lint` 10.00/10 · 328 pytest (`core.py` and `shell.py` both 100% per-module) · `bats tests/*.bats`
627/627 · contract suite verified on ubuntu:24.04 as well as macOS.

## 3. Ready to Commit

Nothing. All five branches are committed and pushed; the working tree has no tracked modifications. The only
untracked paths are the pre-existing `.borg/checkpoints/*` and `docs/research/*` from earlier sessions.

`/simplify` was not run as a standalone pass. Its substance was covered another way and should be recorded
honestly: the workflow's **reuse lens** reviewed the Phase 0 harness specifically (its findings — the
duplicated fixture setup, the vacuous `${#lines[@]}` assertion, the negative-only A2 assertion — were all
applied), and `make lint`'s `duplicate-code` check is what forced the `borg_core/paths.py` extraction. The zsh
fixes in #131 and #132 were **not** reuse-reviewed; they are four lines and one flag respectively.

## 4. Blockers

- **Two host commands still need Noah's hand** (carried over, unrelated to this work):
  `rm -f /Users/noah/.config/borg/plan-promote-debug.log && pipx uninstall cairn`.
- **CI has no Python provisioning on either bats lane** (`test.yml` installs `zsh jq fzf` / `jq`; neither runs
  `setup-python`). Harmless today — Phase 1 added no dispatch arm — but it bites at Phase 2, when a `python3`
  child first runs on the link path. Decide before that lands.
- **`contract: watch dispatches into the live-refresh loop` is flaky on `main`** (failed 3/3 on the
  pre-change baseline, passes intermittently). Untouched and out of scope, but it will intermittently redden
  these five PRs and should not be mistaken for a regression from them.
- **Auto-memory gate still FAIL** at 0.027 reads/session vs the pre-registered 0.2. Calendar time, not work.

## 5. Next Session

**Start by checking CI on the stack**, bottom-up: [#129](https://github.com/noah-goodrich/borg-collective/pull/129)
must merge to `main` before the rest can rebase. `gh pr checks 129`. Expect the flaky `watch` test.

**Then Phase 2 — the `--json` seam (A3).** This is the natural next unit and the plan calls it the safest
stopping point after it lands, because every byte of human output is still produced by untouched zsh
renderers. Concretely:

1. Write `borg_core/link/cli.py` — argparse, mirroring `borg_core/recon/cli.py`'s shape (stdlib only, no
   typer). It assembles the document from the Phase 1 leaves: `shell.registry_with_state()`,
   `shell.collect_all_directives()`, `shell.collect_all_assimilated()`, `shell.cortex_pending()`,
   `shell.read_plan()`, `shell.read_checkpoints()`.
2. **The document's `order` array is where the human table's sort finally lives** — pinned, deliberately, in
   Phase 0's goldens: pinned first, then status priority (waiting > active > idle > archived), then
   `last_activity` **ascending**. Three projects tie the idle bucket in the porcelain and overview fixtures
   precisely so reversing that key fails the byte diff.
3. Add the `link)` dispatch arm using `_borg_py` (already built), and **add `setup-python` to both bats CI
   lanes in the same PR** — that is when the blocker above becomes real.
4. A3's verify is stricter than the directive's: `borg link --json | jq -e '.projects and .generated_at and
   (.order | length) == (.projects | length)'`, plus a test that forces the registry-write warning and asserts
   stdout is still valid JSON. `warn()` writes to **stdout** (`borg.zsh:30`), so diagnostics must move to
   stderr on this path.

Do **not** touch any renderer in Phase 2 — the flip is Phase 3, and it must be atomic across
porcelain/overview/deep.
