**tl;dr** — Two rulings executed for noah-goodrich/borg-collective#220: the employer PR is open as
Ontra-ai/ai-data-engineer#48, and the shim plan is demoted to a directive under One Front Door with `PROJECT_PLAN.md`
back to main's bytes. Head `dc39ef7`, pushed, PR title and body rewritten. Nothing is owed in this worktree; #220
now waits on CI and both machines' stamps like any other PR.

## 1. Goal

Finish the two items Noah left after yesterday's handover: open the employer PR from the pushed
`fix/close-borg-leak-in-plugins` branch, and act on the one-plan-slot ruling the Queen relayed (demote the shim plan
to a directive), then report the head SHA and checkpoint.

## 2. Accomplished

- **Employer PR.** Ontra-ai/ai-data-engineer#48, not a draft, title from `cc71779`'s subject, body from its message
  including the Python 3.14 pycache paragraph, ending with the Generated-with-Claude-Code line. No reviewers
  requested by me and nothing posted to Slack; GitHub's CODEOWNERS (`* @Ontra-ai/data-eng`) auto-requested the team.
  CI was pending at last look.
- **Demotion.** `PROJECT_PLAN.md` restored with `git checkout origin/main -- PROJECT_PLAN.md` (diff against main is
  empty). The shim plan's content became `docs/plans/directives/2026-09-19-employer-shim-and-adapter-layer.md`:
  `*Parent plan: 2026-08-24-one-front-door-link-derived-fact-surface*`, filed 2026-09-19, five `[x]` criteria with
  their evidence lines carried over, the Ship Definition's OPEN DECISION rewritten as RESOLVED with the ruling and
  date, and the nine-orphans risk marked moot. No `- Plan-slug:` line survives in it.
- **Manifest row.** The `viz-program` row for #220 rewritten through `manifest.cli add-row` (append-or-update) so its
  `why` says it files a directive and names Ontra-ai/ai-data-engineer#48. Still lane `_default`, order 1, a root.
- **Readers confirmed.** `manifest.cli resolve` prints `viz-program`; `_borg_plan_declared_slug PROJECT_PLAN.md`
  prints the One Front Door slug; that slug has 10 child directives, this one included; a read-only planstate run
  reads 7 criteria from the restored plan and flipped nothing.
- **Verification after the swap.** `make test` 1258 passed at 93% coverage; `make lint` 10.00/10; `bats tests/` 877
  ok, exit 0.
- **Commit `dc39ef7`**, pushed; it also stages the Queen's on-disk rename of her seed checkpoint from `2200` to
  `2150-seed` (content identical), which makes yesterday's `2156` handover sort last for the start hook.
- **PR 220** title now "file the shim-layer directive — adapter-driven reconcile, child of One Front Door (AC1–AC5
  of 5)"; body opens with "Files a directive; does not replace the plan" and keeps the criteria table, the AC4
  mutation transcript, and the pycache finding.
- Queen messaged twice: the PR 48 URL, then the head SHA with the checklist above.

## 3. Ready to Commit

Only this checkpoint, committed and pushed right after it is written. `.venv` stays untracked (a symlink into the
main tree's venv).

## 4. Blockers

- No blockers on the work. Two engine artifacts to know about, neither acted on: the link-up's planstate run against
  the OLD shim plan (before the swap) reported AC1 pass, AC2 and AC3 fail, AC4 and AC5 unknown; the fails are the
  resolver pointing `PYTHONPATH` at the main tree, which has no `borg_core/reconcile/`, so the suites cannot import
  there. The same suites pass in this worktree. Not re-run after the swap with `--apply`, because the file under
  reconciliation is now One Front Door's plan, which is the other drone's to flip.

## 5. Next Session

Nothing owed here. If picking this up:

1. `gh pr checks 48 --repo Ontra-ai/ai-data-engineer` and `gh pr checks 220 --repo noah-goodrich/borg-collective`.
   Both should be green; PR 220 then needs both machines' comment stamps at `dc39ef7`.
2. If noah-goodrich/borg-collective#222 merges first, rebase and let the rename carry `.borg/programs/viz-program.json`
   to `.borg/chains/`.
3. Shipping the directive is `/borg-assimilate`'s job under One Front Door; it re-evaluates the five ticks itself.

## Criteria Reconciled

**Proposed, not flipped** (`fail` / `unknown` — engine run against the shim plan before the swap; the plan file it
read no longer exists on this branch, and the criteria now live in the directive above):
- AC2 — `borg reconcile` ships READ-ONLY, and writes nothing at all — fail: `pytest:borg_core/reconcile/test_core.py
  exited 1` (resolver artifact, see §4)
- AC3 — Resolution is adapter-driven, with no hardcoded source name on the path — fail:
  `pytest:borg_core/reconcile/test_shell.py exited 1` (same artifact)
- AC4 — The employer leak is closed as an absence, and the grep that missed it can fail — unknown: `no evidence
  annotation`
- AC5 — Nothing breaks — unknown: `no evidence annotation`
- AC1 passed (`bats:tests/prose_contracts.bats exited 0`) but was already `[x]` by hand, so nothing flipped.

Disclosure: the five ticks in the directive were made by hand, not by the engine. `/borg-assimilate` remains the gate.

## Manifest Row

**Wrote:** `noah-goodrich/borg-collective#220` → `viz-program` (lane `_default`, order 1), updated in place with the
directive-filing rationale.
