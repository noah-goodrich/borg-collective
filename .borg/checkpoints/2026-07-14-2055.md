## 1. Goal
Determine whether any borg-collective directives or the PROJECT_PLAN were outstanding, close out what was done,
and act on the two follow-ups Noah picked (roster-drift quick win + usage-guardian Phase-2 data readiness).

## 2. Accomplished
Three PRs shipped to `main`, all CI-green (bats + shellcheck on ubuntu + macos), all squash-merged:

- **#78 — Assimilated usage-guardian Phase 1** (observe-only). The code was already merged in a prior session
  (PRs #66/#67/#68/#69/#70/#71/#75) but the plan was never closed. Verified all 7 acceptance criteria against the
  merged code (24/24 usage_watch bats, 422/422 full suite, shellcheck clean), archived `PROJECT_PLAN.md` →
  `docs/plans/assimilated/2026-07-08-usage-guardian-phase-1-observe-only.md`, removed it from the repo root.
  Disclosed one deviation: criterion 2's "no row" wording was superseded by the never-silent contract (#68) that
  writes an explicit error row every poll — a deliberate strengthening.
- **#79 — Fixed agent-roster source drift** (directive `2026-07-12`). Back-ported the 5 agents that lived only in
  the claude-plugins distro (`borg-grunt`, `borg-scout`, `ROUTING`, `borg-researcher`, `borg-reviewer`) into
  `agents/`; extended `scripts/sync-plugin.sh` with an additive, existing-targets-only `agents/` loop; added
  `scripts/check-agent-roster.sh` drift guard + `tests/agent_roster.bats` (5 tests, negative-tested). Directive
  moved to `docs/plans/assimilated/`. Restores the source→distro invariant for agents.
- **#80 — Fixed the usage-watch 0%-session parse bug** (root-caused the parse_failed bursts). At `session_pct=0`,
  Claude Code renders a bare `Current session: 0% used` with no `· resets` suffix; the regex required it, so
  582/591 observed failures were misclassified 0%-idle polls (incl. a ~6h block on Jul 14). Made the reset clause
  optional in `bin/borg-usage-watch:171`; fail-closed preserved (empty/garbage still → parse_failed). Added
  `tests/fixtures/usage-output-idle.txt` + regression test. Deployed automatically — the live poller at
  `/Users/noah/.local/bin/borg-usage-watch` is a symlink into the source repo.
- **#81 — Annotated the open `usage-guardian-build` directive** with the Phase-2 data-readiness findings so the
  next session doesn't re-derive them.

Key data finding (from `~/.local/state/borg/usage-samples.jsonl`, 3080 rows / ~6 days): only ONE near-cap episode
exists (all ≥85% samples are Jul 9); burn accelerates ~4× near the cap (~1%/min in the 85→93 band); the ~19%
error rate was benign 0%-idle noise (now fixed). Detection near the cap is reliable.

## 3. Ready to Commit
Nothing — working tree clean, all four PRs merged and `main` synced to `origin/main` (HEAD `32bf420`).
Only the two untracked `.borg/checkpoints/*.md` files remain (this one + `2026-07-14-1746.md`), which are
user checkpoints, not repo commits. `/simplify` was run on the #79 code and assessed inline on #80's small diff;
#78/#81 are docs-only.

## 4. Blockers
No blockers.

## 5. Next Session
`usage-guardian-build` (`docs/plans/directives/2026-07-08-usage-guardian-build.md`) is the only borg-collective
directive still OPEN. Its Phase-2 work splits cleanly:
- **Data-gated (wait):** the `≥85%` checkpoint sweep threshold — do NOT tune until the log has 3+ independent
  near-cap episodes. Read the "Data-readiness note — 2026-07-14" at the top of that directive first.
- **Data-independent (can start anytime):** the delivery spike — prove whether `tmux send-keys /borg-link-up`
  can actually reach a *busy* pane (the `borg:8` zombie is the cautionary precedent), plus the two "Also do"
  items: correct `~/.claude/skills/borg-resume/SKILL.md`'s stale "not predictable from inside a session"
  disclaimer, and import `borg-resume` to source (`skills/borg-resume/` — currently installed with no source,
  a canonical-source violation).

Standing review queue (Noah's bottleneck, per prior handover): ingle #269/#270, troth #33, reveal #7.
Capacity warning was active at session start (5 projects). borg-collective itself is now clean.
