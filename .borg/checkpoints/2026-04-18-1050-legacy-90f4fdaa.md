Now I have enough context. Here is the structured debrief:

---

# Session Debrief: borg-collective
*Date: 2026-04-18 10:48 (session ended ~16:48 UTC)*

## Objective
Working assumption based on prior session's next-steps: smoke-test `drone scaffold --supabase` end-to-end on the `ingle` project and verify `pre-up.sh` correctly creates `supabase_network_ingle`. *(Transcript tail is too shallow to confirm — only the final ~90 seconds are visible.)*

## Outcome

**No commits landed.** Working tree is identical to the pre-session HEAD (`032ae26`).

**Untracked files present (not committed):**
- `.borg/checkpoints/2026-04-14-1600.md` — checkpoint from a prior session; not staged
- `.claude/settings.json` + `.claude/settings.local.json` — local plugin/permission overrides (supabase plugin enabled, various `Bash`/`Read` allow-list entries); not committed — likely generated by `borg setup`
- `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md` — portfolio pivot directive (Supabase + Fly.io for both Waypoint and wallpaper-kit); pre-dates today, still untracked
- `templates/supabase/.borg/debriefs/97674e46-….md` — April 16 session debrief written into the template tree rather than `.borg/`

**Confirmed actions from transcript (final ~90 seconds):**
- Three tmux windows active: `borg:1 orchestrator`, `borg:2 ingle`, `borg:3 reveal`
- Fixed tmux layout on `borg:2` (`ingle` window) to `even-horizontal`

## Decisions Made

None visible in the transcript tail — all visible work is environment/layout housekeeping.

## Blockers and Observations

1. **`tmux select-layout` targeting failed on first try** — Used `-t 2:ingle` (treating `2` as a session name); the session is named `borg`, so the correct target is `borg:2`. Claude self-corrected immediately after `tmux list-windows -a` confirmed the session/window names.

2. **`settings.local.json` contains a corrupt allow-list entry** — Line 22 reads `"Bash(matters most\" — all research citations preserved, narrative shifted from deficit framing:*)"`. This is a sentence fragment from what appears to be a commit message accidentally written into the permissions array. It is harmless but should be cleaned up.

3. **Debrief file landed in the wrong directory** — The April 16 debrief was written to `templates/supabase/.borg/debriefs/` instead of `.borg/debriefs/` at the repo root. The `templates/supabase/.borg/` path is untracked and semantically wrong (it's a scaffold template, not the live project's checkpoint store).

4. **No commits this session** — Either the session's work was exploratory/diagnostic only, or it was cut short before the commit phase.

## Next Steps

1. **Clean up `settings.local.json`** — Remove the corrupt permission entry on line 22 of `.claude/settings.local.json` (the fragment starting with `"Bash(matters most…"`).

2. **Commit or discard untracked files** — Decide on each:
   - `.borg/checkpoints/2026-04-14-1600.md` → commit alongside other checkpoints (already tracked pattern per `032ae26`)
   - `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md` → commit; it's a first-class planning artifact
   - `templates/supabase/.borg/` → move the debrief to `.borg/debriefs/` at repo root, then delete the stray directory

3. **Cut the v0.7.2 GitHub release** (still pending from April 16 next steps):
   ```
   gh release create v0.7.2 --title "v0.7.2" \
     --notes-file docs/plans/assimilated/2026-04-15-drone-hooks-supabase-bash-guard.md
   # Then fetch tarball sha256 and patch Formula/borg-collective.rb
   ```

4. **Complete the `ingle` smoke-test** — Run `drone scaffold --supabase ~/dev/ingle` (or wherever `ingle` lives), then `drone up`, and confirm `supabase_network_ingle` is created by `pre-up.sh` without errors. The `borg:2 ingle` tmux window suggests this was in progress but may not have completed.
only; still not committed anywhere.
- **`borg-collective/dotfiles/` subtree not yet deleted** — potentially fully redundant with `~/.config/dotfiles/` post-split; needs a diff-and-decide pass before deletion.

## Next Steps

1. **Switch to `ingle` and start Phase 1 of `2026-04-17-hosted-connector-architecture.md`:**
   - Create `/Users/noah/dev/ingle/mcp-server/src/context.ts` with `getContext(extra): Promise<{householdId: string}>` — reads `process.env.INGLE_HOUSEHOLD_ID`; stubs JWT path with TODO; throws if both are missing.
   - In each `/Users/noah/dev/ingle/mcp-server/src/tools/*.ts` (pantry, lists, meals, family, shopping, budget, household, predict, predict), replace all direct `process.env.INGLE_HOUSEHOLD_ID` reads with `const { householdId } = await getContext(extra)`.
   - Acceptance: `grep -R "process.env.INGLE_HOUSEHOLD_ID" mcp-server/src/` returns zero hits outside `context.ts`.

2. **Decide on `borg-collective/dotfiles/` subtree:** run `diff -rq /Users/noah/dev/borg-collective/dotfiles/ /Users/noah/.config/dotfiles/` and delete the subtree if it's fully redundant (it likely is after the session's split work).

3. **Resolve cairn + snowflake-projects:** either `git init` them and commit the devcontainer fix, or accept the changes as local-only disk state.
