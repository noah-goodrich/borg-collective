## 1. Goal
Continue the orchestration work from the prior session — execute the ingle frontend schema
migration end-to-end, advance reveal's P0 directives, and improve borg-collective's own
orchestrator architecture.

## 2. Accomplished

**ingle — frontend schema migration: COMPLETE (directive assimilated)**
- Phase 0: type regen via Supabase Management API + MCP server `stores→store_chains` fix
  (`61eea7d`)
- Phase 1: `store_chains` / `chain_id` across 7 frontend files + smoke tests (`33481fa`)
- Phase 2+3: `purchase_transactions` write path + exists-check migration (`06d3256`)
- Phase 4: `category_id` FK migration in actions + display (`be8e647`)
- Phase 5: `canonical_item_id` with `.name` text fallback (`f0a8a08`)
- Phase 6: `@types/pg` installed → zero tsc errors (`c4371b6`)
- Directive moved to `docs/plans/assimilated/` (`0451d1b`)
- 22 smoke tests passing, typecheck clean

**reveal — DB integrity, cost circuit breaker, worker hardening 5/7 (migrations pushed to prod
by Noah)**
- DB integrity: `consultation_briefs` RLS (`1544908`), `archetype_scores` UNIQUE (`22e0d5d`),
  `archetype_name` CHECK (`284c31d`), `persist_pipeline_data` transactional (`dce2224`)
- Cost circuit breaker: `model_calls` + `budget_ledger` tables, `record_model_call`
  fire-and-forget telemetry, per-photo cost cap, monthly budget ledger, no Redis
  (Supabase only). Commits `4b76686` + `794e423`. 30/30 tests
- Worker hardening: Gemini timeout (`d28a006`), atomic job claim CTE (`29b13e3`), atomic
  `attempt_count` (`ca00153`), 30s heartbeat (`3693204`), storage upload `exists()`
  verification (`5a0537d`). 1151/1151 tests
- All 4 migrations (`20260507000001-04`) plus `20260504000002_fix_gallery_rls.sql` pushed to
  prod by Noah directly via the reveal drone

**borg-collective — architectural pivot + v0.7.15**
- Multi-persona architectural review of the agentic orchestrator directive (saved at
  `docs/plans/reviews/2026-05-07-agentic-orchestrator-review.md`)
- Severed v1 directive → wrote nanoprobe v2 (`6481a4a`)
- Filed cairn restoration directive in cairn repo (`5f96879`)
- Filed sibling hook PATH completeness directive
- Verification spike: confirmed worktree × `drone exec` works, `SubagentStop` hook is real,
  cairn binary genuinely absent (saved at
  `docs/plans/reviews/2026-05-07-nanoprobe-verification.md`)
- Amended both directives with verified findings (`3f5fe51`, cairn `96e2556`) — pipx install
  for cairn, not dotfiles
- Implemented hook PATH fix in `borg-link-down.sh:20` + `borg-link-up.sh:17` (`7c1e2d1`)
- Implemented nanoprobe orchestrator: `agents/borg-nanoprobe.md`,
  `hooks/borg-nanoprobe-log.sh` (SubagentStop), `borg nanoprobes` / `borg nanoprobe-log <id>`
  + `np` alias, registered in both Claude Code and CoCo settings, README + CLAUDE.md updated
  (`4fc5eb2`)
- Released v0.7.15 — bump (`c87134b`), formula update (`368966c`), tag pushed
  (sha256 `4cb9808b...`)
- Committed prior session checkpoints (`abee9c9`)

**Memory updates:**
- Saved `feedback_nanoprobe_vs_drone.md` — routing rule: nanoprobe by default, drone for
  human-review tasks, escalate when nanoprobe blocks on intervention

## 3. Ready to Commit

borg-collective: 4 untracked items remain. None blocking.
- `docs/plans/reviews/` — architectural review + verification reports (worth committing as
  session artifacts)
- `skills/simplify/` — unknown, predates session — review before committing
- `.claude/` — likely local config, leave untracked
- `templates/supabase/.borg/` — predates session, leave

`/simplify` was not run this session. The major code-touching commits all came from
subagents that ran tests and committed clean — risk of dead code or stale comments is low,
but a `/simplify` pass before next release is reasonable.

Other projects: nothing uncommitted in ingle, reveal, or cairn.

## 4. Blockers

- **Nanoprobe end-to-end live spawn** — `4fc5eb2` shipped but the SubagentStop → JSONL
  pipeline has not been exercised by a real Agent tool spawn from an orchestrator session.
  First spawn next session is the live test.
- **Cairn still uninstalled** — directive filed (cairn `96e2556`), pipx install path
  documented, but binary not yet on PATH. SessionStart still prints CAIRN UNAVAILABLE.
- **Reveal worker hardening 2/7 deferred** — issue 6 (multi-variant transactional upload)
  and issue 7 (hallucination signature → archetype-incompatible exit) per the directive's
  2-3 session estimate.
- **CoCo SubagentStop registration not exercised** — wired identically to Claude Code, but
  no Cortex nanoprobe has run. Treat as opt-in.

## 5. Next Session

**First action: verify the nanoprobe pipeline live.** From a fresh orchestrator session,
spawn a trivial nanoprobe via the Agent tool with `subagent_type: "borg-nanoprobe"`. After
it completes, run `borg nanoprobes` and confirm a JSONL entry landed in
`~/.config/borg/agents.jsonl`. If the hook didn't fire or the payload schema differs from
what `hooks/borg-nanoprobe-log.sh` expects, fix that first — everything else depends on it.

**Then, in priority order:**
1. **Cairn restoration** — install cairn via pipx (per
   `cairn/docs/plans/directives/2026-05-07-cairn-restoration.md`), verify `command -v cairn`
   resolves from a SessionStart hook, confirm CAIRN UNAVAILABLE message disappears
2. **Reveal worker hardening — issues 6 and 7** — `worker/main.py:780-845` (transactional
   multi-variant upload) and `worker/main.py:663-668` +
   `pipeline_stages/claude_verify.py:35-53` (hallucination signature tracking →
   `archetype-incompatible` exit after 2 identical signatures)
3. **Reveal — pick the next P0 directive** — cost circuit breaker is shipped; remaining P0
   is worker-reliability-hardening (in progress). After that, P1 starts with api-hardening /
   observability-foundation / frontend-resilience

**Ingle is done**; no immediate ingle work unless data-quality / catalog-population work
surfaces from the now-deployed schema.
