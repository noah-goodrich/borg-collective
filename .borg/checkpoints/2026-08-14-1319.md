## 1. Goal

Answer "what's next?", then execute Phase 2 of the `borg link` Python port — which expanded, on the owner's
direction, into completing and assimilating the entire seven-criterion plan.

## 2. Accomplished

**The plan is shipped and archived.** `main` at `a9fdb45`;
`docs/plans/assimilated/2026-08-12-port-borg-link-to-the-python-core.md`. `borg link` is Python; the zsh
implementation is deleted. Overview latency 1.2s → 0.09s on the real registry. 651/651 bats, lint 10.00/10.

- **PRs merged**: #134 (`--json` seam, A3), #135 (two directives), #136 (Phase 3 entry-gate tests), #137 (the
  atomic render flip, A4+A5), #138 (`/borg-link` skill rewrite, A6), #139 (SKILL.md defects + simplify
  directive), #140 (`_fold_s` parity), #141 (`jq_default` + error boundary), #142 (parity harness),
  #143 (assimilation).
- **Two host blockers cleared**: `plan-promote-debug.log` deleted, `cairn` uninstalled from pipx.
- **`bin/link-parity-harness`** — the zsh-vs-Python differential, the only surviving oracle (zsh exists solely
  at `ad99612` in git history). Real run: 20 projects, 23 cases, 12 identical, 11 expected, **0 unexpected**.
  Registry checksum verified unchanged before/after.
- **Three defects found by real oracles, every one past a green suite**: the empty-`Shipped:` field-shift
  (real-registry diff), the capacity `>` boundary (mutation audit — survived 346 pytest + 112 bats), and
  `_fold_s` failing to reproduce `fold -s` (3504/4000 mismatch against the real binary).
- **Owner decisions recorded in the archived plan**: A4 amended to an enumerable delta; `total_projects` as a
  wire key (DOCUMENT_VERSION 2); severed dropped from the skill; A5's verify amended to definition/caller greps.

## 3. Ready to Commit

**Nothing.** Working tree clean, no stray worktrees, everything merged and pushed.

`/simplify` ran as part of assimilation — it is what produced the `_fold_s` ship-blocker plus eight deferred
findings, which are filed as `docs/plans/directives/2026-08-14-link-port-simplify-followups.md`.

## 4. Blockers

**The final session summary over-reported the follow-up state: four directives were filed, not five.** Filed:
`2026-08-13-coverage-gate-measures-the-wrong-thing`, `2026-08-13-recon-untested-branches`,
`2026-08-14-link-port-simplify-followups`, `2026-08-14-severed-in-the-link-document`.

**Recommended by the ship review but never filed:**

- **A deployed-artifact drift check** — the generalizable fix for the A6 failure. Both pinning tests
  (`skill_borg_link.bats:17`, `bash_guard.bats:654`) read repo source; nothing reads what `borg setup` writes
  to `$CLAUDE_DIR/skills`. Would cover all 16 skills and 12 hooks.
- **Newline normalization in the LLM summary path** — `summarize.py:272` returns Haiku's output verbatim while
  the heuristic path at :156/:171 defensively strips newlines. A newline in a stored summary makes
  `_summary_block` emit a flush-left sub-line, breaking the `^  [^ ]` contract. Latent: 1 summary live, 0 with
  newlines.
- **`cli.py:159-167` docstring is factually wrong** about the `--json` exception boundary — stdout was already
  empty and the exit code already 1 both before and after; only the stderr text changed.
- **`shell._read_text`'s CR contract** (text-mode open defeats the fidelity `core.heading_title`'s docstring
  asserts; its test uses an in-memory literal that never routes through `_read_text`), **dot-prefixed files in
  `shell._markdown_files`** (`Path.glob('*.md')` matches them, zsh's `*.md(N)` did not), and a
  **`_summary_block` unit test** (only `_fold_s` is tested; the composition around it is not).

**Two stale artifacts flagged and not handled**: `tests/cli_contract.bats:2032` still asserts GNU and BSD `fold`
disagree on break points — the premise's origin, corrected only in the archived plan — and
`docs/plans/directives/2026-08-14-link-port-simplify-followups.md` carries a dead PR link (`stillpoint-labs`
should be `noah-goodrich`).

**Severed is dark**: 24 plans across 4 projects (4 borg-collective, 16 ingle, 2 reveal, 2
reveal-data-consistency) have no readback surface until `2026-08-14-severed-in-the-link-document` lands.

## 5. Next Session

**Start with the deployed-artifact drift check.** It is the only unfiled item that is a *mechanism* rather than
a one-off fix, and it closes the exact failure that produced this session's last blocker: A6 was verified,
ticked, then invalidated forty minutes later when #139 edited the artifact that had been verified. Add either a
`borg doctor` check or a test that diffs every deployed skill and hook against source. `borg setup`'s copy path
is in `cmd_setup` (`borg.zsh`); the two destinations are `$HOME/.claude/skills/<name>/` and
`/Users/noah/dev/claude-plugins/borg-collective/skills/<name>/`.

Then the two five-minute cleanups above (`cli_contract.bats:2032`, the dead PR link) — already known, just not
done.

**Do not re-run a defect search on the link port.** Three rounds found three real defects; the fourth found zero
live ones and the reviewer closed the search explicitly. Every remaining finding is latent and has a directive.

The transferable lesson, if only one thing survives: **a fixture that avoids the hard input proves nothing about
it.** `cli_contract.bats:1375` keeps the deep summary under 70 chars "so `fold -s -w 70` is a no-op"; every
assimilated fixture wrote a `Shipped:` line; `capacity(0, 3)` is mutation-blind by construction. Reach for
`bin/link-parity-harness` before reaching for the suite.
