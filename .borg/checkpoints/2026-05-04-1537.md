## 1. Goal

Orchestration session: ingle-site marketing site scaffold, reveal display_name fix,
borg-collective releases v0.7.8–v0.7.10.

## 2. Accomplished

**ingle-site (ingle.kitchen) — SHIPPED**
- Devcontainer written (Dockerfile, docker-compose.yml, devcontainer.json) mirroring
  reveal-site pattern. Port 4322 (4321 was taken by reveal-site).
- Registered with borg, started with `drone up ingle-site`.
- Tailwind v4 + `@tailwindcss/vite` (correct for Astro 6; `@astrojs/tailwind` requires ≤5).
- Source files: `tokens.css` (brand palette), `Base.astro`, `Header/Footer/HearthIcon.astro`,
  `index.astro` (landing), `privacy.astro`.
- Build verified inside container: 2 pages, clean.
- GitHub repo `stillpoint-labs/ingle-site` created, secrets set, pushed.
- Deploy workflow fixed (use `npx wrangler` with `env:` not wrangler-action inputs).
- GitHub Actions deploy: green. Site live at ingle.kitchen via CF Pages `ingle-holding`.
- Directive `2026-05-03-marketing-site.md` assimilated in ingle repo.

**reveal — display_name fix**
- Root cause: batch upload script never set `display_name`/`location_name`; UI fell back
  to "Untitled photograph" for every image.
- Backfill migration `20260504000001`: updated 291 complete photos from `triage_results`.
- Worker `mark_complete`: now writes `display_name` via `COALESCE` from archetype.
- Test updated, deployed to Fly.io, worker scaled back to 1 machine.
- Queue status: 291 complete, 51 pending, 2 claimed, 31 failed.

**reveal API/subscription audit**
- Confirmed: `ANTHROPIC_SDK_KEY` (custom var) is set on host but Claude Code only reads
  `ANTHROPIC_API_KEY` — all interactive sessions use the Max subscription.
- Reveal worker on Fly.io uses API intentionally via `ANTHROPIC_SDK_KEY`.

**borg-collective releases**
- v0.7.8: store-secret, host-first dispatch, cortex auto-wake (catching up from Apr 28)
- v0.7.9: checkpoint gate on `borg sever` (warn + y/N before closing)
- v0.7.10: `borg sever` now offers to run `/borg-link-up` via tmux send-keys, waits up
  to 120s for checkpoint file before proceeding

## 3. Ready to Commit

**borg-collective**: checkpoint file only.
```
git add .borg/checkpoints/ && git commit -m "chore: session checkpoint 2026-05-04"
```

**ingle**: `fff5eaf` already pushed. Clean.
**reveal**: `4662dfa` already pushed. Clean.
**ingle-site**: all committed and pushed.

## 4. Blockers

- **host node_modules**: `/Users/noah/dev/ingle-site/node_modules` from bad pre-devcontainer
  npm install. bash-guard blocks `rm -rf` on home paths. Clean with:
  `! rm -rf /Users/noah/dev/ingle-site/node_modules`
- **Reveal-site domain**: Worker deployed but domain may still point at old CF Pages project.
  User action in Cloudflare dashboard needed.
- **Reveal 51 pending**: queue stalled (2 claimed). May need worker restart or manual reclaim.
- **Cairn**: still not in PATH.

## 5. Next Session

1. Clean host node_modules: `! rm -rf /Users/noah/dev/ingle-site/node_modules`
2. Check reveal.photo — logo, gallery (now with display names), SSR pages.
3. Check reveal queue: 51 pending + 2 claimed may be stalled — `flyctl logs --app reveal-worker`
4. Run `brew upgrade borg-collective` to pull v0.7.10.
5. Borg agentic plan: convert `docs/plans/directives/2026-05-03-agentic-orchestrator.md`
   into an active PROJECT_PLAN.md if ready to start Phase 1.
