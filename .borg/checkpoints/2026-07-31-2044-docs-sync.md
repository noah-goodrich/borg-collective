## 1. Goal

Review [mswi#239](https://github.com/Ontra-ai/mswi/pull/239), then — when borg:3 reported cairn down — find
out whether cairn was actually down and unblock that session.

## 2. Accomplished

- **Reviewed [mswi#239](https://github.com/Ontra-ai/mswi/pull/239)** (DE-1903 query-manifest loader, M1.1):
  verdict **approve** with five non-blocking notes (account_id type drift, `.id` None-safety for uuid-keyed
  rows, runtime-only inner-query contract, duplicate model→table mapping, `key_column` unused until M1.2).
  Read-only; **push + Slack-ping to Kelly were cancelled** — "Mark got it." Nothing posted to the PR, no
  Slack sent.
- **Diagnosed the "cairn down" false alarm.** borg:3 (ai-data-engineer) reported DEGRADED, but cairn was
  never down: 6/6 `/health` probes HTTP 200 in ~2-3ms, port 8767 listening via gvproxy (Podman). borg:3's
  `20260731-2309` checkpoint also **did** record to the graph (conf=1.00) — its "not recorded" claim was
  false too.
- **Root-caused two bugs in the `/borg-link-up` section-0 cairn snippet**, both macOS/zsh-specific and both
  collapsing to a false DEGRADED: (1) `timeout` binary is absent on macOS (it's `gtimeout`) → empty health
  output; (2) `status` is a read-only variable in zsh → `status=$(…)` aborts the snippet, and the skill's
  "treat any error as DEGRADED" rule then prints the false alarm. The v0.8.9 Stop *hook* already guarded
  `timeout`; only the skill snippet was stale.
- **Fixed both**, in source + deployed copy: guard `command -v timeout` (fallback to bare `cairn health`) and
  rename `status` → `st`. Verified the fixed snippet runs clean in zsh and prints `cairn: healthy · recording`.
  Files: `skills/borg-link-up/SKILL.md` (source) and `~/.claude/skills/borg-link-up/SKILL.md` (deployed
  `.borg-managed` copy).

## 3. Ready to Commit

- **`skills/borg-link-up/SKILL.md`** in `~/dev/borg-collective` (` M`) — the timeout-guard + `status`→`st`
  fix. Change is a 6-line bash-snippet edit inside markdown; `/simplify` is N/A (no code logic). **Caveat:**
  borg-collective is currently on branch `docs/import-infoviz`, which is unrelated to this fix — branch this
  off `main` on its own before committing, don't ride the docs branch. The untracked
  `docs/research/2026-07-28-dependency-graph-tool/` is pre-existing, not mine.
- The deployed `~/.claude/skills/borg-link-up/SKILL.md` was hand-mirrored for immediate effect; it's
  `.borg-managed` and will be regenerated from source on the next `borg setup`.
- mswi: only `.gitignore` (` M`) is dirty and it's **not mine** — leave it.

## 4. Blockers

No blockers. Open decision for Noah: (a) commit the borg-collective skill fix (needs its own branch off
`main`) and/or run `borg setup` to redeploy across all windows; (b) whether to relay the correction into the
borg:3 pane — I did **not** inject, because it has `! drone down` staged in its input line and keystrokes
would corrupt it.

## 5. Next Session

Start by deciding the borg-collective skill-fix commit: `git -C ~/dev/borg-collective switch -c
fix/link-up-timeout-guard main`, then commit `skills/borg-link-up/SKILL.md`, open a PR. After merge, run
`borg setup` so every window's deployed skill matches source. Separately, if borg:3 is still up, paste the
one-line correction (cairn is up, `20260731-2309` recorded, don't restart cairn) so it stops acting on the
false DEGRADED. The mswi#239 review is done and lives only in this transcript (never pushed) — re-post only
if Noah asks; otherwise nothing is owed there.
