## 1. Goal

Clean up the four borg-collective follow-ups deferred by the previous session's lifecycle-inversion
checkpoint (briefing test fix, docs prose sweep, separate-repo commits, historical debriefs cleanup)
so the tree is clean before pivoting to notifyd's Container Notification Bridge plan.

## 2. Accomplished

**Five commits landed across three repos** + one non-committed migration:

1. **`~/.config/dotfiles` `641721c`** `feat(tmux): = / _ layout binds; remove 70/30 resize hook` —
   `bind =` → `even-horizontal`, `bind _` → `even-vertical`; removed stale `after-resize-window`
   hook that force-pulled 3-pane layouts to 70/30; docker-compose.base.yml comment documenting
   `/workspaces/<project>` mount pattern.

2. **`~/dev/reveal` `bd8d560`** (branch `supabase-migration`) `feat(devcontainer): mount at
   /workspaces/reveal for session-dir isolation` — Dockerfile WORKDIR, devcontainer.json
   workspaceFolder + postStartCommand /workspace symlink, docker-compose.yml volume + working_dir.
   Also re-ran `pre-commit install` inside reveal's `.venv` to replace a stale git hook that
   pointed at `/usr/local/bin/python3.12` (nonexistent).

3. **`~/dev/ingle` `62805bf`** `feat(devcontainer): mount at /workspaces/ingle + wire Supabase
   Cloud env` — bundled workspace-path migration + NEXT_PUBLIC_SUPABASE_URL/ANON_KEY env vars
   (anon key is RLS-enforced per in-file comment).

4. **`borg-collective` `e84a2e5`** `fix(briefing): escape nested quotes in prompt; separate --brief
   from overview` — the real win of the session. `_borg_print_briefing` had `"Next Session"`
   literal double-quotes inside the `briefing_prompt="..."` multi-line assignment, which closed the
   zsh string early and caused a "file name too long" failure in `borg link --brief` whenever any
   project was in the registry (production bug, not just test bug). Fixed with `\"..\"` escape.
   Separately, `cmd_link --brief` now dispatches directly to `_borg_print_briefing` via
   `borg_desktop_scan` instead of routing through `_borg_link_overview` — matches the help text
   ("LLM narrative briefing"), drops ~300 ms of jq + desktop-scan work per invocation, and keeps
   `_borg_link_overview`'s signature clean (removed the `do_brief` parameter).
   `tests/briefing.bats` swapped `$BORG_CMD briefing` → `$BORG_CMD link --brief` on 7 lines + header
   comment. All 8 briefing tests now green; full suite 141/141.

5. **`borg-collective` `57ef9bc`** `docs: update hook + skill names for link-up/link-down
   lifecycle` — 9 doc files swept: `architecture.md` (Data Flow rewrite, Skills table rebuild,
   hook registrations JSON), `boris-workflow.md` (Part 3 narrative rewritten around user-authored
   checkpoints, Part 4 Tool Map updated), `cheatsheet.md`, `competitive-landscape.md`,
   `devcontainer-coco.md`, `quickstart.md`, `six-pager.md` (+ new 2026-04-23 Change Log entry),
   `skills-guide.md`, and `docs/plans/directives/backlog-cairn-triage.md` (one line). Renames
   applied: `borg-start.sh` → `borg-link-down.sh`, `borg-stop.sh` → `borg-link-up.sh`,
   `/borg-ship` → `/borg-assimilate`, `/borg-checkpoint` folded into `/borg-link-up`,
   `/borg-debrief` removed (no more auto-LLM session analysis). `plans/assimilated/` intentionally
   untouched (historical).

6. **Git-hygiene save** — the docs commit accidentally included the pre-existing untracked
   `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md` (a separate directive the prior
   session explicitly left deferred). Caught it, did `git reset --soft HEAD^` + unstaged the file,
   recommitted cleanly. File is back to untracked.

7. **Debriefs migration (non-committed, `.borg/` is gitignored)** — 20 legacy LLM-debrief files
   moved into their respective projects' `.borg/checkpoints/` dirs with the naming scheme
   `<mtime-YYYY-MM-DD-HHMM>-legacy-<tag>.md` (where `<tag>` is the first 8 of the UUID for
   per-session debriefs, or `global` / `global-snowfort-audit` for the `~/.config/borg/debriefs/`
   files). Three debriefs dirs removed: `~/.config/borg/debriefs/`,
   `borg-collective/.borg/debriefs/`, `reveal/.borg/debriefs/`. Stale `dev.md.tmp` ("Not logged in"
   content) deleted. New checkpoint dirs created for cairn, dotfiles, wallpaper-kit,
   wayfinderai-waypoint. Legacy files sort chronologically alongside real checkpoints.

8. **`/simplify` three-agent review** ran on the borg.zsh + briefing.bats diff before committing.
   Reuse agent said clean. Quality agent flagged heredoc as a nicer alternative (noted, not
   applied — escape fix is minimal and matches existing pattern in the file). Efficiency agent
   caught the `--brief` dispatch doing 300 ms of wasted overview work per call — this finding was
   applied and is part of the commit.

## 3. Ready to Commit

**Nothing in borg-collective.** Working tree carries only the pre-existing intentionally-deferred
items (same three as the 2026-04-23-1714 checkpoint listed):

- `.gitignore` (pre-existing, unrelated)
- `.claude/` (untracked, session-local)
- `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md` (untracked, separate directive)

`/simplify` was run before committing the code changes; findings applied or explicitly deferred.

## 4. Blockers

No blockers. Deferred non-blockers for future reference:

- Reveal has uncommitted `scripts/deploy_worker.sh` + `scripts/verify_secrets.sh` modifications.
  Out of scope for workspace-path cleanup; left untouched.
- Ingle has an untracked `supabase/migrations/0004_index_nullable_fks.sql`. Out of scope.
- Dotfiles has uncommitted `git/config` + `zsh/secrets.zsh` modifications. Out of scope and
  potentially sensitive; left alone.
- Optional heredoc refactor of `briefing_prompt=` in `borg.zsh:1420-1434` — quality agent's
  suggestion to eliminate the future-quote-footgun. Cheap follow-up; not load-bearing.

## 5. Next Session

**Primary destination: notifyd PROJECT_PLAN.md (Container Notification Bridge).** 0/7 acceptance
criteria met as of the 2026-04-23-1714 checkpoint. This is the actual active plan. The notifyd
project is at `~/dev/notifyd/` (or wherever it lives — grep `PROJECT_PLAN.md` under `~/dev/` if the
path is uncertain). Start by reading `PROJECT_PLAN.md` and `/borg-review` to understand current
state of the 7 ACs, then pick the first unmet criterion to tackle.

If the user wants one more borg-collective follow-up first (quick win, ~5 min):
- Convert `briefing_prompt=` in `borg.zsh:1420-1434` from escape-heavy double-quoted string to a
  `read -r -d '' briefing_prompt <<EOF ... EOF` heredoc so nested quotes are no longer a footgun.
  Would also eliminate the `\"Next Session\"` escape added this session.
