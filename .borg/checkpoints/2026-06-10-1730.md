## 1. Goal

Coordinate the borg-collective + cairn + claude-plugins initiative to a stable, correct state (distribute
borg via the Claude Code plugin system, make cairn installable, fix nanoprobe worktrees), then —
mid-session — instrument cross-session token spend and reframe spend optimization.

## 2. Accomplished

**Plugin rollout (the primary goal) — shipped + installed:**
- claude-plugins `main`: merged #7 (meta-research-review), #8 (directives-02-06), #11 (borg plugin
  content v0.2.2 — 9 self-contained hooks + `borg-nanoprobe` agent; closed #9 as superseded by a clean
  `build-plugin.sh` regen). Permanently fixed the `deep-research-stop.sh` Stop-hook recurrence (hooks now
  on `main`; live plugins resolve from the `main` working tree).
- borg-collective `main`: merged #43 (nanoprobe-managed worktrees, #42), #44 (`build-plugin.sh` + hook
  de-dup + install detection), #45 (de-dup over-removal **data-loss fix** + regression test, 248/248 bats).
- Local env: ran `borg setup` (settings.json de-dup applied), installed + enabled `borg-collective@noah-local`,
  and **restored the user's `session-log.sh`** that the de-dup over-removal had deleted (root-caused → #45).

**cairn — distribution merged + activated:**
- Merged #5 (Dockerfile.standalone, GHCR publish workflow, in-repo `cli/cairn`, `/health` contract, LLM
  key passthrough). `bin/cairn-up` rebuilt + recreated `cairn-api` on the standalone image; `/health` +
  `/ready` green, migrations at `002`. ANTHROPIC key wired (Keychain `cairn-anthropic-key` + gitignored
  `.env` + compose runtime passthrough; image bakes no secret). boardroom now reachable.

**Token-spend tracking (new scope) — shipped:**
- claude-plugins `main`: #10 (subagent-aware token accounting), #12 (SessionEnd collector →
  `~/.claude/token-spend.jsonl`, per-model raw counts + `est_cost_usd`), #13 (corrected cost model +
  `backfill-spend.sh`). **Backfilled 123 historical sessions** ($19,485 API-equiv total; queryable by
  project/time via `jq`).
- **Cost reality corrected:** a heavy session is ~96% main-loop (Opus: thinking-as-output + ~80M cache
  reads) and ~4% subagents (Sonnet/Haiku — harness already tier-routes). Earlier "$56/$43 Opus waves"
  were mispriced. Reframed the spend-opt directive #46 to **orchestrator-first** (lean main context,
  delegate output-heavy work returning summaries, fewer turns).

## 3. Ready to Commit

Nothing — all work is committed and merged across the three repos; working trees are on `main` and clean.
`/simplify` was not run this session, but everything shipped via reviewed PRs with tests (borg bats
248/248; collector shellcheck clean). The token-spend collector will NOT capture *this* session (its hook
loaded before the collector existed) — re-run `backfill-spend.sh` next session to capture it if wanted.

## 4. Blockers

No hard blockers. Pending (user-gated / future):
- **Restart Claude Code** to activate the `borg-collective` plugin and the token-spend SessionEnd
  collector (both load at session start).
- **Work-machine rollout** (now safe with #45): `git pull` both repos → `borg setup` → `claude plugin
  marketplace add noah-goodrich/claude-plugins` → `claude plugin install borg-collective@noah-local`.
- Spend-opt directive #46 is filed/merged but **not implemented** (wiring guidance into CLAUDE.md is future).
- cairn optional: push a `v*` tag → GHCR publish + set the package public; cairn knowledge graph still sparse.

## 5. Next Session

1. **Restart Claude Code first** (activate the plugin + the spend collector).
2. **Implement spend-opt directive** `docs/plans/directives/2026-06-10-subagent-spend-optimization.md`:
   add lean-context discipline to the CLAUDE.md nanoprobe-orchestrator section (delegate output-heavy
   work, return summaries not raw dumps; minimize main-loop turns/thinking). This is the ~96% cost lever.
3. Run the **work-machine rollout** (commands above).
4. Optional: cairn `v*` tag → GHCR publish + package visibility; cairn knowledge backfill; re-run
   `token-cost/scripts/backfill-spend.sh` to capture this session.
