# 2026-05-27-0917 — borg-collective checkpoint

Supersedes the 2026-05-26-2203 placeholder written by the Dispatch orchestrator. Deleted in the
same commit as this checkpoint.

## 1. Goal

Close out the weekend session that pushed three streams in parallel: (a) the GTM agent-teams
research rerun on `research/agent-teams-2026-05-23`, (b) the positioning-refresh research
captured under `docs/research/2026-05-22-positioning-refresh/`, and (c) the cross-project
borg-state cleanup that this checkpoint is part of. Leave the repo in a state where Noah can
pick up Monday morning without rebuilding context, decide the borg-collective ↔ claude-plugins
source-of-truth question, and route the uncommitted project-state edits onto their own branch.

## 2. Accomplished

- **GTM agent-teams research** committed through Phase 6 on `research/agent-teams-2026-05-23`:
  Phase 1 design (`5f555eb`), Phase 3 source cards (`e7370af`, 16 sources), Phase 3.5 citation
  verification (`8f04a98`, 0% failure), Phase 4-6 analysis (`1f67464`). Outputs live at
  `docs/research/2026-05-23-agent-teams/{analysis.md,verification-report.md,sources/,drafts/}`.
- **Positioning-refresh research** drafted under `docs/research/2026-05-22-positioning-refresh/`
  (analysis.md, addendum-agentic-orchestrator-review.md, sources/). Untracked — not on the
  research branch's commit history, just sitting in the worktree.
- **Skill extension protocol (v1)** shipped earlier in the week. Both `skills/borg-plan/SKILL.md`
  and `skills/borg-assimilate/SKILL.md` reference `skill-extensions/<hook>.md` load points;
  CLAUDE.md "Key Patterns" documents the protocol. This is the work PROJECT_PLAN.md describes —
  treat that plan as effectively shipped (see handoff doc on PROJECT_PLAN staleness).
- **Three directives** standing under `docs/plans/directives/`: dev-dir audit (2026-05-11),
  orchestrator-mode separation (2026-05-11), per-project state migration (2026-05-11). No new
  directives this session.
- **`hooks/borg-plan-promote.sh` + `tests/plan_promote.bats`** appear in the worktree as
  untracked. The `PreToolUse` auto-plan-promote pattern is already documented in CLAUDE.md "Key
  Patterns", so the implementation has landed but hasn't been committed yet — separate concern,
  not part of this borg-state PR.

## 3. Ready to Commit

This checkpoint commit (`chore/borg-state-2026-05-27`) covers only borg-state files:

- `.borg/checkpoints/2026-05-27-0917.md` (this file)
- Delete of `.borg/checkpoints/2026-05-26-2203.md` (placeholder superseded)
- `docs/plans/handoff/2026-05-27-borg-claude-plugins-source-of-truth.md`
- `docs/plans/handoff/2026-05-27-project-plan-stale.md`
- `docs/plans/handoff/2026-05-27-research-branch-split.md`

Everything else uncommitted in the worktree is deliberately NOT in this PR — see the
research-branch-split handoff doc for the routing.

## 4. Blockers

- **borg-collective ↔ claude-plugins source-of-truth (skill files).** Captured as an open
  question in `docs/plans/handoff/2026-05-27-borg-claude-plugins-source-of-truth.md`. Needs a
  Noah decision before either repo can confidently delete or re-symlink its skill copies.
- **PROJECT_PLAN.md staleness.** The "Skill Extension Protocol" plan looks shipped against the
  current code, but I'm not editing PROJECT_PLAN.md myself — that's a Noah call. See
  `docs/plans/handoff/2026-05-27-project-plan-stale.md` for the evidence.
- **Research-branch hygiene.** The 5 modified files (`.gitignore`, `CLAUDE.md`, `README.md`,
  `borg.zsh`, `docs/architecture.md`) are project-wide concerns sitting on a research branch.
  They belong on their own branch (`chore/project-state-2026-05-27`). Stashed temporarily to
  cut this PR off main; needs to be unstashed and routed properly. See
  `docs/plans/handoff/2026-05-27-research-branch-split.md`.

## 5. Next Session

1. Read the three handoff docs in `docs/plans/handoff/` — they're the carry-forward.
2. Make the source-of-truth call (borg-collective vs claude-plugins for skill files). Update
   CLAUDE.md + claude-plugins/README to match.
3. Pop the stash and run the research-branch-split commands from
   `2026-05-27-research-branch-split.md` to land the 5 modified files on their own branch and
   open a PR.
4. Disposition PROJECT_PLAN.md: either move to `docs/plans/assimilated/` with a brief
   completion note, or rewrite for the next initiative.
5. Decide whether to commit `hooks/borg-plan-promote.sh` + `tests/plan_promote.bats` — the
   pattern is already documented in CLAUDE.md but the files aren't tracked.

Cross-reference: [[claude-plugins-project]] for the parallel skill-distribution work,
[[cairn-project]] for the optional knowledge-graph backend, [[stillpoint-cross-project-principles]]
for the design principles applied here.

Stash reference (temp): `borg-state-2026-05-27-temp-stash` on `research/agent-teams-2026-05-23` —
contains the 5 modified project-state files. Pop with `git stash pop` after checking out a fresh
branch.
