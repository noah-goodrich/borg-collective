## 1. Goal

Execute the inversion swap plan from the 2026-04-23-1000 checkpoint end-to-end: flip the
link-up/link-down semantics across hooks, skills, canonical + live settings, docs, tests, and
memory; run /simplify; commit as two commits; leave a clean base for the next session to return
to notifyd work.

## 2. Accomplished

**Lifecycle-inversion swap: complete and committed.** All 12 steps from the 0950/1000 checkpoint
plan executed:

1. File renames via `.swap` intermediate — `hooks/borg-link-up.sh` ↔ `hooks/borg-link-down.sh`,
   `skills/borg-link-down/` → `skills/borg-link-up/`.
2. Content swaps — header comments flipped, `/borg-link-down` ↔ `/borg-link-up` text updated,
   SKILL.md `name:` + description + heading rewritten for `borg-link-up`.
3. `borg.zsh cmd_setup` — SessionStart → `borg-link-down.sh`, Stop → `borg-link-up.sh`
   (Claude + CoCo blocks, 4 registrations). Migration cleanup log messages updated.
4. `config/claude/settings.base.json` — flipped.
5. Live env — `~/.claude/settings.json`, `~/.snowflake/cortex/settings.json`, installed hooks in
   both dirs, `~/.claude/skills/borg-link-up/` (old `borg-link-down/` removed). All three
   settings JSONs verified with `jq`.
6. Docs — `README.md` hook table + Session Checkpoints prose + skills section, `CLAUDE.md` hook
   list + skills list, `install.sh` help text, `hooks/tool-count-nudge.sh` nudge text,
   `skills/borg-review/SKILL.md`, `lib/borg-hooks.sh` comment, `tests/cairn.bats` +
   `tests/lifecycle.bats` BORG_START/BORG_STOP.
7. Memory — `feedback_simplify_checkpoint.md` (name + body + history note), `MEMORY.md` index entry.
8. Verify — `zsh -n borg.zsh`, `bash -n hooks/*.sh`, `jq empty` on all three settings files all
   pass. 134/141 bats pass; the 7 failures are all in `briefing.bats` (pre-existing, unrelated —
   `borg briefing` subcommand no longer exists).
9. `/simplify` — three agents, aggregated findings, applied three clear wins: extracted
   `_ws_symlink_snippet` helper in `drone.zsh` (dedupes supabase + generic scaffold paths), merged
   twin `has_uncommitted_changes` jq blocks in `borg-link-up.sh`, trimmed a 5-line narration
   comment in `cmd_scaffold`. Ran live-hook refresh after edits.
10. Commits landed on `main`:
    - **`8d3f3f8`** `feat(drone): default workspace to /workspaces/<project>; side-by-side 2-pane
      default` — `drone.zsh` + `templates/supabase/devcontainer.json`
    - **`cfc6f09`** `feat(lifecycle): kill debriefs; add /borg-link-up + borg-link-down.sh
      session-lifecycle` — 16 files, 251 insertions / 481 deletions
11. Also culled 5 obsolete debrief-era tests from `tests/lifecycle.bats` (bundled into Commit B
    since they tested removed functionality): `start hook injects debrief`, `does not include
    debrief section`, `debrief file is created after stop hook`, `next session start injects
    debrief from previous stop`, `registry summary is updated from debrief objective after stop`,
    `stop hook uses ANTHROPIC_SDK_KEY`.

## 3. Ready to Commit

**Nothing more from this session.** Both commits already landed; working tree carries only the
intentionally-deferred items from the plan:

- `.gitignore` (pre-existing, unrelated to this work)
- `.claude/` (untracked; session-local)
- `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md` (untracked; separate directive)

`/simplify` was run before committing; findings applied or explicitly deferred with reasoning.

## 4. Blockers

**No blockers on the swap itself.** Deferred items (not blockers, just scope):

- Separate-repo commits still outstanding: `~/.config/dotfiles/` (tmux.conf +
  docker-compose.base.yml comment), `/Users/noah/dev/reveal/.devcontainer/`,
  `/Users/noah/dev/ingle/.devcontainer/`. These are orthogonal to the borg-collective commits.
- `briefing.bats` 7 failures — pre-existing. `borg briefing` subcommand was replaced by
  `borg link --brief`; tests still call the old subcommand and return 127. Quick fix (swap
  `$BORG_CMD briefing` → `$BORG_CMD link --brief`), but out of scope for the swap.
- `docs/architecture.md`, `skills-guide.md`, `devcontainer-coco.md`, `boris-workflow.md`,
  `cheatsheet.md`, `six-pager.md` still reference old hook/skill names in prose. Non-functional
  (prose only).

## 5. Next Session

**Primary destination: notifyd PROJECT_PLAN.md (Container Notification Bridge).** 0/7 acceptance
criteria met as of the last checkpoint. That's the actual active plan; this whole session plus
the two prior were tangent work.

If picking up borg-collective follow-ups first (one sitting each):

1. **Separate-repo commits** (5 min, mechanical):
   - `~/.config/dotfiles/` — `tmux/tmux.conf` (bind = even-horizontal, bind _ even-vertical,
     dead 70/30 hook removed) + `devcontainer/docker-compose.base.yml` comment update
   - `/Users/noah/dev/reveal/.devcontainer/` — workspace path change
   - `/Users/noah/dev/ingle/.devcontainer/` — workspace path change + `CLAUDE_PLUGIN_ROOT` env var
2. **Fix `briefing.bats`** — swap `$BORG_CMD briefing` → `$BORG_CMD link --brief` in
   `tests/briefing.bats` lines 52/58/64/71/88/102/120. Would turn the 7 failures green.
3. **Docs sweep** — follow-up directive to update the six markdown files still referencing old
   names.
4. **Historical `.borg/debriefs/` cleanup (optional)** — delete historical dirs in reveal,
   borg-collective, snowfort, ingle + `~/.config/borg/debriefs/`. No longer written to.

The link-up/link-down semantic is now correct and stable. Future `/borg-link-up` calls will save
here; next SessionStart's `borg-link-down.sh` will pick it up automatically.
