## 1. Goal

Merge PR #163 (SA1–SA6 security implementation) into PR #161 (its directive), get #161 reviewed and merged, and
deploy the result to the live install including the plugin publish.

## 2. Accomplished

- **Consolidated the two security PRs.** Merged `feat/security-audit-remediation` into
  `docs/security-audit-directive` in a throwaway worktree (clean, no conflicts), pushed as `a8699aa`. #161 then
  carried both the directive and the SA1–SA6 implementation.
- **Stamped and merged #161.** Posted the work-machine `STACK-APPROVAL ... @ a8699aa` stamp (the protocol's
  head-refresh requirement) and answered the personal machine's open process question by declaring intent: merge
  #161, close #163 as superseded. Noah approved verbally; squash-merged to main as `bd2a153`.
- **Closed #163 manually.** The squash rewrote SHAs, so GitHub did not auto-close it. Closed with a comment
  pointing at `bd2a153` and deleted its branch — neither PR merged twice.
- **Deployed to the live install.** Ran `install.sh --quiet` (all five launchd agents re-bootstrapped;
  usage-watch self-verified with a new sample row) and `borg setup` after the merge. Verified deployed
  `bash-guard.sh` and the skills are byte-identical to the repo.
- **Plugin publish as a branch, per Noah's instruction.** Committed the rebuild to
  `chore/rebuild-borg-collective-plugin` in `~/dev/claude-plugins` and opened PR #43.
- **Found and fixed a real deploy gap:** `claude plugin update` silently no-ops when plugin content changes but
  the version does not (still 0.8.9) — the installed plugin was a cached Aug-10 copy. Forced
  uninstall/reinstall and verified the cache matched the build.
- **Caught stale-content drift the next day.** PR #43 was closed, not merged — the personal machine's comment
  said it was "Superseded by #45" because "Four files on this branch are behind the current working tree". Since
  borg-collective `f7ba070` makes the plugin the *only* delivery channel for skills/agents, my earlier reinstall
  had baked my now-stale branch content into the live cache. Re-synced both repos to their origin tips,
  reinstalled the plugin from current main, verified byte-identical, deleted the superseded local branch.
- **Ran the morning brief** scoped to borg-collective (noted that `data-engineer:de-brief` is not installed on
  this machine, so the live pass was GitHub + git only).

## 3. Ready to Commit

Nothing staged. Six untracked files, unchanged since the 2026-08-22 checkpoint flagged them:

- `docs/plans/directives/2026-08-25-orchestrator-drone-handoff-in-hail.md` — **exists only on this machine**;
  confirmed absent from `origin/main`. The orchestrator filed it and it was never committed. Most at-risk file here.
- `.borg/checkpoints/{2026-08-12-0900, 2026-08-20-0803, 2026-08-22-1251, 2026-08-22-2205}.md` — note that
  `origin/main` *does* track checkpoints (it has `2026-08-25-1117/1312/1738.md` from the personal machine), so
  these four are genuinely un-pushed local history, not an ignored path.
- `uv.lock` — still untracked and still unresolved; the 08-22 checkpoint called it "my test artifact — `rm` it or
  track it deliberately." Noah has not ruled.

/simplify not run this session. It is not needed for the merge/deploy work — this session wrote no product code,
only PR comments, commit messages, and this checkpoint.

## 4. Blockers

No blockers on the shipped work. Two open items that need Noah, not an agent:

- The `uv.lock` decision (rm vs track) has now survived three checkpoints unresolved.
- `hold/backfill-rework` (`278c484`) is confirmed still parked — not an ancestor of `origin/main`. It holds
  findings 1–3 plus the `programs.edges_from` `order: "–"` inversion.

## 5. Next Session

1. **Sync first — local `main` is 73 commits behind `origin/main`.** The personal machine shipped heavily
   2026-08-25 → 08-31, tip `2a2aa42`, including PRs #174/#175/#176 (titles from GitHub: "fix/link-port-latent-
   defects", "fix/unsure-reachable-and-picture-width", "feat/fold-brief-into-the-document"). Run
   `git pull --ff-only`, then `./install.sh --quiet` — `f7ba070` means skills and agents now reach the live
   install *only* through the plugin, so a pull without a rebuild+reinstall leaves the running install stale.
   That is the exact trap that bit this session.
2. **Commit the orchestrator directive** (`docs/plans/directives/2026-08-25-orchestrator-drone-handoff-in-hail.md`)
   before anything else — it is one `rm -rf` from gone. Then the four checkpoints, then settle `uv.lock`.
3. **Fix the plugin-version no-op**, the concrete bug this session found: make the plugin build bump
   `BORG_VERSION` / `plugin.json` version when built content changes, so `claude plugin update` stops silently
   doing nothing. Build code is `scripts/build-plugin*` (the "Phase 5: plugin.json version sync" step that logged
   "version already in sync: 0.8.9"). This belongs to the
   `2026-08-14-deployed-artifact-drift-check` directive.
4. **Then pick up** either the 2026-08-25 orchestrator drone-handoff directive (newest, and the orchestrator has
   hit the underlying problem twice) or `hold/backfill-rework` (oldest parked work; size the rebase first — main
   has moved 73 commits under it).
