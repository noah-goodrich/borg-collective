## 1. Goal

Execute option 3 from the /borg-assimilate verdict: fix link-up/link-down inversion, fix
scaffold_supabase.bats failures, run /simplify, commit as two separate commits, return to
the notifyd PROJECT_PLAN.md work.

## 2. Accomplished

**scaffold_supabase.bats: 10/10 passing.**
- Root cause: I'd moved `_scaffold_preflight` and `mkdir -p "$dc_dir"` UP before the
  `--supabase` branch in `drone.zsh cmd_scaffold` so I could compute the basename for the
  default workspace. That caused `_cmd_scaffold_supabase` to fail on its internal preflight
  because `.devcontainer/` already existed.
- Fix: compute the workspace basename from the raw input string (basename via parameter
  expansion, no need to resolve to absolute path first). Preflight + mkdir moved back to
  after the supabase branch. Behavior is identical; the regression was purely ordering.

## 3. Ready to Commit

**Still not yet.** The link-up/link-down inversion is unchanged since the 0950 checkpoint.
The fix for `drone.zsh cmd_scaffold` is a clean one-commit unit but it's tangled with the
other drone.zsh change (create_2pane_window side-by-side) which belongs with the tmux work.

Uncommitted in borg-collective at this snapshot:
- `drone.zsh` — both create_2pane_window (tmux/layout work) AND cmd_scaffold ordering fix
  (workspace collision fix, now tested)
- Everything else from the 0950 checkpoint (unchanged): the full lifecycle-redesign file set,
  still with the inverted names.

## 4. Blockers

1. **Link-up/link-down inversion (unchanged from 0950 checkpoint).** Still the primary
   blocker. All hook files, skill dirs, settings.json entries, help text, docs, and tests
   reference the inverted names.
2. **/simplify not yet run** on any of the touched files.

## 5. Next Session

Do the inversion swap end-to-end per the 0950 checkpoint's Next Session plan. Exactly that
plan is still correct — none of it has been done yet. Summary:

1. **File renames** (two-step with `.swap` intermediate):
   - `git mv hooks/borg-link-up.sh hooks/borg-link.swap.sh`
   - `git mv hooks/borg-link-down.sh hooks/borg-link-up.sh`
   - `git mv hooks/borg-link.swap.sh hooks/borg-link-down.sh`
   - `git mv skills/borg-link-down skills/borg-link-up`

2. **Content updates**:
   - `hooks/borg-link-down.sh` (now SessionStart): header comment = "Link down from the
     collective — download state INTO session."
   - `hooks/borg-link-up.sh` (now Stop): header = "Link up to the collective — flush state
     from session." The in-file nudge text also flips: "Run /borg-link-up next session…".
   - `skills/borg-link-up/SKILL.md`: `name: borg-link-down` → `name: borg-link-up`;
     description swapped to reflect upload direction; heading flipped too.

3. **borg.zsh `cmd_setup` hook registration swap**:
   - The hook registered for `SessionStart` should be `borg-link-down.sh` (downloads).
   - The hook registered for `Stop` should be `borg-link-up.sh` (uploads).
   - Apply to both Claude and CoCo blocks (4 calls total).
   - Leave the migration-cleanup calls (they reference the old `borg-start.sh`/`borg-stop.sh`
     names for rm/unregister) intact — those do what they need to do.

4. **Live env**:
   - `~/.claude/settings.json`: SessionStart → borg-link-down.sh; Stop → borg-link-up.sh
   - `~/.snowflake/cortex/settings.json`: same swap
   - `~/.claude/hooks/`: rename the two installed hook files to match source
   - `~/.snowflake/cortex/hooks/`: same
   - `~/.claude/skills/borg-link-down` → `~/.claude/skills/borg-link-up`

5. **Canonical**: `config/claude/settings.base.json` — flip SessionStart + Stop paths.

6. **Docs and references**:
   - `README.md` hook table: `borg-link-up.sh | SessionStart` → `borg-link-down.sh | SessionStart`;
     `borg-link-down.sh | Stop` → `borg-link-up.sh | Stop`. "Session Checkpoints" section
     prose: `/borg-link-down` → `/borg-link-up` where referring to the user-invoked save.
   - `CLAUDE.md`: hook list + skills list flipped identically.
   - `install.sh`: `/borg-link-down + git commit` → `/borg-link-up + git commit`.
   - `hooks/tool-count-nudge.sh`: nudge mentions `/borg-link-down` → `/borg-link-up`.
   - `skills/borg-review/SKILL.md`: "Run `/borg-link-down`, then break." → "/borg-link-up".
   - `skills/borg-link/SKILL.md`: description mentions "written by /borg-link-down" —
     flip to /borg-link-up.
   - `lib/borg-hooks.sh`: comment "borg-link-up.sh can call it" — after swap the SessionStart
     hook is borg-link-down.sh, so update the comment accordingly.
   - `tests/cairn.bats` + `tests/lifecycle.bats`: `BORG_START=hooks/borg-link-up.sh` → should
     point at `hooks/borg-link-down.sh` (SessionStart hook); `BORG_STOP` flipped too.
   - `borg.zsh` help text (around line 2199): `/borg-link-down` in SKILLS section → `/borg-link-up`.
   - `borg.zsh` CONFIG section: "via /borg-link-down" → "via /borg-link-up".

7. **Memory**:
   - `feedback_simplify_checkpoint.md`: update `/borg-link-down` references → `/borg-link-up`.
   - `MEMORY.md`: index entry display text `link-down proactively` → `link-up proactively`.

8. **Verify**: `zsh -n borg.zsh`, `bash -n hooks/*.sh`, `jq empty` on all three settings files,
   `bats tests/` full pass.

9. **Run `/simplify`** on the post-swap changed files. Address findings.

10. **Commit as two commits** in borg-collective:
    - Commit A (workspace collision + tmux defaults): `drone.zsh`,
      `templates/supabase/devcontainer.json`. Message something like
      "feat(drone): default workspace to /workspaces/<project>; side-by-side 2-pane window".
    - Commit B (lifecycle redesign): everything else in the current uncommitted set.
      Message something like "feat(lifecycle): kill debriefs; add /borg-link-up +
      borg-link-down.sh session-lifecycle".

11. **Separate repos** (these are independent of the two borg-collective commits):
    - `~/.config/dotfiles/`: commit `tmux/tmux.conf` + `devcontainer/docker-compose.base.yml`
      comment update.
    - `/Users/noah/dev/reveal/.devcontainer/`: commit workspace path change.
    - `/Users/noah/dev/ingle/.devcontainer/`: commit workspace path change.

12. **Return to PROJECT_PLAN.md** (Container Notification Bridge). 0/7 acceptance criteria
    met. That's the actual active plan; the session's tangent work belongs under its own
    directive(s) once shipped.

## Context for the next session

- The 0950 checkpoint is still the authoritative source for the rename plan. Only change:
  `drone.zsh cmd_scaffold` no longer has the preflight-ordering bug (10/10 scaffold tests
  pass now).
- Expect the inversion swap to take ~20-30 tool calls. Do it all in one turn — partial
  swap leaves the environment in a confused state.
- `/simplify` is a real skill in the catalog; can be invoked via the Skill tool after the
  swap. Do not run it before — the renames would just churn its findings.
- After commits land, optionally delete historical `.borg/debriefs/` dirs in each project
  (reveal, borg-collective, snowfort, ingle) + `~/.config/borg/debriefs/`. These are
  no longer written to.
