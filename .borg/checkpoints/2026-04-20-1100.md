## 1. Goal
Prep ingle and reveal for production distribution — move both under the new `stillpoint-labs` GitHub org, wire Supabase Cloud projects + credentials end-to-end into their devcontainers, and unblock the Cowork path for ingle's MCP server.

## 2. Accomplished
- **Org migration:** `stillpoint-labs` org created; `wayfinder-services/wayfinderai-waypoint` → `stillpoint-labs/ingle` (transferred + renamed); `noah-goodrich/reveal` → `stillpoint-labs/reveal`; both local git remotes updated; org confirmed on Pro plan via screenshot.
- **Ingle plugin build pipeline hardened** (even though we ultimately parked the plugin-bundled path): fixed YAML frontmatter in `commands/grocery.md`; tightened `build.sh` exclusions (190 MB → 4.8 MB artifact); added `npm prune --omit=dev` before packaging with restore after; eight iterative version bumps 0.2.0 → 0.3.1.
- **Cowork/Desktop upload dead-end diagnosed:** `.plugin` format is not accepted by Claude Desktop when it bundles an MCP server + `node_modules/`. Desktop silently rejects. Other skills-only plugins (token-cost, noah-writing-voice) upload fine.
- **Strategic pivot:** `ingle/PROJECT_PLAN.md` now records the hosted-connector direction (Pattern 1, per `2026-04-17-hosted-connector-architecture.md`) as primary. Plugin-bundled path parked.
- **Supabase Cloud provisioning under `stillpoint-labs`:**
  - `reveal` project created via `supabase projects create` (ref `jmgjznimsrlcgyevefqm`); auto-generated DB password stored in Keychain.
  - `ingle` project deleted + re-created (ref `muyxsjvgybpyaapccmaz`) so the DB password could enter Keychain through the same automated flow.
  - Both use us-west-1.
- **Secret pipeline end-to-end working:**
  - `SUPABASE_ACCESS_TOKEN`, `INGLE_SUPABASE_DB_PASSWORD`, `REVEAL_SUPABASE_DB_PASSWORD` in macOS Keychain.
  - Registry row + `_keychain_export` call for each in `~/.config/dotfiles/zsh/secrets.zsh`.
  - Devcontainer `docker-compose.yml` in both projects forwards tokens + project refs with empty fallbacks.
  - Filed a latent bug in `dev-tools:store-secret` skill (uses bash-only `read -s -p` which parses differently in zsh — unfiled but flagged).
- **Supabase CLI baked into base image:** `Dockerfile.base` (both canonical copies) fetches the latest Linux binary via GitHub releases; base image rebuilt; ingle + reveal project images rebuilt via `drone rebuild`; verified `supabase` v2.90.0 in both containers, both authenticate, both can list projects.
- **Ingle linked locally** via `supabase link` from inside the container; `supabase/config.toml` bumped `major_version 15 → 17` to match cloud.
- **5 commits across 4 repos, all pushed:** `dotfiles c5fdb62`, `borg-collective af5ee5d`, `ingle a0b7b36` (plugin fixes) + `93189da` (cloud wiring), `reveal 9d509e6` (cloud wiring on `supabase-migration` branch).
- **Notification system bug diagnosed** (last action, not yet fixed): container-based Claude sessions can't fire macOS notifications because `terminal-notifier` is host-only; container `notify.sh` runs, fails silently at the binary lookup, no popup reaches you.

## 3. Ready to Commit
Nothing in scope — all this session's infra work is committed and pushed. Pre-existing noise unchanged from the 0945 checkpoint.

Unreviewed code-change callouts:
- `~/.config/dotfiles/devcontainer/Dockerfile.base` — new `supabase` CLI install block (~8 lines). Consider `/simplify` for portability review.
- `ingle/build.sh` — substantial exclusion list + prune/restore cycle. Consider `/simplify` to check for obviously-redundant patterns.
- `reveal 9d509e6` — accidentally swept in `scripts/gen_exp13_review.py` that was still staged from an earlier attempt; you had planned to commit that from inside the reveal container for pre-commit lint. Non-critical; `git revert 9d509e6 --no-edit` unwinds if you care.

## 4. Blockers
- **Container notification blackhole.** `notify.sh` runs in container → `terminal-notifier` not found → `|| true` swallows → no macOS popup, no tmux bell. Means ingle/reveal sessions can be waiting-on-input without surfacing it. Host-only sessions (this orchestrator) still notify fine.
- **Registry state showing `status: idle` + `last: null` + a set `waiting_reason` on ingle and reveal** — this state shouldn't be reachable through normal hook paths. Either stale from earlier manual-state, or `borg-notify.sh` is partially failing inside the container. Worth a deeper trace.
- **Missing tax ID** on Supabase billing for `stillpoint-labs LLC` — non-blocking for dev, will bite at invoicing.
- **Docker IPv6** — Supabase's free-tier direct-DB endpoint is IPv6-only; Docker Desktop containers don't get IPv6. Doesn't block the CLI (uses HTTPS API), but any raw `psql` to cloud DB from inside a container needs either the Supavisor pooler (IPv4) or Docker Desktop IPv6 config.

## 5. Next Session

**If fixing notifications first (recommended — affects the parallel-work experience):** file a new directive in `borg-collective/docs/plans/directives/` — something like `2026-04-20-container-notification-bridge.md`. Architecture: host-side watcher on `~/.config/borg/registry.json` using `fswatch` wrapped in a launchd agent (or a background zsh loop launched by `borg init`). When it sees any project's status transition to `waiting`, fire `terminal-notifier` on the host. Keep `borg-notify.sh` in the container (registry update is portable). Make `notify.sh` a no-op when `/.dockerenv` is present — host-native calls only from the real host shell.

**Else, pick one project:**
- **Ingle:** start Phase 2 of `ingle/docs/plans/directives/2026-04-17-hosted-connector-architecture.md`. Concrete first commit: add `mcp-server/src/http.ts` with Hono + `StreamableHTTPServerTransport`; expose on port 3001; add `npm run dev:http` to `mcp-server/package.json`; preserve stdio path. Acceptance criterion from the directive: `curl http://localhost:3001/mcp` speaks MCP protocol and existing stdio `dev` still runs under Claude Code.
- **Reveal:** start Session 0 per `reveal/PROJECT_PLAN.md`. Rename sweep — the 24 files still referencing `wallpaper-kit`/`wallpaper_kit` (see the PROJECT_PLAN's "Lingering rename remnants" subsection for the exact list). Local dev against the `reveal-postgres` Docker service; cloud Supabase is deploy-time only. Currently on `supabase-migration` branch, 22+ commits ahead of `main`.

**Opportunistic cleanup any time:** delete the orphan `grocery-assistant` plugin state at `~/Library/Application Support/Claude/local-agent-mode-sessions/*/cowork_plugins/marketplaces/local-desktop-app-uploads/grocery-assistant/` — it's the old name, no longer relevant, was only there because of the now-parked plugin-bundled distribution path.
