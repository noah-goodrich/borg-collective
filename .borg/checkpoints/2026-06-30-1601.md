# Session Checkpoint — 2026-06-30 16:01

## 1. Goal

Recover a `/clear`-lost queued task, scope a work-machine harness sync, and build cross-session messaging so
multiple live Claude/tmux sessions can talk to each other — then ship it.

## 2. Accomplished

- **Shipped `borg vinculum`** — a file-based durable cross-session pubsub bus (the headline deliverable):
  - **PR #60 (merged, squash `b33848a`):** `pub/sub/unsub/ls/pull` verbs in `borg.zsh` + per-pane watcher
    `bin/borg-vinculum-watch`. The append-only per-channel log
    (`$XDG_DATA_HOME/borg/vinculum/<channel>/log.jsonl`) + per-subscriber cursor files ARE the broker — no
    central process to crash; N subscribers/channel, uncapped. Live delivery via `fswatch` → `tmux send-keys`
    with a self-echo filter + 30-msg/60s rate cap. `pub` JSON-encodes via `jq` (replaced a hand-rolled escape
    that mangled quotes/backslashes — caught by the /simplify pass).
  - **PR #61 (merged, squash `8659936`):** fix — `install.sh` now symlinks `borg-vinculum-watch` into
    `$BIN_DIR`. `sub` spawns the watcher by bare name; without the symlink, live delivery silently failed on
    any real install.
  - **Verified end-to-end via the real PATH path:** bats 16/16; live 3-pane `send-keys` fan-out with correct
    self-echo filtering; `fswatch` auto-delivery ~0.9s.
- **Merged cairn presence-blackboard backend** — PR #12 (squash `3102c10`): migration 004 + REST
  `/presence/{open,close,related}` + MCP `presence_related`. (Backend only — see Blockers.)
- **Finished Lever 1 caching-economics research synthesis** (claude-plugins) under corrected pricing —
  `docs/research/2026-06-29-token-cost-optimization/caching-economics-synthesis.md`. Headline: cache
  reads+creation ≈ 88% of main-loop cost; tool-output truncation + `/clear` are the structural levers.
- **Recovered the `/clear`-lost "#1"** from the prior transcript: it was the agent cost-optimization program;
  Noah's own prior call was to build Lever 1's truncation hook INLINE, not via subagents.
- Captured decisions in auto-memory: `project_borg_vinculum.md` (name "vinculum" is FINAL — not "link",
  which collides with `borg-link`).

## 3. Ready to Commit

Nothing pending from this session — all vinculum work is merged; `main` is in sync with `origin/main`.
`/simplify` already ran on the vinculum code (its findings produced the `jq` fix; cursor-dedup deferred).

The 5 untracked files in `borg-collective` (`.borg/checkpoints/2026-06-16-0446.md`,
`docs/brainstorms/2026-06-12-staged-deployment-standard.md`, `docs/research/sources/`,
`docs/research/staged-deploy-primitives.md`, `docs/research/verification-report.md`) are PRE-EXISTING
artifacts deliberately kept out of the vinculum PR — not this session's work. Leave or triage separately.

## 4. Blockers

- **cairn presence is merged but inert:** the live `cairn-api` container (`127.0.0.1:8767`) was built
  06-17/06-26, predating PR #12 — so `/presence/*` returns 404. Needs a `cairn-api` rebuild from `main` +
  migration 004 applied before presence works.
- **Presence has no consumer:** `hooks/borg-link-down.sh` / `borg-link-up.sh` contain ZERO presence wiring —
  the ambient SessionStart-inject / Stop-close half was never built.
- **Work-machine sync is blocked on one push:** one unpushed `dotfiles` commit on this machine must go up
  first (`cd ~/.config/dotfiles && git push`).

## 5. Next Session

Pick one; all are independent:

1. **Activate cairn presence (highest leverage):** rebuild `cairn-api` from `main` + apply migration 004
   (confirm `curl 127.0.0.1:8767/presence/related?...` → 200, not 404), THEN wire the ambient consumer into
   `hooks/borg-link-down.sh` (POST `/presence/open` + GET `/related` → inject one line) and
   `hooks/borg-link-up.sh` (POST `/presence/close`) — fail-open with `--max-time 2`, never block SessionStart.
2. **vinculum follow-ups (small):** (a) extract `_read_cursor`/`_write_cursor` helpers in `borg.zsh` (cursor
   logic is inlined 4×; the watcher already encapsulates them — drift risk); (b) cosmetic — `borg-vinculum-watch`
   line ~160 logs "once" even in persistent mode because `${_once_mode:+ once}` treats the string "0" as set;
   guard with `(( _once_mode ))`; (c) launchd auto-start so watchers survive reboot (Spawn 3); (d) cairn-sink
   consumer tier (drain channels → cairn REST).
3. **Lever 1 truncation hook (claude-plugins):** build it INLINE per Noah's prior call (research synthesis is
   done; this is the hook implementation + cache-delta measurement).
4. **Work-machine sync:** push the dotfiles commit, then run the sync runbook (dotfiles → borg-collective
   `install.sh` + `borg setup` → claude-plugins → plugin installs → cairn). `cairn` `main` now carries
   presence, so a synced work machine gets the backend automatically.
