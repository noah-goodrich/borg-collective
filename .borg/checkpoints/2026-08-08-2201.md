## 1. Goal
Triage borg-collective's open PRs/issues, ship what's ready, and reconcile the backlog against the cairn
decommission.

## 2. Accomplished
- **Fixed `borg sever` leaving Supabase up** — added `_borg_stop_shared_supabase` to `borg.zsh` (`cmd_down`),
  idempotent + fail-open; shipped as **PR #110, merged to main** (`a9371c8`).
- **Assimilated docs-sync plan** — merged **PR #109**, archived `PROJECT_PLAN.md` →
  `docs/plans/assimilated/2026-07-30-docs-sync-v0.8.x.md` (7/7 criteria, C4 path corrected), committed the 3
  straggler checkpoints.
- **Backlog triage:** **closed #103** (obsolete under cairn decommission), **de-scoped #99** (dropped
  cairn-down escalation, kept clock-skew check).
- **Discovered the cairn cleanup was already done** by the concurrent `~/dev/cairn` session (`df8cf91`,
  `3ea90cb`, `17ac49c`) — correctly declined to write a redundant `PROJECT_PLAN.md`.
- **Reaped cairn infra:** removed both cairn containers; reclaimed **~11.3GB** of dead cairn images (GHCR
  0.5.2–0.5.5 + generic compose images). Held the `feat-cli-http-transport-*` pair (5.44GB, live branch).

## 3. Ready to Commit
Nothing — working tree is clean on `main`, 0 ahead / 0 behind origin. All git work this session already
landed via PRs #109/#110 (the cairn commits were the other session's). `/simplify` was run on the one
code change (`borg.zsh`); no outstanding files to review. The container/image reaps were infra, not git.

## 4. Blockers
No blockers.

## 5. Next Session
1. **PR #106 (infoviz docs)** — held by choice; merge when ready: `gh pr merge 106 --squash --delete-branch`.
2. **PR #104 (PR-control-hub renderer)** — still a **draft**; `gh pr ready 104`, review (recommend
   `/borg-verify`), then merge — that closes **#97**. Confirm its renderer has no cairn dependency first.
3. **Decommission directive forward work** (`docs/plans/directives/2026-08-08-cairn-decommission-and-unconditional-block.md`):
   the genuinely-open items are **Phase 1.6** (one unattended verdict gate — the load-bearing step) and
   **Phase 3** (hookify lifecycle layer + hard session-length mechanism). Not teardown.
4. **Optional:** reap the two held `feat-cli-http-transport-*` cairn images (5.44GB) once that branch is done.
5. **#98** checkpoint-freshness clock-skew bug + **#99** clock-skew check cluster naturally into one session.
