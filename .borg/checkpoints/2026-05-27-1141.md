## 1. Goal

Fix three things: drone up Claude pane position (left not right), borg-link auto-detect current project, and capacity warning in adhd-guardrails.

## 2. Accomplished

- **`drone.zsh` — Claude pane on left:**
  - `create_2pane_window`: swapped cmd args at both `drone up` call sites so left pane runs `claude`, right is shell; updated focus to `$main`
  - New `get_left_pane()` helper — sorts by `pane_left` ascending, reliable for horizontal splits
  - `cmd_claude`, `cmd_cortex`, `cmd_feature`: switched from `get_bottom_pane` to `get_left_pane`

- **`skills/borg-link/SKILL.md` — auto-detect current project:**
  - No-arg + `.borg-project` marker found → deep dive on current project
  - No-arg + no marker → overview of everything
  - Updated Modes and When-to-use sections; synced to `~/.claude/skills/borg-link/SKILL.md`

- **`hooks/borg-link-down.sh` + `skills/adhd-guardrails/SKILL.md` — capacity warning:**
  - Hook now counts active+waiting projects at SessionStart; injects ⚠ CAPACITY WARNING when count > BORG_MAX_ACTIVE
  - adhd-guardrails gets a new Capacity Management section: surface warning first, block new work threads at capacity
  - Both deployed live to ~/.claude/

- **Source-of-truth decision resolved:** Found original Dispatch session (f9ef8d07) confirming
  borg-collective is canonical, claude-plugins distributes the publishable subset. Updated
  handoff doc in borg-collective and fixed the backwards directive in claude-plugins.

- **PRs merged:** #25 (borg-state), #26 (drone+borg-link fixes), #27 open (capacity warning)

## 3. Ready to Commit

PR #27 (fix/capacity-warning-hook-and-skill) is open and ready to merge — no further changes needed.

Everything else is already merged to main. Nothing uncommitted on the current branch.

## 4. Blockers

No blockers.

Remaining deferred work (not blocking, routing defined):
- chore/project-state-2026-05-27 — .claude/, docs/research/, templates/supabase/.borg/
- hooks/borg-plan-promote.sh + tests/plan_promote.bats — own PR
- PROJECT_PLAN.md — archive to docs/plans/assimilated/2026-05-04-skill-extension-protocol.md

## 5. Next Session

1. Merge PR #27 (fix/capacity-warning-hook-and-skill) — clean, no conflicts.
2. Archive PROJECT_PLAN.md:
     git mv PROJECT_PLAN.md docs/plans/assimilated/2026-05-04-skill-extension-protocol.md
     # add "Shipped: 2026-05-27" at top, commit
3. Route deferred untracked files per docs/plans/handoff/2026-05-27-research-branch-split.md:
   cut chore/project-state-2026-05-27 off main, pop stash @{0} from
   research/agent-teams-2026-05-23, review diffs, PR.
4. ~/dev audit directive (2026-05-11-dev-dir-final-audit.md) — nothing above is blocking it;
   good candidate if time allows.
