## 1. Goal

Talk through workflow architecture for managing long-running sessions, then capture the resulting
designs as durable directives so the thinking survives compaction.

## 2. Accomplished

- **Architectural conversation** spanning three workstreams: host-first Claude with container
  delegation, directive-orphan prevention, and Cortex session-cap auto-wake.
- **Read reveal session logs via Explore agent** to ground the orphan diagnosis — confirmed the MVP
  plan was correctly assimilated 2026-05-01, but two directives spawned during MVP execution
  (`2026-04-25-attempt-persistence-and-detail.md`, `2026-05-01-user-submission-context-pipeline.md`)
  got orphaned because nothing back-linked them to the parent plan or surfaced them at session
  start.
- **Captured snowfort cortex pane scrollback directly** via `tmux capture-pane -p -t 2.1 -S -8000`
  after the Explore agent hit JSONL file-size limits in read-only mode. Confirmed the
  cap-and-resume pattern: cap message → session pauses → manual `> wake up!` → resumes mid-task with
  no state loss. Three cap-hits visible across ~8000 lines.
- **Created three directives** in `docs/plans/directives/`:
  - `2026-05-01-host-first-claude-delegation.md` — move Claude to host, container becomes dispatch
    target via `drone exec`.
  - `2026-05-01-directive-orphan-prevention.md` — `Parent plan:` back-links + `/borg-assimilate`
    child-check + auto-surface directives in `borg-link-down` + post-commit nudge in `borg-link-up`.
  - `2026-05-01-cortex-auto-wake.md` — daemon detects cap message, schedules wake, auto-sends
    `wake up!` via tmux at reset time. Cortex scrollback evidence embedded inline.
- **Reconciled metadata convention** in orphan-prevention directive: replaced YAML-frontmatter
  design with italic-line metadata (`*Parent plan: <slug>*`) to match existing reveal directive
  style.
- **Added back-links to the two reveal orphans** at `~/dev/reveal/docs/plans/directives/`. Both now
  carry `*Parent plan: 2026-04-14-reveal-mvp-supabase-flyio (orphaned at MVP assimilation
  2026-05-01; back-link added 2026-05-01).*`
- **Diagnosed orphan state:** neither shipped. `user-submission-context-pipeline` is actively
  misleading users (form live in production collecting `user_vision`/`user_intentions`/`user_notes`,
  pipeline ignores all three). `attempt-persistence-and-detail` is inert infrastructure (schema
  empty, worker doesn't write).
- **Recommendation captured:** ship both, `user-submission-context` first (urgency: dishonest UX),
  then `attempt-persistence`. Don't sever either.

## 3. Ready to Commit

**borg-collective (this repo):**
- `docs/plans/directives/2026-05-01-host-first-claude-delegation.md` (new)
- `docs/plans/directives/2026-05-01-directive-orphan-prevention.md` (new)
- `docs/plans/directives/2026-05-01-cortex-auto-wake.md` (new)

Plus the existing sea of untracked `.borg/checkpoints/*.md` files that have been accumulating across
sessions and `.claude/` — those are not from this session, leave them or address separately.

**reveal (separate repo at `~/dev/reveal`):**
- `docs/plans/directives/2026-04-25-attempt-persistence-and-detail.md` (modified — `*Parent plan:*`
  line added)
- `docs/plans/directives/2026-05-01-user-submission-context-pipeline.md` (modified — `*Parent
  plan:*` line added)

`/simplify` not run this session because the work was directive-writing (markdown prose), not code —
`/simplify` is largely N/A here. Recommend re-skimming each directive's Acceptance Criteria and
Scope Boundaries sections before committing, since those are the load-bearing parts. Suggested
commit grouping: borg-collective trio as one commit (`docs(plans): capture host-first delegation +
orphan prevention + cortex auto-wake`), reveal back-links as one commit in the reveal repo
(`docs(plans): back-link orphaned MVP directives`).

## 4. Blockers

No blockers.

## 5. Next Session

**If staying in borg-collective:** ship `host-first-claude-delegation` first. It has the biggest
blast radius (changes how every project's Claude session starts), so doing it before
`orphan-prevention` (which adds hook logic to `borg-link-down`/`borg-link-up`) avoids those new
hooks fighting in-container Claude assumptions during the transition. Concrete first step: read
`drone.zsh` `cmd_up` and sketch the Claude-pane spawn change — host `claude` with
`cwd=<project_dir>` instead of `drone sh <project>` → `claude`. Entry point is acceptance criteria
#1 (`drone exec`) and #3 (host Claude pane) in
`docs/plans/directives/2026-05-01-host-first-claude-delegation.md`.

**Faster-payoff alternative:** ship `cortex-auto-wake` — smallest scope (~30 lines of zsh + a
launchd plist), ends a daily friction point. Evidence is already embedded in the directive. Entry
point is the detector component in acceptance criterion #1.

**If switching to reveal:** ship `user-submission-context-pipeline` first. Open
`~/dev/reveal/docs/plans/directives/2026-05-01-user-submission-context-pipeline.md` and start at
Layer 1 — add `_format_user_submission_context` next to `_format_photographer_notes` at
`reveal/prompts.py:765`. The directive specifies file paths and line numbers throughout; follow the
implementation order section. It's a single-PR vertical slice; ship all four layers together.
