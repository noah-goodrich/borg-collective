**tl;dr** — Cleared the previous session's uncommitted S1 + adapter work and executed S2, so the one-front-door chain now
sits at six green commits on a branch nobody has pushed yet. Next session starts at S3, the sweep fold — and the branch
needs a push-or-PR decision first.

## 1. Goal

Commit the S1 and adapter-rewrite work left uncommitted at the end of 2026-08-25-1117, then execute S2 of
`PROJECT_PLAN.md` ("One Front Door: `borg link` as the Derived-Fact Surface") — the manifest reader.

## 2. Accomplished

**Six commits on `feat/link-one-front-door-s1-s2`** (branched rather than committing to `main`, since this repository
uses PRs and it keeps S2 reversible):

| commit | what |
|---|---|
| `b984616` | `PROJECT_PLAN.md`, the hardened spec, 2 research bundles, 18 backlogged checkpoints |
| `eb5ffec` | **S1** — env bridge, `core.scope_for()`, `--local` opt-down (blockers B1/B2/B3) |
| `d2152a0` | **adapter** — batched GraphQL, 12.6s → 1.93s (B5, plus the 30-item recency change) |
| `6920a4d` | `borg_core/proc.py` — one subprocess runner, collapsing the third copy of run/capture/degrade |
| `e57a80f` | **S2** — `borg_core/manifest/`, the reader the sweep folds into |
| `1c54389` | **fix** — `recon-adapter-github` swept no linked worktrees |

**S2 itself** (`borg_core/manifest/`, 5 files, no `cli.py` — AC1 adds no verbs). Ported the read side of
`merge-tree/programs.py` rather than importing it (hyphenated directory, plus `pyproject.toml:76`'s
`allowed_prefixes`). Five new pure functions: `declared_refs`, `parse_ref` (normalizes NOTHING —
`recon/core.py:186` keys dedup on the raw string), `levels` (Kahn longest-path with rank as the ROW index, the
deliberate transpose of `render_graph.py:659`), `ready_set`, `select_for_repository`. Row-level `after: [refs]` landed
with it for AC4's forks, and it SUPPRESSES the inferred lane edge into that row — otherwise the fork renders as a
linear chain. `shell.discover_registered` carries B6: discovery is global across every registered repository,
selection is scoped.

**Gates, all re-run by the orchestrator rather than trusted from the subagent report**: `make test` 658 passed / 98%
coverage (floor 90, manifest at 100%); `make lint` ruff + mypy clean, pylint 10.00/10; `bats tests/` 721 passed
(baseline 720 plus the new worktree case).

**Two bugs found incidentally, neither one anybody was looking for:**

- **`recon-adapter-github:77` tested `[ -d "$PPATH/.git" ]`.** A linked worktree's `.git` is a FILE, so every
  `drone feature` checkout registered with `borg add` has been contributing zero items to every sweep.
  `reveal-data-consistency` is one in the live registry. Surfaced because a reviewer attacked the identical rule in
  S2's `repository_slug`. Both sides now use `-e` / `os.path.exists` and each carries a comment naming the other.
- **`scripts/build-plugin.sh --dry-run` reports the 5 lib-inlining hooks as changed unconditionally** — line 238 skips
  `_embedded_lib` in dry-run, then diffs a lib-less candidate against a lib-full destination. The real build reports
  all 12 unchanged. This false positive is recorded as fact in
  `docs/plans/directives/2026-08-08-cairn-decommission-and-unconditional-block.md:218-220` ("the dry-run currently
  shows 5 hooks would change, i.e. there is pre-existing unrelated drift to review before shipping"). There was no
  drift. Saved to auto-memory as `reference_build_plugin_dryrun_false_positive`. **Not yet fixed** — one-line change.

**`claude-plugins` rebuilt, committed and pushed** (`dd18b84`, `noah-goodrich/claude-plugins`). Only real delta was the
`borg-link` skill learning the `scope` key; all 12 hooks, 17 skills and both agents were already in sync.

**Method note:** S2 ran as a 10-agent workflow — 3 recon, 1 implement, 4 blind adversarial lenses, 1 fix, 1 independent
verify. Two lenses returned `unsound`; 2 blockers and 11 majors were fixed and one finding was rejected with a
reproduction showing the proposed test asserted false behavior. The verify agent mutated `discover_registered` to be
repository-scoped and confirmed the B6 test goes red — the first attempt passed for the wrong reason (the host
repository sorted first), so the test was rewritten to put it second of four.

## 3. Ready to Commit

**Nothing.** The working tree is clean except `docs/research/.gate-armed`, an untracked transient research-gate marker
that is deliberately not tracked.

`/simplify` was not run as a standalone pass, but its equivalent ran inside the S2 workflow as the `simplify-quality`
review lens over `borg_core/manifest/`, and every finding it raised was applied in the fix phase — that lens is what
produced `borg_core/proc.py` and the deletion of the dead `edges_from`. The three pre-S2 commits (`b984616`,
`eb5ffec`, `d2152a0`) were committed without a simplify pass; their code was written and reviewed in the prior session.

## 4. Blockers

No blockers. Two open decisions, neither one an obstacle:

- **The branch is unpushed by choice.** Six green commits sit on `feat/link-one-front-door-s1-s2`. The orchestrator
  committed to holding the borg-collective push as a separate authorization from the `claude-plugins` one, so it is
  waiting on a push-or-PR call rather than on any technical problem.
- **The memory-gate re-aim workflow is still running** (`wf_419b8003-611`). It writes only to the session scratchpad,
  never to the repository, so it blocks nothing here.

## 5. Next Session

**Start at S3 — the adapter rewrite's sweep fold** (§4 of
`docs/plans/directives/2026-08-25-link-front-door-hardened-spec.md`). It consumes what S1 and S2 built and carries
blockers B1, B2, B6 and B9. This is the first commit in which `borg link` acquires network cost, which is exactly why
S1 front-loaded `--local` at `borg.zsh:266`, `borg.zsh:2222` and `drone.zsh:964` — verify those are still in place
before adding the sweep, and audit for any `borg link` call site inside a loop that the earlier pass missed.

Wire `borg_core/manifest/shell.discover_registered` into `borg_core/link/cli.py:_document`, which already computes
`scope` but does not yet read a manifest. Per B4 the targeted fetch must be a single `subprocess.Popen` with a
monotonic deadline — **no `ThreadPoolExecutor`**, whose non-daemon workers are joined by an interpreter atexit hook and
were measured holding the process open for 12s after output completed. Per B5 always parse stdout and merge `data`
even when `gh` exits non-zero. Per B7 define the `BORG_LINK_SWEEP_FIXTURE` / `BORG_LINK_FETCH_FIXTURE` stub seam
BEFORE writing the renderer, and neutralize `BORG_RECON_ADAPTER_PATH` in `tests/test_helper/setup.bash` in the same
commit or all 46 existing link tests start shelling out to `gh auth status`.

Two loose ends worth a few minutes each: fix `scripts/build-plugin.sh:238` so `--dry-run` composes the same bytes it
would write, and strike the false-drift note at
`docs/plans/directives/2026-08-08-cairn-decommission-and-unconditional-block.md:218-220`.

The memory-gate re-aim deliverables will be in the scratchpad at `memory-gate-prereg.md`,
`2026-08-25-memory-gate-reaim.md` and `memory-gate-summary.md` — read the summary first; the directive still needs to
be moved into `docs/plans/directives/` and its three §1.6 judgment calls confirmed by Noah before anything is wired.

Vocabulary stays ratified: *repository* / *project*. "Program" is retired in new code — S2 honored this
(`programs_dir` became `manifest_dir`), though the on-disk `.borg/programs/` path and the manifests' `program` key
still use it. That is AC7's problem, not S3's.
