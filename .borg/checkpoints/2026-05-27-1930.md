## 1. Goal

Close three research-identified gaps in borg: auto-enrich cairn session records with structured
content at SessionStop, add a nanoprobe evidence gate to `SubagentStop`, and add `borg watch` as a
live-refresh display of project status and nanoprobes.

## 2. Accomplished

**Evidence gate — `hooks/borg-nanoprobe-log.sh`** (74L → 134L)
- Added `_score_evidence()`: scores `last_assistant_message` 0–3 for file-path citations and git
  diff presence
- Appends `evidence_found: bool` + `evidence_score: 0–3` to every agents.jsonl record
- Prints a one-line stderr warning when `evidence_found` is false and a message was present
- /simplify fix: removed an `|| true` from the jq build that could have shadowed errors

**Cairn enrichment — `hooks/borg-link-up.sh`** (149L → 174L)
- Parses `transcript_path` from hook input (was previously ignored)
- Builds structured `--notes` for `cairn record session`: `## Recent commits` (git log --oneline
  -3) + `## Last assistant message` (last 20KB of transcript, jq-extracted, capped at 800 chars)
- Guards all access: empty path, missing file, unreadable file — all fall through silently
- /simplify fix: dropped the fragile grep+sed first-pass (stopped at first quote, masking jq
  fallback); now single jq path handles all cases correctly

**`borg watch` — `borg.zsh`** (+42 lines)
- `cmd_watch [interval]`: `tput clear` loop (default 5s) showing `cmd_link` output + last 5
  nanoprobes from agents.jsonl with ⚠/✓ evidence badge
- Wired into dispatch (`watch`) and `borg help`

**Tests**
- `tests/nanoprobe.bats` (new, 11 tests): evidence scoring 0–3, warning/no-warning cases,
  multi-run accumulation, required JSONL fields
- `tests/cairn.bats` (+4 tests): `--notes` passed when transcript present, exits 0 on
  empty/missing/nonexistent transcript path
- /simplify fix: `! grep -q` negative assertion replaces `grep -qv ... || true` dead assertion

**212/212 bats tests passing** (up from 197).

## 3. Ready to Commit

All changed. /simplify was run — two issues found and fixed. Ready to commit:

```
borg.zsh
hooks/borg-link-up.sh
hooks/borg-nanoprobe-log.sh
tests/cairn.bats
tests/nanoprobe.bats    ← untracked, needs git add
PROJECT_PLAN.md         ← untracked, needs git add
```

Suggested commit:
```bash
git add PROJECT_PLAN.md tests/nanoprobe.bats
git commit -m "feat(hooks): cairn enrichment, nanoprobe evidence gate, borg watch"
```

## 4. Blockers

No blockers.

## 5. Next Session

Three research gaps remain open with no directives yet — all from
`docs/research/borg-next-level-review.md`:

1. **`/borg-spec` skill** (Gap 6) — 4-question interview → `SPEC.md` → feeds `borg-plan`. New
   skill file at `skills/borg-spec/SKILL.md`. Update `borg-plan` to read `SPEC.md` if present.
   ~45 min.

2. **`borg mcp serve`** (positioning addendum Move 3b) — expose registry + spawn + checkpoint
   reads as MCP tools. Bigger bet; plan a separate session.

3. **Skills marketplace submission** (positioning Move 1) — `adhd-guardrails`, `borg-plan`,
   `borg-assimilate` to Anthropic marketplace. Process: `claude plugin publish`.

Also: the **Ingle shopping smoke test** is still pending — unblocks Item Canonicalization work.
