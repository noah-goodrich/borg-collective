## 1. Goal
Complete the `/borg-assimilate` on the mechanism-layer extraction plan (PR #41) and answer the test
coverage question.

## 2. Accomplished

- **Answered coverage question** — no `kcov`/`bashcov` installed; produced a qualitative function-level
  map: ~44/64 functions exercised (~69%). Identified zero-coverage cluster: `lib/tmux.zsh` (8 fns),
  `lib/coco.zsh` (6), `lib/desktop.zsh` (3), `lib/colors.zsh` (2), `lib/borg-sync.zsh` (1). Gaps in
  `lib/borg-hooks.sh`: `_borg_sync_file`, `_borg_apply_claude_extensions`, `_borg_is_container`,
  `_borg_osa_notify`, `_borg_strip_ctl`, `_borg_resolve_proj_dir`.
- **PR #41 merged** — `feat(mechanism): plugin 80/20 split — single reaper home + plugin self-containment`
  squash-merged to main and branch deleted.
- **Plan archived** — `PROJECT_PLAN.md` removed; archived to
  `docs/plans/assimilated/2026-06-08-mechanism-layer-extraction-plugin-80-20-split.md` with all criteria
  `[x]` and ship date `2026-06-09`.

## 3. Ready to Commit

Nothing. Working tree is clean. All work is on `main`.

## 4. Blockers

No blockers.

## 5. Next Session

Two immediate threads:

1. **`claude-plugins/directives-02-06`** — 2 commits are staged but unpushed in
   `~/dev/claude-plugins/borg-collective`. Push + open PR when next in that project. Commits:
   `hooks/notify.sh` inlined `_borg_osa_notify` (fresh-install fix) + `borg-link` / `borg-next`
   SKILL.md re-synced from source.

2. **Follow-on 80/20 directives** — now that the reaper slice is proven, file 3–4 directives parented to
   `2026-06-08-mechanism-layer-extraction-plugin-80-20-split` for the remaining verbs:
   - `scan/scoring` — reaper-scoring logic duplicated in `borg.zsh`; extract to mechanism layer
   - `cairn-client` — shared adapter; both CLI and skills call cairn inconsistently today
   - `search` — skill-callable vs CLI-only; same 80/20 treatment as reap

   Start with `/borg-plan` targeting one verb at a time.

**Don't forget:** Ingle shopping smoke test still outstanding — blocks Item Canonicalization.
