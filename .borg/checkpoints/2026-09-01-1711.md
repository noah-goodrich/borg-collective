**tl;dr** — Shipped AC7's expand phase: a validating manifest writer, a typed ref vocabulary that lets chains hold
Jira and links, and two decision records. Next session: the 57-ref rewrite in `test_coordinator.py`, then the repoints.

## 1. Goal

Move AC5/AC6/AC7 of the One Front Door plan forward, starting from "what's next?" — which resolved into AC7's
merge-tree retirement, and then into a scope correction when validation turned out to be a design question.

## 2. Accomplished

**Nine commits on `feat/ac7-manifest-writer-port`, PR
[noah-goodrich/borg-collective#186](https://github.com/noah-goodrich/borg-collective/pull/186).** All four gates green:
`make lint` rc=0 · `make test` 1002 passed, 99% vs the 90 floor · `make test-viz` 344 passed, 95% vs 85 ·
`bats tests/` 774 ok, 0 not ok.

- **`34a11e2` the writer.** `borg_core/manifest/shell.write_manifest` — the package's first write path; all nine prior
  functions read. Validates, refuses whole, writes nothing. Three defects fixed rather than ported: no `program`
  backfill, every `_`-prefixed key stripped via a shared `core.declared_body` (the old one stripped only `_path` and
  persisted `_id`), and `mkstemp` over a racing fixed `.tmp`. Suggests but never repairs a shorthand ref.
- **`ad571b4` the ref vocabulary.** `refs.ref_kind` → `github` | `jira` | `link` | `""`. Tracked kinds (github, jira)
  can block; links are reference context and never do. Typed, not loosened — every pattern anchored, five new
  near-miss negatives.
- **`41b6143`** split the ref vocabulary into `borg_core/manifest/refs.py` (verified byte-identical move), deleting
  `core.py`'s one-commit-old `too-many-lines` disable rather than carrying it.
- **`d7767ee`** a fourth `▸ CHAINS` diagnosis. A repository whose only manifest failed to load rendered "no project
  manifests in the registry yet" — false, and it pointed the reader at scaffolding a second manifest.
- **`7b12dd7`** replaced `test_proc`'s `elapsed < 1.0` wall-clock assertion with sibling-visibility. Not a widened
  threshold — the threshold is gone.
- **`fff42d4` + `9d151ce`** Architecture Rule: schema evolution is always expand → migrate → contract, plus the
  correction that you migrate only what the *surviving* validator rejects.
- **`22c41ca` + `819c029`** two directives filed: structured storage for borg-generated artifacts, and the reversal of
  the 2026-08-27 row-salvage decision.
- **`uv.lock` resolved after eight checkpoints — deleted.** CI installs via `pip install --group dev`; nothing reads
  the lock. Tracking it would have committed a second, unread source of dependency truth.

Every new test was mutation-verified. Sixteen mutations applied across the session; two initially **survived** and the
design was fixed rather than the test.

## 3. Ready to Commit

Nothing. Working tree is clean and all nine commits are pushed. `/simplify` has not been run this session — worth a
pass over `borg_core/manifest/{refs,core,shell}.py` and `borg_core/link/{grid,render,cli}.py` before the PR merges.

## 4. Blockers

No blockers. Three things to carry forward rather than blockers:

- **PR #186 is unreviewed.** Nine commits is a large single PR; it may want splitting if review stalls.
- **D3 is settled** (refuse the file, don't salvage rows) but **unimplemented** — the directive is filed, `shell.py`'s
  `_drop_invalid_rows` is still live.
- **A trap in code written this session:** `shell.refused_manifest_paths` deliberately excludes the salvage arm, so the
  obvious cleanup of `coordinator.py:398`'s `".json" in w` string sniff re-opens the row deletion. Refusal semantics
  must land first.

## 5. Next Session

Retirement order is fixed by the expand→migrate→contract rule. Expand is **done**; migrate is next.

1. **E3 first, not M1** — implement the refusal semantics from
   `docs/plans/directives/2026-09-01-refuse-the-manifest-stop-salvaging-rows.md`. Write the acceptance test *before*
   the change: a manifest with one invalid row, run the sync path, assert every row still on disk. It must be RED
   against today's tree (measured: 3 rows in, 2 written, `warnings == []`).
2. **M1 — rewrite all 57 shorthand ref occurrences in `merge-tree/test_coordinator.py`** (53 lines; distinct tokens
   `r#1`×47, `r#2`×4, `z#1`×2, `r#3`×2, `a#1`×2). **Whole-file with a uniform owner (`o/`), never targeted at
   `_row(...)`**: 41 of the 57 are join keys in recon state maps and target-report rows, so rewriting a row's ref while
   leaving its state-map key turns the join into a silent no-match that keeps assertions green. A uniform prefix also
   preserves the lexicographic sort `coordinator.py:138` depends on. Verify by running the rewritten file against the
   **unmodified** `programs.py` — 49/49 must pass.
3. **NEVER run that rewrite outside `merge-tree/test_coordinator.py`.** The 14 shorthand literals in
   `borg_core/manifest/test_core.py` and `test_shell.py` are the port's own negative guards, including the writer-
   refusal test. A blanket sed leaves every one green while asserting nothing.
4. **Then C1/C2** — repoint `coordinator.py` and `gather.py`, each in one commit, never split. A simulated repoint
   measured **12 failed / 37 passed of 49** in `test_coordinator.py`. One of those 12,
   `test_same_program_in_two_projects_writes_each_to_its_own_home`, *passes* after the repoint while testing a state
   production can no longer reach — its helper calls `discover` once per directory, `cmd_sync` calls it once with the
   whole list, and `shell.discover` dedups on content identity with zero warnings. Write a batched-discover test first.
5. **`merge-tree/test_s4_manifests.py` is a fifth importer** the retirement directive never names, and the only test
   running a validator against committed on-disk manifests.

Open decision carried forward: **AC6's `make eval --skip-model` is not a valid make invocation** (rc=2), and of its
three "deterministic" cases two need live GitHub — so "deterministic cases green in CI" currently has zero members.

No external-origin text is quoted in this checkpoint beyond the PR number.
