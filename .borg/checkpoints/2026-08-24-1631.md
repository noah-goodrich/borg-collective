**tl;dr** — PROJECT_PLAN.md is approved and read into context: `borg link` becomes the one front door
rendering derived fact, 7 acceptance criteria, no work started (weekly credits nearly out). Tomorrow
2026-08-25 starts with measuring AC3's targeted-fetch cost, because out-of-window decay is the risk most
likely to sink the plan.

## 1. Goal

Read the approved PROJECT_PLAN.md into context and checkpoint the session so tomorrow starts warm —
explicitly no implementation work.

## 2. Accomplished

- **Read `/Users/noah/dev/borg-collective/PROJECT_PLAN.md`** — "One Front Door: `borg link` as the
  Derived-Fact Surface," established 2026-08-24, 7 ACs, work begins 2026-08-25.
- **Vocabulary ratified and carried forward**: *repository* = a git repo (currently miscalled "project" in
  code); *project* = related requirements/PRs/work spanning one or more repositories (previously "program").
  **"Program" is retired** — absent from new code, output, skills, help text, and docs. The repository-side
  internal rename is deferred to a parented directive (AC7), ~500 occurrences across 57+ files plus the
  registry schema key and `.borg-project` markers.
- **Plan shape absorbed**: AC1 one context-scoped front door with no cache (~2.7s repository / ~12.5s
  orchestrator, `recon` retires as a human verb); AC2 the topological grid as the single renderer everywhere;
  AC3 targeted-fetch so declared members outside the sweep never render `unknown`; AC4 rows drive
  next/yours-vs-mine plus row-level `after: [refs]` for forks; AC5 lifecycle skills author manifests by
  default; AC6 the `make eval` harness generalized from `evals/s4-k3/run.sh` with paired positive/negative
  cases; AC7 "program" eliminated and the rename filed.
- **Scope boundaries noted**: no S1 `borg show`, no `--md`/`--html` (ANSI-to-stdout only), no internal
  rename, no infoviz Track 6 blocker (the one reading with teeth is Ghoniem et al. — matrices beat node-link
  past ~20 vertices except on path-finding; live manifests are 3 and 14 rows on a path-finding task, so an
  hour of reading, not a phase).
- Prior session's work confirmed landed and clean: borg-collective @ 638b7c4, stillpoint main @ 90c6f63.

## 3. Ready to Commit

Nothing — no files changed this session by design. `PROJECT_PLAN.md` is untracked in the working tree
(plan-of-record convention; it gets assimilated at ship, not committed now). One manual cleanup still open
for Noah: `git -C /Users/noah/dev/stillpoint push origin --delete feat/ingle-reconcile-meal-entries`
(accidental WIP-branch publish from the prior session; classifier-blocked for me).

## 4. Blockers

- **Weekly credits nearly exhausted as of 2026-08-24** — the reason work is deferred to 2026-08-25. Not a
  technical blocker.
- borg:5's code-governance revive (install-audit Gap 2) still in flight, unconfirmed.

## 5. Next Session

**Measure before building.** Start with AC3's targeted-fetch cost — the plan's own top risk is that
out-of-window decay makes repository-scoped `link` lose the 2.7s that makes it reflexive. Time a batched
`gh` fetch over the 14 declared refs in `stillpoint/.borg/programs/ingle-t1-cutover.json` (13 of 14 were
dangling on a 14-day window one day after authoring) and decide the fetch strategy against that number
*before* touching the renderer. Then AC1/AC2 (the render entry point + golden snapshots for both contexts),
with AC6's harness as its own session. Use *repository*/*project* vocabulary from the first line of code;
"program" is retired.
