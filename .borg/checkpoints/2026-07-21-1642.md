## 1. Goal
Continue closing the Usage Guardian directive: build the `>=92%` dispatch hard-stop veto hook (the last
data-independent, code-buildable Phase-2 item), default-OFF and fail-OPEN.

## 2. Accomplished
- **Assimilated the completed sweep-mechanism plan** (PR #88 work) → `docs/plans/assimilated/2026-07-21-usage-guardian-phase-2-sweep-mechanism.md`.
- **Built the dispatch hard-stop veto** (`/borg-plan` → TDD → PR #89, borg-verify PASS, merged `9b9c219`):
  `hooks/borg-dispatch-guard.sh` — a `PreToolUse` hook (matcher `Agent|Workflow`) that DENIES new
  nanoprobe/workflow dispatch (exit 2) only when armed + the latest `usage-samples.jsonl` row is a fresh `ok`
  reading at/above `BORG_USAGE_HALT_PCT`. Reads the existing samples file — no poller change, no new state.
  **Fail-OPEN** on every uncertainty (disabled, stale/missing/garbage/non-ok/non-numeric sample, missing jq, empty
  stdin, non-dispatch tool). **Default-OFF** (`BORG_USAGE_HALT_ENABLED`). Wired into `scripts/build-plugin.sh`
  (hooks.json `Agent|Workflow` entry + build-list copy), both asserted by a source-parity test.
- **18 new bats** (`tests/dispatch_guard.bats`) incl. one fail-open test per path; `usage_watch` 34/34 unchanged;
  shellcheck clean. Independent reviewer confirmed the fail-open contract is airtight.
- **Assimilated the veto-hook plan** → `docs/plans/assimilated/2026-07-21-usage-guardian-phase-2-dispatch-guard.md`;
  directive updated with a dated "Dispatch-guard result" section + progress line.

## 3. Ready to Commit
Nothing pending — working tree clean, `main` synced to `origin/main` (HEAD `194c36a`). PRs #88 and #89 merged;
both plans archived; the 1252 + 1642 checkpoints retained. `/simplify` run on both builds (no fixes needed).

## 4. Blockers
No blockers. The remaining directive item is not code — it needs a real near-cap session to run against.

## 5. Next Session
Both guardian halves (85% sweep + ≥92% dispatch-guard) are **built, merged, and shipping default-OFF**. The
directive `docs/plans/directives/2026-07-08-usage-guardian-build.md` has ONE remaining item — a single **live-cap
validation** pass (cannot be manufactured; needs a genuine near-cap session, ideally a heavy multi-agent run):
1. Arm both halves for the test: `BORG_USAGE_SWEEP_ENABLED=1` + `BORG_USAGE_HALT_ENABLED=1` (env or the
   usage-watch plist's `EnvironmentVariables`; the dispatch-guard reads env at hook-fire time).
2. Confirm end-to-end near a real cap: the sweep delivers `/borg-link-up` to active drone panes, AND a dispatch
   attempt at ≥92% is actually blocked by the hook's exit 2 (bats only proves the hook emits exit 2 — not that
   Claude Code honors it).
3. If both hold, keep them armed and let the threshold-tuning data accrue (the 85% number still rests on ONE
   near-cap episode — do not tune until 3+ exist). Then the directive can be assimilated/closed.
Entry points: `bin/borg-usage-watch` (`_run_sweep`), `hooks/borg-dispatch-guard.sh`.
