## 1. Goal

Finish the executable remainder of the cairn-decommission directive so no cairn artifacts linger in borg, and
run `/borg-plan` on the link-unification directive — two independent tracks opened in one instruction.

## 2. Accomplished

**Answered "why are you still referencing cairn?" — the cause was not the stray binary.** `~/.claude/skills/`
was never redeployed after the decommission. The deployed `borg-link/SKILL.md` still listed
`cairn search P` in its data-contract table (which is why this session opened by running it), and
`borg-collective-review/SKILL.md` still instructed `cairn convene` (which is why the Collective Review ended in
a failed call). Redeployed via `install.sh`; orphaned `borg-search` skill removed; zero cairn references remain
in `~/.claude/skills/`.

**PR #122 — cairn Phase 1 (merged, `ea2f5a0`).** Items 1.2 (checkpoint truncation → awk sections 4+5 with a
legacy byte-cap fallback, 3 regression tests), 1.3 (the missing CLAUDE.md pointer), 1.4-partial (dead cairn mock
+ stale permission), 1.5 (four platform plugins enabled), and the big one:

**Phase 1.6 had never once run.** `bin/borg-memory-gate:47` used `${0:A:h}` — a zsh expansion — in a bash
script under `set -euo pipefail`. Bash evaluated `A` as arithmetic and aborted with `A: unbound variable`
before the `||` fallback could fire. It reached that line only because `command -v memory-hits-report` always
failed (`borg setup` installs it to `~/.claude/bin`, on neither PATH). It was invisible because **every test in
`memory_gate.bats` exports `BORG_MEMORY_GATE_REPORT_BIN`, so the resolution block never executed under bats** —
the same shape as the usage-watch blindness bug, in a new component. Fixed with portable symlink-following
resolution + 2 tests that fail against the old line. `launchctl` now reports exit 0 on a real launchd fire.

**First verdict delivered: `FAIL, ratio=0.027`** against the pre-registered `< 0.2 reads/session`. By the
directive's own criterion, auto-memory is a second write-only store — the cairn replacement failing cairn's own
test, ~7x under the null. Also found and fixed the causal doc bug: CLAUDE.md said `borg setup` installs launchd
jobs; only `install.sh` does.

**PR #123 — usage-watch prune (merged, `411333b`).** `~/.claude/projects/-` was *not* dead-poller residue as the
directive assumed — `usage-watch` runs `claude -p "/usage"` every 120s with no `WorkingDirectory`, persisting a
transcript per probe (15,108 files / 60MB since Jul 12, still growing). Added age-based self-pruning, 5 tests.
Rejected the plist `WorkingDirectory` change on evidence: `/` is a *declared quarantine*
(`borg-link-down.sh:39`), so relocating would abandon a guard that already exists.

**PROJECT_PLAN.md written for the link Python-core port** (branch `docs/link-port-plan`, pushed, no PR). A
5-agent scoping workflow corrected three of my own claims — **L4's `---` bug does not reproduce** (0 occurrences,
0 of 118 directive files start with `---`), L5's baseline is 160 lines not 83, and `_borg_read_assimilated:147`
has a real `(NOm)` bug listing *oldest* plans as "Recently assimilated". Adversarial challenge raised the
estimate to 30-40h and found the governing conflict: parity cannot be proven against a target being
simultaneously redesigned. Split accordingly — port with behavior unchanged (L1/L2/L6), layout second. Blocker
findings became acceptance criteria: four config vars set without `export`, and `warn()` writing to stdout
breaking `borg link --json | jq`.

## 3. Ready to Commit

Nothing uncommitted from this session's work. PRs #122, #123, and #126 are all merged; `main` is at `28a47c3`.
`/simplify` ran and found one issue (a duplicated `find` predicate); that fix shipped as #126 after being
missed on the first pass — it was made and tested but not committed before #123 merged.

**`docs/link-port-plan` is pushed but has no PR** — `PROJECT_PLAN.md` is not yet on `main`, so the SessionStart
hook will not surface it. This is the one loose end.

Pre-existing untracked files in the tree are unrelated to this session and were deliberately left alone:
`.borg/checkpoints/*` (3), `docs/research/.gate-armed`, and `docs/research/` sources from prior work.

## 4. Blockers

- **Two host commands still blocked** by the auto-mode classifier, needing Noah's hand:
  `rm -f /Users/noah/.config/borg/plan-promote-debug.log && pipx uninstall cairn`. The 15k-file delete is no
  longer needed — the merged prune reclaims it automatically on first poll.
- **Phase 1.6's proof obligation is calendar time, not work.** The gate must fire unattended, with nobody
  remembering it. Clock started 2026-08-12. Re-check on/after **2026-08-19**.
- **CI has a flaky test**: `contract: watch dispatches into the live-refresh loop (background+kill)` polls a
  5-second window for first output. Failed once on #122, passed on re-run with no code change.

## 5. Next Session

1. **Open a PR for `docs/link-port-plan`** so `PROJECT_PLAN.md` lands on `main` where the SessionStart hook
   surfaces it. Nothing else from this session is outstanding.
2. **Start the link port at Phase 0** — the parity harness, *before* any port work. 14-18 cases in
   `tests/cli_contract.bats` against today's zsh, covering all four output modes plus the three external
   consumers: `drone.zsh:963` (greps `Status:` out of the deep dive), `drone.zsh:1405` (`drone link` execs into
   `borg link`), and `borg.zsh:689` (fzf `--preview`). These must pass **unchanged** on Python later — an edited
   assertion is not a parity proof. Full plan in `PROJECT_PLAN.md` on `docs/link-port-plan`.
3. **viz-1 is NOT a constraint on the port — corrected.** The link-unification directive warns "viz-1 should
   land first… this directive must not undo its tier," implying a shared surface. Verified against PR #125
   (`feat/viz-1-awaiting-you-tier`, open): it touches **only** `merge-tree/` — `curate.py`,
   `render_graph.py`, `test_curate.py`, and a fixture. Nothing in `borg.zsh`, `cmd_link`, or
   `_borg_link_overview`. viz-1 is infoviz-program work and never writes to `borg link`'s terminal output, so
   it cannot move the parity harness underneath Phase 0. Do not re-raise this as a sequencing risk.
4. **Optional, unfiled:** a directive on the recurring "instrument ships green, never actually runs" pattern —
   now 4 instances (usage-watch ×3, memory-gate ×1), same root cause every time: a test injects the env var that
   skips the broken path. Noah has not asked for this; do not file unprompted.
