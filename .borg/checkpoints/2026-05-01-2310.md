## 1. Goal

Orchestrate reveal, ingle, and snowfort to completion while user was away (bath), with full decision
logging. Simultaneously audit and fix all photographer-name violations across reveal, reveal-site, and
reveal-design-system per the archetype rename directive.

## 2. Accomplished

**Reveal (app.reveal.photo)**
- All 7 archetype example images generated and verified (burtynsky retry succeeded)
- `bw_conversion` constraints bug fixed — Adams now produces true B&W grayscale
- `scripts/smoke_prod.py` written (512 lines), committed, running against prod
- GalleryMonograph.tsx fix in progress: wall label "after Joel Sternfeld" → tradition-name example;
  split-logic cleanup (photographer/tradition split removed, archetypeLabel simplified)
- DB purge/backfill correctly skipped — slugs are internal identifiers, not photographer surnames
- Blocks 1–3 complete; Block 4 (TS/UI/picker/history/SubmitForm) and Block 5 pending

**Reveal-site (reveal.photo)**
- Full archetype rename DEPLOYED to Cloudflare Pages:
  - All 7 tradition names live (Zone System B&W, New Color, Quiet Light, Wilderness Sublime,
    Industrial Sublime, Decisive Moment, Cinematic Twilight)
  - "Rooted in [keyArtist]" subline on gallery cards
  - Homepage blockquote reframed to "The New Color tradition" — no photographer name
  - `/about/independence` page with canonical non-affiliation disclaimer
  - Footer link to `/about/independence` on every page
- Fixed burtynsky-painterly-epic.md caption: "in the Burtynsky tradition" → "in the Industrial
  Sublime tradition"
- Hero image task sent: no image found in public/ yet; session checking

**Reveal-design-system**
- All 6 static files updated: data.jsx (rootedIn arrays), components.jsx (archetypeTradition
  primary), HANDOFF.md (rename map + deprecated archetypeName), components-story-card.html,
  SKILL.md — all tradition names, no photographer names as primary labels

**Ingle**
- Worker deployed, DNS configured, MCP server at mcp.ingle.kitchen live
- INSTALL_CLAUDE_DESKTOP.md written
- Prod DB confirmed empty (household bootstrapped fresh today — no prior data exists)
- Directed to use family-preferences.md as the source of truth for seeding real preference data
  (NOT fake/synthetic data)
- MCP registration in Claude Desktop in progress — fixing stale JWT config

**Snowfort**
- Cortex audit completed (ran in background, ~10 min)
- 4 CRITICAL findings returned:
  1. SEC_016: REQUIRE_MFA_FOR_ALL_USERS not enabled
  2. SEC_002: BAD_ADMIN_USER has MFA disabled (checking if demo object)
  3. REL_001: PRD_BAD_DB — no replication (checking if demo object)
  4. GOV_003: Snowflake Budgets not configured
- Session analyzing which findings are real vs demo objects; will present to user for triage

**Decision log**: `/Users/noah/dev/borg-collective/.borg/decisions/2026-05-01-session.md`

## 3. Ready to Commit

**borg-collective (this repo)**:
- `.borg/decisions/2026-05-01-session.md` — new decision log
- `.borg/checkpoints/2026-05-01-2310.md` — this checkpoint
- Plus large backlog of untracked checkpoint files from prior sessions

**Per-project commits happening in their own repos** — no borg-collective code changes.

## 4. Blockers

- **Ingle missing data**: No SQL backup found anywhere. Family preferences in
  `skills/grocery-hub/references/family-preferences.md` but no historical order/purchase data
  exists. Data was never persisted. Session can seed from family-preferences.md but order history
  is genuinely absent.
- **Snowfort demo objects**: BAD_ADMIN_USER and PRD_BAD_DB appear to be demo/chaos objects from
  the snowfort-audit test fixtures — need user confirmation before treating as real findings.
- **Reveal Block 4+5**: Not yet started — GalleryMonograph fix first, then full TS/UI sweep.
- **Reveal Opus extra-usage cap**: Hit earlier, resets 6:10pm MDT — may slow Block 4/5.

## 5. Next Session

**If returning to borg-collective**: Review decisions log at
`.borg/decisions/2026-05-01-session.md`. Check reveal Block 4+5 status. Confirm ingle MCP
works end-to-end in Claude Desktop. Sit with snowfort Cortex session to triage the 4 CRITICAL
findings (real vs demo objects).

**Reveal**: Check `GalleryMonograph.tsx` fix landed, then Block 4 entry point is
`src/lib/archetypes.ts` — add/verify `rootedIn` field usage in ArchetypeCard, picker, history
views, and SubmitForm.

**Ingle**: Verify `claude mcp add ingle` works from clean shell. Confirm family preferences
seeded from `family-preferences.md`.

**Snowfort**: User should sit in the Cortex session (%40) and tell it which findings to tackle
first after it confirms which are real vs demo.
