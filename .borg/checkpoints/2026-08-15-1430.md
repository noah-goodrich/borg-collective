## 1. Goal

Answer "what are the open directives, and do we have a current PROJECT_PLAN?" — which became: verify which directives
are still real work, triage the whole backlog into agent-clearable versus owner-only, and clear the mechanical half.

## 2. Accomplished

**No PROJECT_PLAN.md** (`plan: null`) — correct post-ship state after the link port assimilated at `a9fdb45`.
**14 open directives** triaged with evidence.

**`2026-08-11-link-unification-and-layout` is NOT complete** — 3/3 adversarial skeptics refuted closure. Its 2026-08-12
revision reassigned L1/L2/L6 to the now-assimilated port plan; only **L3 (bottom-anchored layout)** and **L5
(re-measure)** remain and both have **zero work**. `borg link | tail -5` fails L3's own verify command; `wc -l` = 169
against a 160 baseline. Three findings no directive captures: **127 of the 169 lines are an uncapped directive dump**
(`render.py:215-220`, ~75% of output) making the one-screen goal arithmetically unreachable; **L6 is vacuous** and
four byte-exact goldens under `tests/fixtures/link/` will need deliberate regeneration; **L1's "mirroring the recon
contract" is false** — the two documents share exactly one top-level key.

**`2026-07-08-usage-guardian-build` reconstructed, and half of it verified live this session.** Noah ran the forced
sweep: `/borg-link-up` delivered to **5 panes, fired=5 failed=0** at 21:21:13Z, and `~/.config/borg/usage-guardian.json`
now exists. Combined with the 2026-08-13 run showing the poller correctly observing 90-100% under launchd, the
remaining unknown is narrower than it looked: **(i)** the *enabled* path under launchd's minimal env, and **(ii)**
whether Claude Code honors the guard's exit 2 — still completely unproven.

**Backlog triage (14 agents).** The headline: this is not a 14-item backlog. It is ~6 real items plus **an 8-file
argument about what borg's status screen is**, spread across four competing renderers (`borg.zsh` tables,
`borg_core/link/render.py`, `merge-tree/render_graph.py`, and the half-dead `merge-tree/render.py`). Five directives
point into that argument and three are blocked on the same nonexistent thing — nothing writes `gather.raw.json` and
Recon Contract v0 carries no `edges`, so anything built there renders empty on live data.

**The critic caught four bad calls in the triage's own plan**, the worst being an attempt to let an agent arm the
guardian machine-wide (plist template + an installer-written mutation of global `settings.json`). Vetoed. Also caught:
wrong drift-check scope (17 skills not 16, missing `agents/` and `lib/*.sh`), wrong cairn dates, and an instruction
that would have shipped a permanently-red acceptance criterion.

**Pulled main `a9fdb45` → `0d321c5`** (viz-1 #125, viz-2 #144).

**Batch 1 dispatched — 3 nanoprobes, 4 PRs, all mergeable, all five CI legs green:**

- #145 — coverage measures production code by branch, not self-graded statements (1143 stmts / 384 branches / 96%,
  8 pts headroom)
- #147 — both HIGH recon branch gaps closed, `cli.py` 81% → 90% branch; **stacked on #145**. Falsifiability proven by
  breaking each production path and confirming the test failed
- #146 — infoviz phase 2 closed, cairn reconciled, drift-check directive filed
- #148 — briefing fallback reason surfaced, field-collapse fixed (`\t` → `\x1f`), ~13-site sweep found a latent
  instance in `hooks/borg-link-down.sh`, dead `cmd_hail` deleted; 658/658 bats

**AC4 ruling recorded: defer to the consolidation.**

## 3. Ready to Commit

**Nothing local.** Working tree is clean of tracked changes, `main` is at `0d321c5`, and all three nanoprobe worktrees
were removed. Every deliverable is in the four open PRs above. `/simplify` is not applicable — no session-touched code
lives outside those PRs, and each nanoprobe ran its own suite before opening.

## 4. Blockers

**Four PRs unmerged.** Merge order is **#145 → #147** (stacked), then #146 and #148 in any order.

**Batch 2 not dispatched** — it needs #148 merged first to release the `borg.zsh` lock, and it needs its own
authorization. Contents: link-port simplify follow-ups, chained-auto-promotion (AC1/2/3/5/6 with AC4 deferred), and the
guardian's three mechanical gaps — **no arming instructions**.

**The consolidation `/borg-plan` session has not happened.** Five directives are parked behind it. It deserves a fresh
session, not the tail of a long one.

**Verification #2 still unproven** — whether Claude Code honors the dispatch guard's exit 2.

**Cairn criterion 1.6 is date-gated to 2026-08-18** (seven consecutive daily fires counted from 08-12). Two clocks are
in play: `memory-gate.log` has 2 fires; `memory-hits.log` has 6 rows on 3 distinct days.

**Capacity is still over limit** — 4-5 active against a limit of 3.

## 5. Next Session

**Merge the four PRs first**, in the order above. Board goes 14 → 12 (infoviz, coverage-gate, recon-branches closed;
briefing partial) plus one properly-filed new directive.

**Then the consolidation `/borg-plan` — start here if you only have one good session in you.** Its first question is
whether the merge-tree HTML surface survives at all. It must carry **capping the uncapped directive dump at
`render.py:215-220` as a first-class criterion** — without that, no layout change makes `borg link` fit one screen.
Pin the line-count baseline to a fixture, not the live board.

**Then dispatch batch 2**, with AC4 explicitly marked deferred and a named successor.

**Verification #2**, whenever convenient — check the sample is fresh, then in a throwaway session:

```bash
tail -1 /Users/noah/.local/state/borg/usage-samples.jsonl
BORG_USAGE_HALT_ENABLED=1 BORG_USAGE_HALT_PCT=40 claude
```

Ask that session to spawn a subagent. Expected: denial naming the threshold and reset time. If it dispatches anyway,
the guard is decorative — a real finding, and the 92% half needs a different mechanism.

**One-minute decision still outstanding:** briefing Phase 3, which #148 deliberately deferred.

**Do not re-run a defect search on the link port.** Closed by the reviewer after three rounds.

The transferable lesson, and it showed up **three separate times today**: *a check can be green exactly when the thing
it checks is broken.* The coverage gate let test files grade themselves. The recon tests supplied the value production
was supposed to derive. AC4's grep passes only when the chain is dead at runtime. That family — not ordinary bugs — is
this repo's dominant defect class, and the fix is always the same: make the test fail first against real inputs.
