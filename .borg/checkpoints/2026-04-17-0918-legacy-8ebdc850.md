Now I have enough context. Here's the debrief:

---

# Session Debrief: borg-collective
*Date: 2026-04-17 09:17*

## Objective
Spin up the `ingle` project devcontainer via `drone up ingle`, confirm the Supabase stack was healthy, and wire the Supabase Claude plugin into the borg-collective workspace config.

## Outcome

**Files created/modified:**
- `.claude/settings.json` *(created, untracked)* — enables `supabase@claude-plugins-official` plugin for this workspace
- `.claude/settings.local.json` *(created, untracked)* — local overrides (contents not inspected)
- `.gitignore` *(modified, unstaged)* — added `.borg/` to ignore list
- `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md` *(created, untracked)* — portfolio strategy doc directing both Waypoint and wallpaper-kit to pivot to Supabase + Fly.io for MVP

**Runtime result:**
- `drone up ingle` succeeded. All 13 Supabase containers for `ingle` confirmed healthy (`supabase_db_ingle`, `supabase_kong_ingle`, `supabase_studio_ingle`, etc., all up 21h). The `pre-up.sh` hook's `supabase start` was a no-op against the already-running stack; devcontainer reattached to `supabase_network_ingle`.

## Decisions Made

- **Supabase plugin over manual MCP wiring**: `supabase@claude-plugins-official` added to `.claude/settings.json` rather than configuring Supabase MCP by hand — standardizes the integration for this workspace.
- **`.borg/` excluded from git**: Runtime borg state dir added to `.gitignore` to prevent session/registry artifacts from leaking into the repo.
- **Portfolio-MVP directive filed in borg-collective**: The `2026-04-14-portfolio-mvp-pivot.md` directive lives here as cross-project coordination context (covers Waypoint + wallpaper-kit pivot to Supabase + Fly.io; zero Snowflake/Clerk in MVP; total infra ≤ $25/mo).

## Blockers and Observations

- **`ingle` is likely a renamed project** (the `cb3f166` scan-fix commit references `wallpaper-kit → reveal` as a rename that caused phantom registry entries; `ingle` may be a subsequent or parallel rename/project). The devcontainer tree has `migrations/`, `seed.sql`, and `borg-hooks/` — confirms it's a Supabase-native setup, not just scaffolded.
- **Three files are untracked and unstaged**: `.claude/`, `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md`, and the `.gitignore` change. None were committed this session.
- The `bash-guard` hook correctly pre-approved the read-only `docker ps` inspection via the intent classifier (`permissionDecision: allow, reason: read-only by intent classifier`) — confirming `94e898c` is working in production.

## Next Steps

1. **Commit the open changes**: Stage and commit `.gitignore` (`.borg/` exclusion), `.claude/settings.json`, and `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md`. Decide whether `settings.local.json` belongs in git or should be added to `.gitignore`.
2. **Verify `borg scan` no longer resurfaces `ingle` subpaths**: Run `borg scan` and confirm no phantom entries appear (e.g., `.devcontainer/borg-hooks/` or `migrations/` shouldn't register as separate projects — the `cb3f166` fix should handle this, but worth a smoke-test against `ingle`).
3. **Start active work in `ingle`**: Per the portfolio-mvp-pivot directive, the next concrete action is in `/Users/noah/dev/ingle` (or wherever the project root is) — check `PROJECT_PLAN.md` there for the current milestone.
