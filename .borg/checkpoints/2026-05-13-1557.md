## 1. Goal

Plan + ship Directive A (orchestrator vs project session separation), coordinate two parallel
data-migration arcs (ingle working-state + troth legacy CSV import) end-to-end through prod
apply + deploy, and surface architectural lessons for the per-project state migration
(Directive B) that comes next.

## 2. Accomplished

**Directive A — Orchestrator-mode session separation, shipped to main.**
- Branch `feat/orchestrator-mode-separation` (4 commits) fast-forward merged to `main` at
  commit `f1e2fb4`, plus a directive-docs commit `4f899ba`.
- `_borg_session_mode` added in `lib/borg-hooks.sh`. Exact-match `$BORG_ORCHESTRATOR_ROOT`
  (default `$HOME/dev`), trailing-slash tolerant.
- All three hooks (`borg-link-down.sh`, `borg-link-up.sh`, `borg-notify.sh`) mode-guarded.
  Orchestrator-mode `SessionStart` renders cross-project overview block (sorted by
  `last_activity`, archived skipped, status + humanized age + next-step hint), writes
  NOTHING to registry. Stop + Notification early-exit in orchestrator mode.
- `BORG_ROOT` renamed to `BORG_ORCHESTRATOR_ROOT` across 8 sites (`borg.zsh:23`,
  `drone.zsh:36/120-122/1203/1212`, `lib/registry.zsh:122`, `tests/registry.bats:134`).
  `install.sh` exports new `BORG_ROOT` as install-path semantics.
- Docs updated: `README.md`, `CLAUDE.md` (new "orchestrator-mode vs project-mode" pattern
  entry), `docs/architecture.md`, `docs/cheatsheet.md`.
- Tests: `tests/session_mode.bats` with 10 cases. `bats tests/` is 165/165 green on main.
- Sandboxed smoke confirms the core bug case: `cwd == ~/dev/borg-collective` correctly
  classifies as project mode (not orchestrator).

**Directive B stub filed** at `docs/plans/directives/2026-05-11-per-project-state-migration.md`.
Blocked on Directive A being active (i.e., on `borg setup` re-run). Carries the move-state-
out-of-registry plan, cutover Option C, and the `.gitignore` regression concern.

**Memory writes** to cross-conversation store:
- `reference_supabase_keychain_naming.md` — shared Supabase projects use the primary-app
  prefix (e.g., `REVEAL_SUPABASE_DB_PASSWORD` covers troth too).
- `feedback_prod_write_authorization.md` — each prod-write nanoprobe needs its own explicit
  auth; halted dispatches don't carry forward.

**Architectural conversation**: orchestrator session vs `borg-collective`-the-project
conflation diagnosed, vocabulary clarified (`BORG_ROOT` = install path,
`BORG_ORCHESTRATOR_ROOT` = workspace root), two-directive split (A shipped, B stubbed)
locked in.

## 3. Ready to Commit

Working tree clean. All work landed.

## 4. Blockers

- **New hooks are NOT active until `borg setup` is re-run.** The current session is still
  using the previous installed hooks at `~/.claude/hooks/`. Running `borg setup` copies the
  new ones in; next session start sees the new behavior.
- Directive B's plan can't begin until A is verified live (post-setup).

## 5. Next Session

1. **Run `borg setup`** to activate Directive A. Single command, ~5 seconds. Then verify the
   orchestrator session at `~/dev` renders the overview block instead of injecting any one
   project's checkpoint.
2. **Promote Directive B** (`docs/plans/directives/2026-05-11-per-project-state-migration.md`)
   to a full directive. The stub captures cutover Option C, `.gitignore` regression, and the
   field-by-field migration list — ready for /borg-plan when bandwidth allows.
3. **Open commits to push (if not already)**: `main` is at `4f899ba` (origin status unknown
   from this session — check before any push).
4. The historical doc footnote in `docs/plans/reviews/2026-05-07-nanoprobe-verification.md`
   was added during the rename — review for clarity if it looks awkward.
