Now I have enough context. Here's the debrief:

---

# Session Debrief: borg-collective
*Date: 2026-04-23 09:16*

## Objective
Fix `drone create_2pane_window` to default to side-by-side layout; the session also surfaced a `drone scaffold` workspace-collision bug and produced a decision to kill the debrief system entirely.

## Outcome

**Files modified (uncommitted):**
- `drone.zsh` — two independent changesets:
  1. `create_2pane_window` (line ~299): vertical 75/25 split → horizontal 50/50 (`split-window -h -p 50`); pane renamed `bottom` → `right`; focus lands on right (Claude) pane
  2. `cmd_scaffold` (line ~955): workspace default changed from hardcoded `/workspace` → `/workspaces/<project_name>`; added `workspace_explicit` flag; `__WS_SYMLINK__` substitution added to `_subst_template`; workspace default moved *after* preset check
- `templates/supabase/devcontainer.json`: `postStartCommand` now includes `__WS_SYMLINK__` placeholder, which expands to a `sudo rm -rf /workspace && sudo ln -sfn <workspace> /workspace` when workspace ≠ `/workspace`
- `dotfiles/tmux/tmux.conf` (not git-tracked, appears symlinked): assistant reports adding `bind = select-layout even-horizontal` and `bind _ select-layout even-vertical` at lines 64–65; deleting the 70/30 3-pane auto-resize hook at lines 84–89 — **these changes are not visible in git diff; verify they actually landed**

**Nothing committed this session.**

## Decisions Made

1. **Kill the debrief system.** Debriefs (auto-generated by `hooks/borg-stop.sh` via Sonnet) are redundant once checkpoint discipline is solid. Decision: drop the Sonnet call from `borg-stop.sh`, update `borg-start.sh` to read newest `.borg/checkpoints/*.md` instead of the debrief, update `borg link` deep-dive to show 3 newest checkpoints, update `borg init` morning briefing to surface last checkpoint timestamp, delete `~/.config/borg/debriefs/`. Not yet implemented.

2. **`drone scaffold --workspace` default changed to `/workspaces/<project>`** to avoid Claude Code session history collisions. When all drones shared `/workspace`, Claude Code encoded that as `~/.claude/projects/-workspace/` — one history shared across every project. Per-project paths (`/workspaces/myproject`) give each drone its own isolated Claude history. A legacy `/workspace` symlink is created in `postStartCommand` for backward compat.

3. **`borg regenerate` rename + `/borg-sever` skill** — decided to do it, but deferred until after debrief kill. Not started.

4. **`borg-link-up` / `borg-link-down` naming** — pushed back. Start hook is never user-invoked (hook file rename is cosmetic); `/borg-checkpoint` already functions as "link-down." No rename needed.

## Blockers and Observations

- **tmux.conf changes unverified in git.** The file lives at `dotfiles/tmux/tmux.conf` (symlinked, not tracked by git). Confirm the layout hotkeys and removed resize hook are actually on disk: `grep -n "even-horizontal\|even-vertical" ~/.config/dotfiles/tmux/tmux.conf` — if empty, those edits were described but not written.
- **Reload required before drone changes take effect.** tmux: `prefix + r` (or `tmux kill-server && tmux`). drone: changes apply to next `drone up`, not running sessions.
- **All three modified files are unstaged.** Session ended without a commit.

## Next Steps

1. **Verify tmux.conf edits landed:** `grep -n "even-horizontal\|even-vertical\|84\|85\|86\|87\|88\|89" ~/dev/borg-collective/dotfiles/tmux/tmux.conf` — if missing, re-apply the two `bind` additions and delete the 70/30 resize hook block.
2. **Commit the working changes:** `cd ~/dev/borg-collective && git add drone.zsh templates/supabase/devcontainer.json dotfiles/tmux/tmux.conf && git commit -m "feat(drone): side-by-side 2pane default, per-project workspace scaffold"`
3. **Kill debriefs:** Edit `hooks/borg-stop.sh` to remove the Sonnet call; edit `hooks/borg-start.sh` to read `$(ls -t .borg/checkpoints/*.md 2>/dev/null | head -1)` instead of the debrief; update `borg link` and `borg init` display; `rm -rf ~/.config/borg/debriefs/`.
4. **`borg regenerate` + `/borg-sever`** after debrief kill is committed.
