## 1. Goal

Get borg-collective to a fully clean state, then implement Directive A (orchestrator-mode session
separation) and Directive B (per-project state migration out of registry.json).

## 2. Accomplished

**Cleanup (all merged to main):**
- PR #27 merged (capacity warning hook + adhd-guardrails)
- PR #28 merged (PROJECT_PLAN.md archived to assimilated)
- PR #29 merged (borg-plan-promote hook, gitignore fixes, checkpoints — fixed erroneous `.borg/`
  gitignore line, added `.claude/` and `templates/supabase/.borg/`)
- PR #30 merged (~/dev audit directive archived as assimilated)
- PR #21 rebased and merged (orchestrator-mode session separation — 5 good commits, 3 stale
  borg-plan-promote commits dropped)
- PRs #22, #23 rebased and merged (research docs — dropped 8–10 stale commits from each)
- PR #31 merged (session checkpoints)
- PR #32 merged (outstanding branch content: 12 positioning source cards, analysis restructure,
  sacrament talk docs)
- PR #33 merged (lifecycle.bats orchestrator-mode test coverage — 4 new cases)
- All stale local branches deleted; 0 open PRs; working tree clean (except `borg setup` injects
  erroneous `.borg/` into .gitignore — see Next Session)

**Directive A — Orchestrator-Mode Session Separation:** SHIPPED
- `_borg_session_mode()` helper already in `lib/borg-hooks.sh` (from PR #21)
- All three hooks (`borg-link-down.sh`, `borg-link-up.sh`, `borg-notify.sh`) already had
  orchestrator-mode guards (from PR #21)
- `borg-link-down.sh` already had the full orchestrator overview renderer (from PR #21)
- 4 new bats tests in `tests/lifecycle.bats` added and merged (PR #33)
- Deployed live via `borg setup`; `diff ~/.claude/hooks/borg-link-down.sh repo` = IN SYNC

## 3. Ready to Commit

Nothing uncommitted. Clean.

**Discovered bug:** `borg setup` re-injects `.borg/` into `.gitignore` on each run (same line we
fixed in PR #29). Root cause is in `install.sh` — the setup routine appends `.borg/` when it
initialises per-project gitignore entries. Must be fixed in Directive B or a dedicated bugfix.

## 4. Blockers

No blockers.

## 5. Next Session

**Implement Directive B — Per-Project State Migration.** This is the largest remaining work item.

Scope: move `status`, `last_activity`, `claude_session_id`, `has_uncommitted_changes`,
`waiting_reason`, `notify_origin` from `~/.config/borg/registry.json` into per-project
`<project>/.borg/state.json` files. Registry becomes a pure discovery index.

Start here:

1. **Fix `borg setup` `.gitignore` injection bug** (in `install.sh`) — find the line that
   appends `.borg/` and change it to append only the specific runtime exclusions
   (`.borg/state.json`) or nothing (if the root `.gitignore` already covers it). Check
   `install.sh` around line 200+.

2. **`lib/borg-hooks.sh`** — add bash state helpers:
   - `_borg_state_file <project_dir>` → `<dir>/.borg/state.json`
   - `_borg_state_read <project_dir>` → cat state.json or `{}`
   - `_borg_state_write <project_dir> <json>` → atomic write (tmp + mv)

3. **`hooks/borg-link-down.sh`** — replace registry writes (lines 142–156) with state.json
   writes; read `has_uncommitted_changes` from state.json (line 234); orchestrator overview
   reads status from each project's state.json (lines 90–97).

4. **`hooks/borg-link-up.sh`** — replace registry writes (lines 47–80) with state.json writes.

5. **`hooks/borg-notify.sh`** — replace registry writes (lines 38–54) with state.json writes.

6. **`lib/registry.zsh`** — add `borg_state_*` functions; remove volatile fields from
   `borg_registry_init` defaults (lines 83–89); add `borg_registry_with_state()` that merges
   state.json data into the registry JSON for consumers.

7. **`borg.zsh`** — replace all `borg_registry_read` with `borg_registry_with_state` in
   `cmd_ls` (line 254), `cmd_next` (line 550), `cmd_status` (line 432), `cmd_hail`
   capacity count (line 115), `cmd_link` session_id reads (lines 904, 928, 935, 947).
   Bump `BORG_VERSION` to v0.8.0.

8. **`install.sh`** — on setup, write empty `state.json` per registered project; add
   `.borg/state.json` to each project's `.gitignore` (NOT `.borg/` — that's the bug).

9. **Tests** — update `lifecycle.bats` to assert state.json instead of registry for volatile
   fields; add `tests/state.bats` for the state helper functions.
