Here is the structured debrief:

---

# Session Debrief: borg-collective
*Date: 2026-04-15 17:25 · Branch: main*

## Objective

Ship v0.7.1: make `borg setup` the single source of truth for `permissions.allow` in both Claude Code and Cortex settings by union-merging a versioned dotfiles base into live settings files — additive only, never removing machine-local entries.

A secondary objective emerged mid-session: file a cross-project portfolio directive pivoting Waypoint and wallpaper-kit from Snowflake/MCP-first to Supabase + Fly.io for MVP validation.

## Outcome

**Files created/modified:**
- `borg.zsh` — added `_borg_merge_settings_permissions()` helper and wired it into `cmd_setup` for both Claude and Cortex settings; fixed tmp file cleanup on `jq` failure (`|| { rm -f "$tmp"; return 1; }`)
- `VERSION` — bumped `0.7.0` → `0.7.1`
- `Formula/borg-collective.rb` — updated SHA/version for Homebrew
- `docs/plans/assimilated/2026-04-14-v0.7.1-versioned-settings.md` — plan archived (all criteria checked green)
- `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md` — **untracked, not yet committed** — portfolio-level directive for Waypoint + wallpaper-kit Supabase pivot
- `.gitignore` — **unstaged change** — added `.borg/` to ignore list

**Features shipped:**
- `borg setup` now union-merges `permissions.allow` from `$DOTFILES_DIR/claude/code/settings.json` into `~/.claude/settings.json` (idempotent, additive only)
- `borg setup` does the same for Cortex: `$DOTFILES_DIR/cortex/settings.base.json` → `~/.snowflake/cortex/settings.json`
- `borg setup` generates `~/.config/borg/claude-settings.local.json` template on first run (pre-populated with `model`, `enabledPlugins`)
- `__DOTFILES_DIR__` placeholder substituted in base before merge

## Decisions Made

| Decision | Reasoning |
|---|---|
| Additive-only merge (`unique` in `jq`) | Never clobber machine-local `permissions.allow` entries; `borg setup` must be safe to re-run anywhere |
| `__DOTFILES_DIR__` placeholder in base JSON | Avoids hardcoding absolute paths in the versioned dotfiles base; substituted via `sed` at merge time |
| Machine-local overlay for `model`/`enabledPlugins` | These are machine-specific; pulling them out of the base prevents `borg setup` from overwriting plugin config |
| Portfolio pivot to Supabase + Fly.io (both apps) | Snowflake Postgres at Stage 1 costs ~$10/mo with zero user benefit; SPCS is wrong shape for consumer SaaS; Supabase free tier carries 0–500 households at $0 |
| Two separate Supabase projects (not one shared) | Clean separation so either app can be killed/sold independently |

## Blockers and Observations

- **User typed `source ~/.zshrc` into chat instead of the terminal** at session end — the command never executed. The session closed without confirming the shell reload. If any PATH or alias changes were needed post-setup, they may not have taken effect.
- The session transcript's last 8KB shows only the tail (version check, stray shell command) — the substantive work happened earlier and is fully captured in commits.

## Next Steps

1. **Commit the two pending changes** — the portfolio directive and `.gitignore` update are not committed:
   ```
   git add .gitignore docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md
   git commit -m "chore: ignore .borg/ dir; file portfolio mvp pivot directive"
   ```
2. **Begin Waypoint Stream A, Session 1** — active plan at `/Users/noah/dev/wayfinderai-waypoint/PROJECT_PLAN.md`. Start by reading the plan cold, then scaffold the Supabase project and Next.js PWA skeleton.
3. **Name the wallpaper-kit rename** — Stream B Session 0 is blocked on branding. Resolve the new name so `docs/plans/directives/2026-04-14-supabase-flyio-mvp-pivot.md` can be promoted and work can start.
4. **Verify `borg setup` end-to-end on this machine** — confirm the session-end `source ~/.zshrc` was never run; if a fresh shell wasn't opened, run `borg setup` and check `jq '.permissions.allow | length' ~/.claude/settings.json` ≥ 95.
