**tl;dr** — Audited the infoviz remainder, designed borg/employer plugin coexistence, shipped AC7's verb half, and
merged all of it to main. Next session: the merge-tree retirement, starting with the writer port.

## 1. Goal

Answer what remains to complete the infoviz work, then execute the agreed next steps — which grew into a plugin
coexistence design, AC7 execution, and landing everything on main.

## 2. Accomplished

**Everything shipped.** Seven PRs merged (noah-goodrich/borg-collective#178 through #183, and #185) plus six
checkpoints committed directly. Main is green at `11beee7`: `make lint` rc=0, `make test-viz` 344 passed,
`borg chain` live, `borg program` tombstoned and forwarding its args.

**Infoviz audit** — 14 agents, 7 dimensions, each adversarially verified. AC1–AC4 genuinely done; AC5/AC6/AC7 are
the whole remainder with no artifact in flight. Verifier corrections that mattered: AC7's skills leg already passed,
`make eval --skip-model` is not a valid `make` invocation, and five parented children block `borg-assimilate`
Step 0.75, not one.

**Plugin coexistence design** — `docs/research/2026-08-31-plugin-coexistence/recommendation.md`,
`AI-scoring: 100/100`, epub at `~/Documents/Claude/borg-collective/`. Six options, a four-persona council with
mandatory dissent, three blind reviewers (revise ×3, none overturned). Reframed when Noah recalled the shim pattern:
borg already ships it twice — recon adapters (`recon-adapter-*` on `BORG_RECON_ADAPTER_PATH`) and skill extensions
(`01-context`/`02-output`/`03-followup`) — and **both sockets are empty**, verified.

**Root-caused `make lint`** as Python 3.13-vs-3.14, after two prior diagnoses were wrong. Held pylint 4.0.8 and
pylint-clean-architecture 1.5.2 fixed and varied only the interpreter: 3.14 clean, 3.13 raises W9006.

**Fixed 11 pre-existing `test-viz` failures** — a half-finished employer scrub pointing both directions. Ten had
inputs sanitized and expectations not; one had the reverse, fixed by generalizing the source to a Jira-key regex
rather than restoring the identifier. Added a negative case, verified by mutation.

**Shipped AC7's verb half** (`borg program` → `borg chain`, count still 25, bats 774 green) and **ratified its three
decisions** into `PROJECT_PLAN.md`, including fixing a verify clause that could never pass against the
test-enforced REMOVED-tombstone convention.

**Filed four directives:** the orchestrator drone-handoff (untracked across four prior sessions), the shim
architecture, the `project` → `repository` rename (re-measured at 3142 occurrences across 97 of 139 tracked code
files, against the plan's ~500 across 57+), and the merge-tree retirement carrying the ~30 traps a five-reader
survey found.

## 3. Ready to Commit

Nothing. All work is merged to `main` at `11beee7`; every session branch is deleted locally and on the remote.
Working tree carries only `uv.lock`.

**`/simplify` was NOT run this session** and `feat/ac7-rename-program-to-chain` has already merged. Worth a
retrospective pass over `borg.zsh` (`cmd_chain`, the dispatch arm, the help entry) and `merge-tree/coordinator.py`.

## 4. Blockers

- **Two load-sensitivity flakes.** `borg_core/test_proc.py::test_the_work_runs_between_the_start_and_the_collect`
  (1.0s ceiling over two 0.6s children, 0.4s headroom) and `cli_contract.bats`'s
  "np alias reaches the same nanoprobes rendering" case. Both pass 3/3 alone, fail under full-suite load.
  Deliberately not "fixed" — widening a timing threshold after seeing an inconvenient number is what the cairn
  postmortem forbids.
- **`uv.lock`** — track or delete. Open across six checkpoints.
- **AC7 still cannot be ticked**; the retirement half must ship first, because two validators still disagree.

## 5. Next Session

1. **Start the merge-tree retirement** —
   `docs/plans/directives/2026-08-31-retire-merge-tree-programs-into-borg-core.md`, step 1: port `write_manifest`
   into `borg_core/manifest/shell.py` **additively**, deciding on the record which validator it calls and whether it
   backfills `program`. **Do not repoint `coordinator.py`'s `discover` until `sync_borg`'s row semantics are
   decided** — repointing first silently deletes declared rows from `<repo>/.borg/programs/*.json` at exit 0 with a
   "wrote N manifest(s)" success line, and every existing test stays green.
2. **Then AC5**, which needs a write path built before any skill text; there is none in `borg_core/manifest/` today.
3. **Fleet note worth acting on:** `--delete-branch` on a stacked PR's parent **auto-closes its child**. It closed
   noah-goodrich/borg-collective#184 tonight (recovered — the child branch already carried the commit, so
   retargeting #185 to main landed all three). This belongs in the `stacked-pr-program` skill, since managing
   stacks is that format's whole job.

No external-origin text is quoted in this checkpoint beyond PR numbers.
