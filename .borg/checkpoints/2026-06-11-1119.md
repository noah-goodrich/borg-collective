## 1. Goal

Take the borg-collective + cairn + claude-plugins + dotfiles system through a pressure-tested,
testing-first hardening pass and ship it to a deployable, correct state — then bring cairn fully up
and running on a published release.

## 2. Accomplished

**Planning (pressure-tested under ultracode):** A 16-agent adversarial review (6 fact-checkers + 9
Collective personas + synthesis) overturned the first plan draft: the central "move skills out of
borg-collective" refactor was built on an **inverted premise** and was deleted. Corrections captured
in the plan and in memory `project_borg_source_of_truth` (borg-collective is canonical; claude-plugins
is the `build-plugin.sh` output; CoCo can't load a `.plugin`, so source must live in borg-collective).

**Shipped — all PRs merged, CI green:**
- **cairn #6**: fixed a real cross-surface data bug — `cli.py` `record_*` bypassed `service.py` and
  skipped embeddings, making CLI-recorded knowledge unsearchable; repointed through `cairn.service`,
  added an 8th fitness invariant (AST-assert write entrypoints route through service), a
  record-via-CLI→searchable integration test, fixed `ci.yml` `POSTGRES_DB`, added `.dockerignore`,
  bumped to `0.2.0`, made the schema drift-check pg-version-portable.
- **cairn #7**: multi-arch publish workflow (QEMU + buildx, `linux/amd64,linux/arm64`, `workflow_dispatch`).
- **borg-collective #48**: reaper GNU/BSD `stat` fix (`lib/reaper.sh:88`) — `main` CI had been silently
  RED since #46 (BSD-first `stat -f` returns garbage on GNU). Same bug class fixed in research-tools.
- **borg-collective #47**: spend-opt lean-context contract in `agents/borg-nanoprobe.md` +
  turn-discipline in `skills/borg-plan/SKILL.md`, `tests/FLOWS.md` (19-flow matrix), mutation decision.
- **claude-plugins #14**: the previously-absent CI workflow + 54 hook BATS + research-tools GNU/BSD
  portability (39/39 both OS; the should-pass-fixtures-failed cascade traced to the same `stat` bug +
  locale-sensitive en-dash regex).
- **claude-plugins #15**: `build-plugin.sh` rebuild propagating the reaper fix + spend-opt into the
  distribution.
- **dotfiles #3**: corrected stale plugin counts + recovery block, secrets matrix, removed dead `dev.sh`.

**cairn release + local deploy (the milestone):** Tagged `v0.2.0-rc.1` (validated runtime), added
multi-arch, `v0.2.0-rc.2` (verified amd64+arm64 manifest), then `v0.2.0`. Package flipped public (by
Noah). Deployed `ghcr.io/noah-goodrich/cairn:0.2.0` to the local stack: `/health` ok, `/ready`
migrations at head `002_documents`, **knowledge counts before==after (no data loss: 86 sessions, 8
decisions, 5 patterns, 8 observations, 1 document)**, and the data-fix verified live (a decision
recorded via the in-container CLI returned in search at sim 0.94). `compose.yml` pinned `:latest`→
`:0.2.0` (`f5c83cc`, pushed to `main`). Rollback digest: `sha256:4565aede...`.

Testing-first earned its keep: it surfaced 4 latent local-passes/CI-fails bugs that were all fixed.

## 3. Ready to Commit

Nothing pending — all work merged via PRs (CI green) or pushed (`cairn f5c83cc`). Working trees are on
`main`. `/simplify` was not run as a standalone pass this session, but every change shipped through a
reviewed PR with passing tests/CI (borg-collective bats green, claude-plugins 54 BATS + research-tools
39/39, cairn fitness + 324 tests). The compose-pin direct-push to cairn `main` was a 1-line validated
infra change.

## 4. Blockers

No hard blockers. The cairn GHCR publish/visibility was briefly blocked on a missing `read:packages`/
`write:packages` token scope on this session — resolved by Noah flipping the package public in the UI.

## 5. Next Session

1. **Activate this machine** (this session still runs PRE-session hooks/skills — the merged work is not
   live in the shell): `git -C ~/dev/borg-collective pull && git -C ~/dev/claude-plugins pull && git -C
   ~/.config/dotfiles pull && git -C ~/dev/cairn pull && borg setup`, then **restart Claude Code**.
2. **Work-machine rollout** (original Wave 6): clone borg-collective + run its install.sh (gets
   borg/drone symlinks — nothing else installs the CLI), clone claude-plugins, pull dotfiles, install.sh,
   `borg setup`; then the acceptance gate — skills/agents load in BOTH Claude Code and CoCo, and `command
   -v cairn` resolves the HTTP shim (not the pipx Python CLI that raises on missing POSTGRES_PASSWORD).
   Provision only work-appropriate Keychain entries (NOT personal ANTHROPIC/FLY/CLOUDFLARE/PORKBUN).
3. **reveal (parked this session)**: the two conformance directives (data-consistency, testing) ALREADY
   SHIPPED to `main` (PR #9 + testing commits) — it's a **verify-don't-rebuild** task. Live devcontainer
   is `reveal-data-consistency`, not `reveal`. The stale `feat/data-consistency` worktree (HEAD `e52a595`,
   orphaned/divergent from main) should be removed and the devcontainer re-pointed at `main`.
4. Optional backlog: rotate the cairn `.env` Anthropic key (hygiene — no leak occurred); cairn knowledge
   backfill (unblocks the spend-opt cairn-warm-brief criterion); `borg.zsh` BSD-only `stat -f` calls
   (host-CLI, macOS-only, low priority).
