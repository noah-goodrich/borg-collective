**tl;dr** — Verified against live GitHub: all four train PRs are MERGED and both machines' e2e smoke sweeps
are green — the pipeline-complete banner is posted; only the security line (noah-goodrich/borg-collective#161
+ #163) remains open, awaiting work-machine stamps. Next session: close the security line, S4 manifests PR,
then S1 + deriver.

## 1. Goal

Drive noah-goodrich/borg-collective#158/#159/#160/#162 (+#161/#163) through cross-machine approval, blind
review, merge, deploy, and e2e smoke — then verify the end state against reality rather than memory.

## 2. Accomplished

- **Verified MERGED**: noah-goodrich/borg-collective#159 (`86983b5` into feat), #158 (`b4ec080`), #160
  (`b73010a`), #162 (`7a48a27` = main head). Four-round blind opus gate, findings 13→8→5→0.
- **Both smoke halves green and posted to the #159 hub**: mine (7 legs incl. spine grouping
  ingle-t1-cutover as 6 items / 4 repos / one workstream) and the work machine's 🏁 (432+335 tests,
  `borg program sync` rewrites in place with no duplicate files, PM5 provenance verified reaching
  data.json, doctor exit 0).
- **Deployed here**: main pulled, `borg setup`, gh 2.96.0→2.98.0 (SA1 / CVE-2026-64652 cleared).
- **Reality-check findings**: watcher (`bmhdy7z9f`, still running) drops MERGED PRs, so the hub went
  unwatched post-merge — that's how the 🏁 comment arrived silently. #161/#163 remain watched.

## 3. Ready to Commit

- `skills/borg-link-up/SKILL.md` (only tracked mod — the checkpoint tl;dr mandate): hold until
  noah-goodrich/borg-collective#163 merges (same file), then reconcile + commit. Docs-only, no /simplify
  needed.
- `.borg/programs/viz-program.json` here + `ingle-t1-cutover.json` in stillpoint (smoke placements) —
  S4's PR.
- 18 untracked files: mostly the checkpoint backlog + research artifacts — standing drift item.

## 4. Blockers

- noah-goodrich/borg-collective#161 @ `3b1d0c5` and #163 @ `d00a859`: my stamps posted (19:03Z / 21:56Z);
  the work machine's own 🏁 lists them as "still open for review" on its side.
- Docs nit from the 🏁: the committed stack manifest's statuses need refreshing to `merged`.

## 5. Next Session

Check noah-goodrich/borg-collective#161 and #163 for work-machine stamps first (watcher covers both; the
hub on merged #159 it does NOT — check that manually). Then: (1) merge #161/#163 when stamped; reconcile +
commit `skills/borg-link-up/SKILL.md`; (2) refresh the stack manifest statuses to merged (small docs PR);
(3) S4 PR landing both manifests in their owning repos; (4) S1 `borg show` per
`docs/plans/directives/2026-08-20-comms-delivery-surfaces.md` (AC1b, `$TMUX_PANE`) + deriver D4
(`borg_core/recon/core.py:resolve_since` relative-since fix); (5) fold opus NITs into the next `borg.zsh`
change; (6) fix the watcher's drop-on-merge gap before it becomes S-something.
