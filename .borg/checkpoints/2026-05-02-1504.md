## 1. Goal

Long orchestration session (started 2026-05-01 ~16:00 MDT): drive reveal, reveal-site, ingle, and
snowfort to completion while user was away; execute archetype rename compliance sweep; import
high-scored experiment photos into prod Supabase; answer 9-point product question list from user.

## 2. Accomplished

**Reveal (app.reveal.photo)**
- All 7 archetype example images generated and verified (burtynsky retry succeeded, bw_conversion
  bug fixed)
- `scripts/smoke_prod.py` written (512 lines), committed, passing against prod
- GalleryMonograph.tsx: wall label "after Joel Sternfeld" replaced with tradition name example;
  dead displayName.split(" — ") removed from GalleryMonograph, gallery/page, BetaFeedbackForm
- Archetype example JPEGs renamed from photographer slugs to tradition slugs (e.g.,
  `adams_zone_system_0.jpg` → `zone-system-bw.jpg`) — all 7 renamed, references updated
- Scope messaging added to SubmitForm: landscape/outdoor only, portrait coming
- Decisive Moment + New Color archetype prompts: landscape fallback added for no-people scenes
- DB audit run: no photographer-name violations found in triage_results.artist_statement or
  selected_archetype
- IMPORT TASK IN PROGRESS: 13 high-scored landscape photos being triaged inside container via
  `scripts/run_triage_analysis.py` — ~10-15min runtime, then upload + DB insert

**Reveal-site (reveal.photo)**
- Full archetype rename deployed to Cloudflare Pages (all 7 tradition names live)
- "Rooted in [keyArtist]" sublines on gallery cards
- `/about/independence` page + footer disclaimer
- Logo fixed: `logo-mark-nav.png` (aperture icon, 40px) added to SiteHeader.astro alongside
  wordmark
- 7 SVG archetype placeholders replaced with real generated tradition-slug JPEGs
- Scope copy added to hero and CTAs: landscape/outdoor only, portrait support coming
- SSR gallery deployed: `index.astro` + `seen/` pages now fetch from Supabase (potential_score>=4,
  status=complete), `prerender = false`, Cache-Control s-maxage=86400. Shows "empty state" until
  reveal import completes.

**Ingle (mcp.ingle.kitchen)**
- Worker deployed, DNS configured, MCP server live
- `INSTALL_CLAUDE_DESKTOP.md` written
- Real Goodrich family data migrated to prod Supabase:
  - Household: Goodrich Household (234b71c5-ad0c-49bc-b091-5bd1d8d2ccc2)
  - Members: Noah (38), Elisabeth (37), Son (3)
  - 13 dietary preferences (all real from family-preferences.md)
  - Budget target: $150/week, Walmart/Target
- Jane/John Smith dummy data: was never in prod (only local seed) — no cleanup needed
- MCP smoke test passed: `family_get_preferences` returns real Goodrich data live
- PROJECT_PLAN.md archived, v0.3.0-mcp-hosted tagged and pushed

**Snowfort**
- Full audit completed (82 findings → 24 demo objects + 58 real)
- Demo teardown executed (user confirmed): BAD_COST_WH, BAD_MC_WH, BAD_PRACTICE_DB, PRD_BAD_DB,
  BAD_ADMIN_USER, ORPHAN_ROLE, SNOWFORT_TEST_* removed
- Post-teardown rescan: 58 real findings. Cortex presented remediation plan:
  - APPLY NOW (13): MFA enforcement, password policy, statement timeouts, resource monitor,
    break-glass ACCOUNTADMIN, AUTO_SUSPEND, network policy
  - DEFER (8): perf items needing workload analysis, Budgets, tagging
  - ACCEPT: operational maturity, informational
- **Awaiting user**: Cortex session (%40) waiting on "apply the plan" decision

**Decision log**: `/Users/noah/dev/borg-collective/.borg/decisions/2026-05-01-session.md`

## 3. Ready to Commit

`borg-collective` has untracked checkpoint files and the decisions log. No code changes to
borg-collective itself. Commit:
```
git add .borg/checkpoints/ .borg/decisions/
git commit -m "chore: session checkpoints + decision log 2026-05-01/02"
```

## 4. Blockers

- **Reveal import task**: `run_triage_analysis.py` running in container on 13 photos — will
  complete in ~10-15min. After triage, `import_experiment_plates.py` uploads to Storage and
  inserts DB records. Not blocked, just time.
- **Reveal-site gallery empty**: Shows "empty state" correctly. Will populate once reveal import
  completes and plates appear in prod with status=complete and potential_score>=4.
- **Snowfort**: User must say "apply the plan" in Cortex session (%40) to execute remediation.
- **Reveal scoring granularity**: `potential_score` and `archetype_fit_score` are 0-5 integers —
  not granular enough to distinguish top 10% from top 30%. Python `scoring.py` computes rich
  float rubric scores (0-10) but these are never stored in Supabase. Need `rubric_score NUMERIC(4,2)`
  column added to triage_results and populated by pipeline.
- **Reveal hero image (reveal-site)**: Using brand/hero images now. Archetype example JPEGs
  available for SSR gallery once import lands.
- **Reveal next directives**: google-oauth + vertex-ai-migration both gate on GCP setup (user
  action required).

## 5. Next Session

1. **Verify reveal import landed**: Check prod Supabase for 13 new plates. Run
   `scripts/smoke_prod.py`. Check reveal-site gallery at reveal.photo/seen — should show real
   photographs.
2. **Snowfort**: Sit with Cortex session (%40), say "apply the plan" to execute the 13 APPLY NOW
   items. Start with MFA enforcement (SEC_016) — highest impact.
3. **Scoring granularity**: Add `rubric_score NUMERIC(4,2)` to triage_results migration in reveal,
   populate from `scoring.py` rubric output during pipeline run.
4. **GCP setup**: Enable google-oauth and vertex-ai-migration directives once GCP project is
   configured (user action).
