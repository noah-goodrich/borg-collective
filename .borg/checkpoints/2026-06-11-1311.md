## 1. Goal

Pull + `borg setup` to activate the machine, audit whether the 3 open borg-collective directives were genuinely
still open, action the findings (fix / assimilate), and un-strand the dotfiles repo — ahead of a shell restart and
a cairn validation pass.

## 2. Accomplished

**Activation:** Pulled borg-collective / claude-plugins / cairn (all already current); ran `borg setup` (hooks +
agents deployed, all "unchanged"; plugin version `0.2.3 → 0.2.9`).

**Verify-gated directive audit** (workflow: investigate → adversarial verify; investigator and skeptic agreed on
all three):
- **reaper-TZ** (`2026-06-06`): **NOT_DONE** — `lib/reaper.sh:37` parsed the UTC `Z` timestamp via `date -j -f`
  with no `-u`; the code even carried a standing "known bug, do not fix here" comment. Commit #48 was a different
  (`stat`) fix.
- **nanoprobe-worktrees #42** (`2026-06-10`): **DONE but un-archived** — all 10 criteria met in `c795f96` (#43);
  full suite 248 bats green, shellcheck clean. It was stale cruft in the backlog.
- **subagent-spend-optimization** (`2026-06-10`): **PARTIAL** — AC1 (lean-context contract) + AC2 (turn/thinking
  discipline) shipped in #47; AC3 (cairn-warm brief, gated on the out-of-repo cairn backfill) + AC4 (in-repo
  `token-spend.jsonl` measurement artifact) still open.

**PR #49** (merged, squash `5fb26d4`): reaper UTC fix (BSD `-u` + GNU `TZ=UTC` in `lib/reaper.sh`) + 2 TZ-boundary
tests in `tests/reap.bats` (250/250 bats green, shellcheck clean, CI green GNU+BSD) + assimilated the reaper-TZ and
#42 directives. `docs/plans/directives/` is now **only spend-opt**.

**claude-plugins:** committed + pushed `da82581` (borg-collective plugin → `0.2.9`).

**dotfiles un-stranded:** `main` was diverged on a merged-and-deleted PR branch. Reset `main` `--hard` to
`origin/main` (`9ab2892`), deleted dead branch `dotfiles-doc-fixes-and-secrets-matrix`, committed WIP `36ac626`
(`.borg/state.json` ignore + 6 keychain exports: `REVEAL_SUPABASE_SERVICE_ROLE_KEY`, `FLY_API_TOKEN`,
`CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `PORKBUN_API_KEY`, `PORKBUN_SECRET_KEY`). Archived `borg-compat`
(3 unmerged commits / 3269 lines, superseded by claude-plugins + dead `dev.sh`) to tag `archive/borg-compat`, then
deleted the branch. Confirmed `~/.claude/bin` is already on PATH via `zsh/.zshrc:81` (the setup warning was just a
stale shell — restart fixes it; no edit made).

## 3. Ready to Commit

- **dotfiles**: `main` is **ahead 1** (`36ac626`) — committed but **NOT pushed** (auto-classifier correctly held a
  default-branch push; awaiting explicit push authorization).
- **borg-collective**: clean on `main` (`5fb26d4`); only untracked items are user checkpoints in
  `.borg/checkpoints/` (`2026-06-10-1730.md`, `2026-06-11-1119.md`, this file).
- **claude-plugins**: clean (`da82581` pushed).
- `/simplify` not needed — the only code change (`lib/reaper.sh` + `reap.bats`) shipped through reviewed PR #49 with
  green CI.

## 4. Blockers

No hard blockers. dotfiles `main` push to origin is pending explicit authorization. spend-opt AC3 is gated on the
out-of-repo cairn knowledge backfill (tracked in the cairn repo).

## 5. Next Session

After the shell restart (activates the merged borg work + the `~/.claude/bin` PATH line):

1. **Validate cairn is fully functioning** — container `cairn-cairn-app-1` (`0.2.0`) is up. Check: `/health`,
   `/ready` (migrations at head `002_documents`), knowledge counts (expect no data loss vs last session: ~86
   sessions / 8 decisions / 5 patterns / 8 observations / 1 document), and a record→search roundtrip (`borg search`
   or the in-container cairn CLI). Rollback digest if needed: `sha256:4565aede…`.
2. **Push dotfiles `main`** (ahead 1, `36ac626`) to origin when ready.
3. **spend-opt directive** (the only remaining file in `docs/plans/directives/`): AC3 (cairn-warm brief wired into
   dispatch, gated on cairn backfill) + AC4 (in-repo `token-spend.jsonl` main-vs-subagent measurement artifact).
