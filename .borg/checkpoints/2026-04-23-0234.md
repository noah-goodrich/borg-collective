## 1. Goal
Implement the container notification bridge from `PROJECT_PLAN.md` — close the gap where devcontainer Claude sessions can't fire macOS popups when transitioning to `waiting`.

## 2. Accomplished
- **`bin/borg-notifyd`** — host daemon (zsh, 65 lines): fswatch loop, per-project state tracking, silent startup snapshot, deleted-project pruning, Homebrew PATH fix
- **`launchd/com.stillpoint-labs.borg.notifyd.plist`** — LaunchAgent template; installed and running
- **`hooks/notify.sh`** — `/.dockerenv` guard added (silent exit inside containers)
- **`hooks/borg-start.sh`** — CLAUDE.md sync/extensions skipped inside containers (was polluting host CLAUDE.md with `/home/dev/...` paths)
- **`install.sh`** — installs borg-notifyd symlink, fswatch, launchd plist via `sed` substitution + `launchctl bootstrap`
- **`borg.zsh`** — two `_borg_merge_claude_md` fixes: atomic write (prevents truncation on interrupted setup), dangling symlink removal before cp
- **Releases cut:** v0.7.3 (notification bridge), v0.7.4 (setup fixes), v0.7.5 (dangling symlink fix)
- **PROJECT_PLAN.md** — acceptance criteria 1, 3, 4, 5 verified; AC2, AC6 require manual smoke test

## 3. Ready to Commit
Nothing uncommitted — all work is pushed to `origin/main`.

## 4. Blockers
- **AC2** (host-origin no double-fire) and **AC6** (click-to-focus) still need a 30-second manual smoke test before `PROJECT_PLAN.md` can be archived via `/borg-assimilate`
- Work machine setup confirmed working after v0.7.5, but not yet verified end-to-end

## 5. Next Session
Run the manual smoke test for AC2 and AC6, then execute `/borg-assimilate` to archive `PROJECT_PLAN.md`. Specifically:
- AC2: let an orchestrator Claude turn end in tmux → confirm exactly one popup fires (not two)
- AC6: click the popup → confirm Ghostty activates and tmux switches to the correct window
- Then: `/borg-assimilate` → confirm + archive `PROJECT_PLAN.md` → `docs/plans/assimilated/2026-04-20-container-notification-bridge.md`
