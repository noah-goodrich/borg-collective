## 1. Goal

Improve borg's testing posture after a session that found three bugs behind a green suite — which expanded into
deciding the repo's long-term language/toolchain direction and decomposing the cross-repo viz work into
executable projects.

## 2. Accomplished

**Design study (`/research` decision-design).** Four parallel research tracks, five options (A–E), one blind
adversarial review. Noah selected **B+D** for immediate execution, **C** as long-horizon.

**Shipped — PR #115, `a90469b`:**
- **161 assertions were silently ignored on macOS.** Documented bats-core gotcha: bash's `set -e` handling of
  `[[ ]]` changed in 4.1, macOS ships **3.2.57**. All now `[[ … ]] || false`. When finally enforced, **all 161
  passed** — correct, just unchecked.
- **`tests/cli_contract.bats`** — 12 black-box tests invoking `borg.zsh` under a real `zsh`. The only suite that
  runs the CLI in its own interpreter.
- **`contract-macos` CI leg** — green in **27s**. The only place BSD-vs-GNU (#114) is observable; a Linux
  container can't catch it.
- shellcheck extended to `lib/*.sh`; bats pinned to `bats-action@4.0.0`; scheduled cron re-tests main.
- `tests/README.md` stating what each lane does **not** prove.

**Decided and documented — PRs #116(closed)/#118/#117/#119:**
- **Language: Python + shell.** Verified the premise was wrong: **no packaging approach fixes CPython startup** —
  Nuitka measured *257ms vs 152ms*, PyInstaller onefile re-unpacks every run, PyOxidizer dormant since 2024-11-03,
  and `py2many` can't transpile `typer`. Measured `-S` recovers only **6.5ms** on 3.14, not the ~45% still quoted
  from 2017. **Python's floor ~41ms vs zsh's ~27ms — the hook boundary is arithmetic.**
- Blind review returned **REVISE** and refuted two of my claims. Both verified and corrected; the strongest became
  gate **M0**.
- Found the answer to "bake in the testable-core rule" already existed: **`pylint-clean-architecture`'s Silent
  Core Rule (W9013)**, published by Noah.
- Seven single-concern directives; stale combined directive **severed** with a 14-criterion disposition map.

**Root-caused the 2026-08-10 failure.** `sme-self-service-pat` was missed because its blocker *was Noah*, it had
**no Jira ticket** (stakes axis scored it zero), and effort was never an axis. The remedy already existed —
`REVIEW_BUCKET`, commit `914c7e2`, **twelve days earlier, in the renderer being replaced.**

## 3. Ready to Commit

**Nothing.** Working tree clean, main green at `c7771ce`, zero open PRs. All work landed via #112/#115/#117/#118/#119.
`/simplify` was run on the code changes; the remaining commits were docs-only.

## 4. Blockers

No blockers. Two handoffs:
- **Branch protection** — `D5`, needs repo-admin auth. Left unchecked deliberately; an agent with admin-bypass
  defeats the rule.
- **`story.json` still has no generator** — unchanged; that's `viz-2`.

## 5. Next Session

1. **`viz-1` — awaiting-you tier.** Highest value, already designed. Port `REVIEW_BUCKET` from
   `merge-tree/render.py` into `render_graph.py` (it's in the wrong file), add it to `borg link`'s landing region.
   Directive: `docs/plans/directives/2026-08-11-viz-1-awaiting-you-tier.md`. **V6 requires confirming the test
   fails before the fix** — no mutation tooling exists, so it's manual and mandatory.
2. **Branch protection** — `lint` + `test` + `contract-macos`, empty bypass list.
3. **`warehouse-permissions` #340/#341** — approved and mergeable, then the `ALTER USER … SET TYPE=SERVICE` flip.
   Pressing before vacation, still is.
4. Then `viz-2` → `link-unification-and-layout` → `viz-3` → `attention-routing`.
   `python-core-and-toolchain` Part 1 is independent and available anytime.
