## 1. Goal
Ship borg-collective v0.7 Session 1: reliability fixes — CLAUDE.md copy strategy, always-on
read-permission skill, checkpoint persistence, and /simplify gate on assimilate.

## 2. Accomplished
- `borg.zsh` + `drone.zsh` + `borg-start.sh`: CLAUDE.md symlink strategy replaced with
  mtime-based copy via shared `_borg_sync_file` helper; heals on every session start and
  `borg setup` run
- `lib/borg-hooks.sh` + `lib/borg-sync.zsh`: new `_borg_sync_file` helper extracted (mtime,
  no python3, handles symlink-to-file migration)
- `skills/no-unnecessary-read-perms/SKILL.md`: new always-on skill enforcing the read-permission
  rule from global CLAUDE.md; auto-installed by existing skill loop
- `skills/borg-checkpoint/SKILL.md`: updated to write timestamped file at
  `<project>/.borg/checkpoints/<YYYY-MM-DD-HHMM>.md`
- `skills/borg-assimilate/SKILL.md`: new Step 0 gates shipping on `/simplify` confirmation
- `PROJECT_PLAN.md`: v0.7 plan locked with 16 criteria across 6 sessions
- `CLAUDE.md` Learned section: two new entries (lib source path mismatch; copy-success
  tracking anti-pattern)
- Two commits on main: `680d387`, `e2958ee`

## 3. Ready to Commit
Nothing — both commits landed clean. Working tree is clean.

## 4. Blockers
No blockers. Session 1 criteria all verified: 69/69 bats tests pass, shellcheck clean.

## 5. Next Session
Session 2 — pathing bugs:
- **Plugin marketplace path fix** (`~/.claude/settings.json` line 227 has host-absolute path
  `/Users/noah/.config/dotfiles/claude/plugins` that fails inside containers). Start at
  `drone.zsh` postStartCommand / devcontainer scaffold, add host-path compatibility symlink.
- **Neovim pathing fix** — run `nvim --headless "+Lazy sync" "+q"` in a fresh container,
  capture errors, apply fixes (likely lazy.nvim state dir or similar). May require changes
  in the dotfiles repo (`~/.config/dotfiles/nvim/`), not borg-collective itself.
