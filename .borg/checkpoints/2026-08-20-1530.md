## 1. Goal

Review PR #158 — which grew into: prove the viz pipeline on live data, audit completion across all projects,
and prototype the communication program (how work state and technical writing actually reach Noah).

## 2. Accomplished

- **PR #158 review** (15-agent adversarial workflow): core `programs.py` sound; `backfill.py` carries 3
  reproduced correctness defects; split recommendation. All findings survived dedicated refutation.
- **Rider pushed: `rider/pr-158-review`** — REVIEW.md (new format), `apply_program_projects()` fix
  (0 → 2 cross-repo workstreams on live data, incl. a real 4-repo chain) + 6 tests (293 pass, ruff clean),
  evidence manifests. For the work machine's second opinion. Simplify pass run, one fix amended in.
- **Live PoC of the whole pipeline** (first time it ever ran for real): 136 PRs swept; proved the spine
  severs declared cross-repo chains without the fix; found `borg recon --since 30d` silently returns 0
  items (relative since-marks unresolved before the adapter); found the awaiting-you tier can never fire
  (adapter fetches no reviewDecision/mergeable).
- **Completion audit** (research skill, evidence mode, gate PASSED after format fixes):
  `docs/research/2026-08-20-project-completion-audit/` + epub. Headlines: 97% of 381 checkpoints
  hand-restate plan position; 36 of 109 open directives are already done; 87% of started work ships.
  Overturned my own "park the viz program" — investment goes to state derivation, not graph decoration.
  Side fix: partitioned the shared `docs/research/sources/` pool per-deliverable (it was silently breaking
  older runs' verification sample math).
- **Communication program filed**: umbrella `docs/plans/directives/2026-08-20-communication-program.md`
  (3 decision boxes for Noah) + children `2026-08-20-comms-delivery-surfaces.md` (borg: `borg show`,
  `borg chains` terminal-first, chat contract) and claude-plugins
  `docs/plans/directives/2026-08-20-design-doc-and-brevity-skills.md` (design-doc/brevity/pr-description
  skills). Style research done (Google style guide, Smart Brevity unbranded, design-doc naming).
- **Chain-map prototypes, three forms**: ANSI terminal (color + OSC-8 links), `chains.md` in the nvim side
  pane, HTML (rejected twice by Noah — demoted to explicit-request-only). Rescued from scratchpad to
  `~/.local/state/borg/merge-tree/prototypes/` (scratchpad dies with the session).
- **claude-plugins drone up** (borg:5), kickoff sent: review its directive, plan, wait for approval.
- **Memories written**: `feedback_response_density`, `feedback_communication_delivery` (tl;dr at BOTTOM of
  chat / top of docs; terminal/vim first, HTML last; vertical chains; enterprise CC has no artifact tool;
  spacing beats typography — eyesight).

## 3. Ready to Commit

Nothing committed in the main repo. Candidates, all reviewed markdown (no code to /simplify):
- `docs/plans/directives/2026-08-20-{communication-program,comms-delivery-surfaces}.md` — hold until Noah
  checks the umbrella's decision boxes.
- `docs/research/2026-08-20-project-completion-audit/` + `docs/research/sources` re-partition +
  `.gate-armed` — commit-ready as a docs PR.
- claude-plugins repo: its directive file (that drone should commit it, not this session).
The rider branch is already pushed. Checkpoint backlog (11 untracked files in `.borg/checkpoints/`) still
uncommitted — tracked by the standing drift issue, not this session.

## 4. Blockers

- **Noah's three decision boxes** in the umbrella (approve children / GitHub stance / Obsidian stance) gate
  all implementation. claude-plugins drone is parked on the same approval.
- **Work machine's take on the rider** gates the #158 split (merge core + fix, hold backfill).
- `borg recon --since <relative>` bug: real, unfixed; workaround is ISO dates. Feeds everything derived.
- Directive-state deriver (the audit's #1 recommendation) is referenced by the program but not yet filed —
  deliberately waiting on the umbrella decision.

## 5. Next Session

**Start with Noah's answers**: the umbrella decision boxes
(`docs/plans/directives/2026-08-20-communication-program.md`, open in the borg:4 nvim pane) and the work
machine's verdict on `rider/pr-158-review`.

Then, in order:
1. If approved: implement S1/S2 from `~/.local/state/borg/merge-tree/prototypes/` (renderers are working
   code; S2 needs the registry-resolved `--programs-dir` and the `.gitignore` carve-out from S4). Dispatch
   K1-K3 to the claude-plugins drone (borg:5, session already primed).
2. File the directive-state deriver directive (mechanize the audit rubric; audit doc §1 has the spec seed).
3. Fix `borg recon` relative since-marks (`lib/recon.sh`, since-mark resolution) with a test that uses the
   production path, not a fixture-supplied date.
4. #158: if the split is agreed, cherry-pick the rider fix into the narrowed merge.

**Format contract for whoever picks this up**: chat replies short with tl;dr at the BOTTOM; documents to the
nvim side pane via tmux (`borg show` once S1 exists); HTML only on explicit request. See memory
`feedback_communication_delivery` — it encodes what Noah rejected twice and why.
