Here is the structured debrief:

---

# Session Debrief: borg-collective
*Date: 2026-04-18 08:04*

## Objective
Set up Claude Code project-level configuration for borg-collective and groom pending housekeeping items (`.gitignore`, drone window colors, portfolio directive). The session ended with an exploratory deep-dive into how the `borg-plan` skill works.

## Outcome

**Files created:**
- `.claude/settings.json` — Claude Code project-level settings (content untracked/unshown but directory created this session)
- `.claude/settings.local.json` — local overrides for Claude Code settings
- `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md` — cross-project directive for Waypoint + wallpaper-kit Supabase/Fly.io pivot (authored 2026-04-14, committed to repo this session)

**Files modified (not yet committed):**
- `.gitignore` — added `.borg/` to excluded paths
- `drone.zsh` (line 140) — fixed active tmux window style from `fg=$color,bold` to `fg=colour0,bg=colour255,bold` (inverted colors so the active window is visually distinct regardless of project color)

**Features/fixes:**
- Drone active-window highlight now works correctly — selected window renders as black-on-white instead of inheriting the project color with bold (which was hard to read on some colors)
- `.borg/` runtime directory is now gitignored

**Session ended with:** Reading `skills/borg-plan/SKILL.md` to answer "Can you break down exactly what the shape and structure of a borg-plan is?" — no plan was actually kicked off

## Decisions Made

| Decision | Reasoning |
|---|---|
| `fg=colour0,bg=colour255,bold` for active window | Project-color + bold was indistinguishable from inactive windows at a glance; inverted black/white is unambiguous regardless of per-project hue |
| `.borg/` added to `.gitignore` | Runtime state directory (analogous to already-ignored `registry.json`, `.borg-project`) — should never be committed |
| Portfolio directive filed under `docs/plans/directives/` | Not a `PROJECT_PLAN.md` itself; it's a cross-repo constraint document that governs two external repos (Waypoint, wallpaper-kit) |

## Blockers and Observations

- **Nothing was committed this session.** Four changes (`.gitignore`, `drone.zsh`, `.claude/`, directive) are sitting unstaged. They look intentionally left for a single cleanup commit.
- **`.claude/` is untracked** — unclear whether `settings.json` / `settings.local.json` should be committed or if `settings.local.json` belongs in `.gitignore`. The existing `.gitignore` doesn't reference `.claude/`.
- **`borg-plan` was read but not run** — the session ended on an explanatory question about skill structure, not an actual planning session. If Noah was scoping a new plan, it wasn't started.
- The portfolio directive (`2026-04-14-portfolio-mvp-pivot.md`) references paths in two external repos (`~/dev/wayfinderai-waypoint/`, `~/dev/wallpaper-kit/`) — those repos are the actual locus of work, not borg-collective.

## Next Steps

1. **Decide on `.claude/` gitignore policy** — either add `settings.local.json` to `.gitignore` (standard: local secrets stay out) and commit `settings.json`, or commit both if neither contains secrets. Check: `cat /Users/noah/dev/borg-collective/.claude/settings.json` and `settings.local.json`.
2. **Commit the housekeeping batch** — stage and commit `.gitignore` (`.borg/` addition), `drone.zsh` (active-window color fix), and the portfolio directive together: `git add .gitignore drone.zsh docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md && git commit`.
3. **If a `borg-plan` session is next** — run `/borg-plan` inside either `~/dev/wayfinderai-waypoint` or `~/dev/wallpaper-kit` (those are the active work streams per the directive, not borg-collective itself). Switch to Opus + Plan Mode first per the skill instructions.
