## 1. Goal

Answer "what are the open directives, and do we have a current PROJECT_PLAN?" — which became triaging the whole
backlog and clearing every part of it that did not require the owner personally.

## 2. Accomplished

**Ten PRs merged, `main` at `8c1afd6`.** #145 coverage gate · #146 directive reconciliation + drift-check filing ·
#148 briefing fallback + field collapse · #150 recon branch gaps · #151 chained auto-promotion ·
#152 usage-guardian gaps · #153 link simplify AC2–AC6 · #154 latent-defects filing · #155 AC1 + fold
comment · #156 port-era comment retirement · #157 archival of three directives.

**Board 14 → 12 open**, and the composition matters more than the count. Closed: infoviz-phase2, coverage-gate,
recon-untested-branches, link-port-simplify-followups (all seven ACs). Filed: `deployed-artifact-drift-check`
(5 targets incl. plists), `link-port-latent-defects` (5 latent defects that existed only in a checkpoint).

**Two directives proven NOT complete, against their paperwork.** `link-unification-and-layout` — 3/3 adversarial
skeptics refuted closure. Its 2026-08-12 revision reassigned L1/L2/L6 to the now-assimilated port; only **L3
(bottom-anchored layout)** and **L5 (re-measure)** remain and both have zero work. **127 of `borg link`'s 169 lines
are an uncapped directive dump** (`render.py:215-220`, ~75%) that no criterion anywhere covers — the one-screen goal
is unreachable as written. `usage-guardian-build` — fully built, 57/57 green, and had never once executed its own
acting path.

**Usage guardian: sweep and cadence now verified live.** Noah's forced sweep delivered `/borg-link-up` to 5 panes
(fired=5, failed=0). All three mechanical gaps shipped: `week_pct >= 90` warn-only, consecutive-UNKNOWN alert, and
adaptive cadence. Gap 3 was initially refused as impossible, then shipped after the orchestrator supplied the
one-plist design: **gate the probe, not the invocation.** Verified end-to-end in the log — perfect probe/skip
alternation at 60s wakes, real sample intervals **122–129s**, statistically identical to baseline. Zero extra probes.

**A live deploy hazard found and fixed.** `borg setup` deploys skills/hooks/agents/libs but **not plists**; the poller
is a **symlink to source**. So #152's due-check went live at merge while the installed plist still said 120s — and with
the code's baseline also 120s, jitter would intermittently halve the sample rate. Corrected with `install.sh`; the
cause is now recorded in the drift-check directive.

**AC4 ruling recorded (defer).** `chained-auto-promotion`'s AC4 greps for a short-form `*Next:*` pointer, but
`cmd_start` resolves slugs literally — so the criterion **passes exactly when the chain is broken at runtime.**

**Agent quality control.** #151 was sent back: its falsifiability proof was deleting the helper and seeing exit 127,
which shows wiring, not discrimination. Redone as 5 targeted mutations — all caught by their owning test, no coverage
gaps. The tests were already good; the evidence wasn't.

## 3. Ready to Commit

**Nothing.** Working tree clean, no stray worktrees, `main` at `8c1afd6`, everything merged and deployed.
`borg setup` + `install.sh` both run; hooks, libs, and the plist verified byte-identical to source.

## 4. Blockers

**#149 is open, green, and three days old** — `fix(recon): credential leak in item refs + bridge recon to the raw
gather (viz-2 S6)`, branch `feat/gather-producer`. Two reasons it matters: it names a **credential leak**, and it is
the missing `gather.raw.json` producer that viz-2 S6 and viz-3 are blocked on. Untouched here because viz/layout is
being worked elsewhere.

**Owner-only, undelegable:** arming the guardian (`BORG_USAGE_SWEEP_ENABLED=1`, `BORG_USAGE_HALT_ENABLED=1`) and
proving Claude Code honors the dispatch guard's exit 2. Bats can prove the script *emits* exit 2, never that the
harness respects it.

**Two one-minute decisions outstanding:** briefing Phase 3 (deferred in #148), and whether AC4 stays deferred or gets
amended.

**Date-gated:** cairn criterion 1.6 to **2026-08-18** (7 consecutive daily fires from 08-12). Two clocks —
`memory-gate.log` has 2 fires; `memory-hits.log` has 6 rows on 3 distinct days.

**`week_pct` was at 85 and climbing** against the warn-at-90 that just shipped. It will fire on its own. Warn-only by
construction; nothing to do.

## 5. Next Session

**Start with #149.** It is the only item with a security dimension and the only one unblocking parked work. Read the
credential-leak half specifically — that part is orthogonal to the viz work and can be judged on its own.

**Then verification #2**, the last undelegable guardian step:

```bash
tail -1 /Users/noah/.local/state/borg/usage-samples.jsonl
BORG_USAGE_HALT_ENABLED=1 BORG_USAGE_HALT_PCT=40 claude
```

Ask that session to spawn a subagent. Expect a denial naming the threshold and reset time. If it dispatches anyway
the guard is decorative — a real finding, and the 92% half needs a different mechanism. Set `HALT_PCT` below whatever
the current `session_pct` is; the sample must be under 300s old.

**Do not plan the layout work here** — `link-unification-and-layout` and viz-1/2/3 are being worked elsewhere. If that
changes, the one thing to carry over: **capping the 127-line directive dump at `render.py:215-220` must be a
first-class criterion**, and the baseline must be pinned to a fixture, not the live board (169 grows with the
registry).

**Do not re-run a defect search on the link port.** Closed by the reviewer after three rounds.

The lesson this session kept re-proving, four times in different costumes: **a check can be green exactly when the
thing it checks is broken.** The coverage gate let test files grade themselves. The recon tests supplied the value
production was supposed to derive. AC4's grep passes only when the chain is dead. And deleting a helper to watch tests
fail proves wiring, not discrimination. The fix is always the same — make it fail first, against real input, for the
right reason.
