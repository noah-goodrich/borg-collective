## 1. Goal

Multi-project orchestration session that started as a post-`borg setup` status check
(Directive A verification) and evolved into reactive shipping across four projects:
ingle migration closure, reveal brand-mark port + deploy fix, borg-collective
auto-plan-promote hook, and a brand-new stillpoint-labs marketing site stood up from
zero to `.pages.dev` live with custom-domain wiring in flight.

## 2. Accomplished

**Directive A verified live.** Cross-project overview rendered on SessionStart, not a
single project's checkpoint. P0 #1 from the prior session closed.

**ingle** — `nanoprobe/migrate-local-to-prod` merged to `main` via `--no-ff` (merge
commit `f7aadf6`), pushed to origin. Worktree at `/tmp/ingle-migrate-worktree`
removed, local feature branch deleted. (P1 #5 closed.)

**reveal** — Two PRs shipped to production.
- **PR #5** (`feat/brand-mark-design-port`, squash merged as `b548491`): ported
  reveal-site swirl mark + favicon + Header.tsx + login page updates. 9 files, 2
  commits.
- **PR #6** (one-line fix, squash merged as `b85c3e0`): added
  `export const runtime = 'edge'` to `/admin/users/page.tsx` to unblock the
  Cloudflare Pages build that PR #5's merge had exposed (the route was
  pre-existing-broken — never deployed before).
- Cloudflare run `25880720883` ✅. Production `https://reveal.pages.dev` → HTTP 200.

**borg-collective — PR #21 open, awaiting review.** Auto-plan-promote hook:
`hooks/borg-plan-promote.sh` is a PreToolUse hook (Edit/Write/NotebookEdit) that
silently writes the in-session `ExitPlanMode` plan to `docs/plans/PROJECT_PLAN.md`
before the first edit. Project-mode-only, idempotent, always exits 0. `borg setup`
registers it in both Claude Code and CoCo. 14 new bats cases; full suite at 179/179
green. Docs updated (CLAUDE.md, README.md, architecture.md).

**stillpoint-labs marketing site — scaffolded + live.**
- Recon: ingle-site (Astro/Tailwind, static) vs reveal-site (Astro/CSS-vars,
  CF Pages). Chose reveal-site as template; matched its plain-CSS pattern.
- Brand assets read from `~/Documents/Claude/Projects/WayfinderAI Co-founder/`:
  stillpoint_brand_architecture.md + brand_guide_stillpoint_labs.html.
- New public repo: `github.com/stillpoint-labs/stillpointlabs-site`.
- 4 pages shipped (Home / Products / About / Contact). All copy paraphrased from
  brand docs, no invented positioning.
- Brand tokens wired as CSS custom properties (bone, pitch, stillwater + product
  accents). Cormorant Garamond + Inter self-hosted from `/public/fonts/`.
- First deploy: `https://stillpointlabs-site.pages.dev` → HTTP 200.

**stillpointlabs.dev domain wiring** — Cloudflare token + Pages project verified,
Porkbun API access initially blocked (account-level disabled), Noah enabled,
re-fired domain nanoprobe. **In flight at checkpoint time.** Will create CF zone →
update Porkbun NS → bind apex + www → poll for cert. Rollback NS captured:
`{curitiba,fortaleza,maceio,salvador}.ns.porkbun.com`.

**Misc**: `/deep-research` skill expected but not present after `/reload-plugins`.
No action taken — flagged only.

## 3. Ready to Commit

Nothing in the orchestrator session itself — all work landed via nanoprobes in
isolated worktrees (PRs opened or merged-and-pushed by the respective nanoprobes).

The `.gitignore` modification in `/Users/noah/dev/borg-collective` working tree noted
at session start is still present, unaddressed.

**`/simplify` was not run** in the orchestrator. Each nanoprobe is responsible for
its own simplify pass; the PR #21 worktree did its own. Recommend a `/simplify` pass
on the borg-collective PR #21 branch before merging if it didn't already happen
(check the PR diff for the borg-plan-promote files: `hooks/borg-plan-promote.sh`,
`borg.zsh`, `tests/plan_promote.bats`).

## 4. Blockers

- **Domain nanoprobe for `stillpointlabs.dev` still in flight.** Until it lands,
  apex doesn't resolve. Expected to complete in 3–10 min depending on NS
  propagation.
- **PR #21 unmerged.** Once reviewed + merged, requires `borg setup` re-run to
  install the new PreToolUse hook into `~/.claude/settings.json` (same pattern as
  Directive A activation).
- Troth MCP smoke test (P0 #2 from prior session) — still open, manual task only
  Noah can do.

## 5. Next Session

1. **Check domain nanoprobe result.** If success: `curl -sI
   https://stillpointlabs.dev` → 200 + valid cert. If pending: re-poll
   `GET /accounts/4c043f0adf6a080de5c456d7f2d13bf3/pages/projects/stillpointlabs-site/domains/stillpointlabs.dev`.
2. **Review + merge PR #21** (borg-plan-promote). Then re-run `borg setup` to
   activate the hook.
3. **Troth MCP smoke test** — sign in at `app.troth.money/sign-in` as
   `goodrich.noah@gmail.com`. Verify accounts + categories + transactions render.
   Onboarding should NOT fire.
4. **stillpointlabs.dev v2 follow-ups** (from scaffold report):
   `public/og-default.png` (1200×630, ripple mark on bone), Cloudflare Email
   Routing for `hello@stillpointlabs.dev` → gmail forwarding, Plausible analytics
   once apex is live, Git integration in Cloudflare Pages dashboard for auto-
   deploys on push to main.
5. **Reveal vertex-ai-endpoint-migration assimilation** (P1 #3 carryover): run
   `scripts/run_calibration_sample.py --sample 5`, paste 5-row table into
   directive at line 289, move to `docs/plans/assimilated/`. ~30–60 min.
6. **Reveal cost-circuit-breaker items 3+5** (P1 #4 carryover): billing-alerts
   runbook + cost-per-photo SQL. ~1 hour. Then `/borg-assimilate`.
7. **Promote Directive B** (per-project state migration). Unblocked now that
   Directive A is verified live. Stub at
   `docs/plans/directives/2026-05-11-per-project-state-migration.md` — run
   `/borg-plan` when ready.
