## 1. Goal

Cut borg-collective v0.7.13 and fully assimilate the ingle database normalization directive
(migrations 0013–0019, frontend guide, schema docs).

## 2. Accomplished

**borg-collective v0.7.13 — shipped**
- Tagged `v0.7.13`, pushed tag, updated `Formula/borg-collective.rb` with new sha256
- Release covers: bash-guard pre-approval for `.borg-project` marker walks (eliminates
  2 permission prompts on every `/borg-link` invocation) + `borg-link-up` skill now
  agent-invocable (removed `disable-model-invocation: true`)

**ingle — DB normalization (0013–0019) — fully assimilated**
- Migration 0018 (`item_categories`): 9 seeded categories, `category_id` FK on 5 tables,
  77 grocery_items + 5 pantry_items backfilled. Applied local + remote.
- Migration 0019 (`cleanup`): `profiles_select` RLS tightened to `auth.uid() IS NOT NULL`;
  orphan seed household_members deleted; `fk_household_members_user` validated. Applied
  local + remote.
- Frontend migration directive created:
  `docs/plans/directives/2026-05-05-frontend-schema-migration.md` — covers all 7 breaking
  schema changes with TypeScript query patterns, fallback guidance, and CSV reimport note.
- `/simplify` run: comment fix in `0016_purchase_transactions.sql` (NOT EXISTS dedup
  description). All other findings were false positives or non-fixable in already-applied
  migrations.
- `/borg-assimilate` run: The Collective review passed; two follow-up items actioned
  (0019 migration + directive updates).
- Schema docs generated: `docs/schema/erd.md` (Mermaid ERD, 27 tables) + 27 table files
  under `docs/schema/tables/`.
- Directive archived: `docs/plans/directives/2026-05-06-db-normalization.md` →
  `docs/plans/assimilated/2026-05-06-ingle-db-normalization.md`

**ingle final state:** `main` at `4515b10`, clean working tree (7 untracked `scripts/_*`
files are pre-existing dev scripts, not part of normalization work).

## 3. Ready to Commit

**borg-collective:** Only untracked files — this checkpoint + `.claude/` + `templates/supabase/.borg/`. Commit the checkpoint:
```
git -C /Users/noah/dev/borg-collective add .borg/checkpoints/ && \
git -C /Users/noah/dev/borg-collective commit -m "chore: session checkpoint 2026-05-06"
```

**ingle:** Clean — all changes committed and pushed.

## 4. Blockers

- **`items` catalog is empty on remote** — `canonical_item_id` on grocery/pantry/meal tables
  is 0% populated. Backfill requires either: (a) CSV reimport from Walmart/Target/Costco
  order history, or (b) app-driven population as users add items. Frontend must handle
  `canonical_item_id IS NULL` gracefully (documented in directive).
- **ingle untracked `scripts/_*` files** — pre-existing dev scripts with `@types/pg`
  missing. Not part of normalization work. Either add to `.gitignore` or install `@types/pg`
  inside the devcontainer.
- **Supabase CLI version** — devcontainer has v2.90.0; v2.98.2 available. Low priority.

## 5. Next Session

**ingle — next directive:** Write `docs/plans/directives/2026-05-06-frontend-integration.md`
to drive the frontend migration. The frontend still reads all legacy text columns
(`grocery_items.name`, `meal_ingredients.ingredient_name`, etc.) and ignores the new FK
path. Start by auditing `src/` for all Supabase queries that touch the deprecated columns
listed in `docs/plans/directives/2026-05-05-frontend-schema-migration.md` Section 9.

Or, if data quality is the priority first: write the CSV import pipeline
(`scripts/import-orders.ts`) to seed `items` and `purchase_transactions` from the
Walmart/Target/Costco order CSVs — this unblocks the canonical link backfill.
