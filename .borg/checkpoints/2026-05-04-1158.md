# Session Checkpoint — 2026-05-04 11:58

## 1. Goal

Design and ship a lightweight protocol that lets `borg-plan` and `borg-assimilate` absorb local
context-specific behavior (JIRA, Linear, etc.) without forking the upstream skills.

## 2. Accomplished

**Design discussion + decision:**
- Compared wrapper, hook, and scaffolder patterns. ELI10'd the trade-offs and picked
  hook-protocol (load points in upstream SKILL.md) as the right shape for this case.
- Locked: per-machine + per-project layered scopes, markdown only in v1, three hook points per
  skill. Renamed proposed dir from `skill-hooks/` → `skill-extensions/` to avoid collision with
  Claude Code's existing `hooks` vocabulary.

**Pre-design + plan:**
- `/Users/noah/.claude/plans/we-need-to-talk-twinkly-porcupine.md` — full plan-mode design doc
- Ran `/borg-plan` on top of it, ran `/borg-collective-review` (Devil's Advocate as specialist),
  applied The Adult's three corrections (cut standalone doc, add regression criterion, rename to
  `skill-extensions/`), wrote `PROJECT_PLAN.md` with validated objective + 6 acceptance criteria.
- Archived stale `PROJECT_PLAN.md` (store-secret, shipped in v0.7.8) →
  `docs/plans/assimilated/2026-04-30-store-secret.md`.

**Implementation (5 files changed, 1 archived):**
- `skills/borg-plan/SKILL.md` — three load-point blocks (`01-context` before Collective Review,
  `02-output` before writing PROJECT_PLAN.md, `03-followup` after writing it).
- `skills/borg-assimilate/SKILL.md` — three load-point blocks (`01-context` before Step 0,
  `02-output` at start of Step 4b before merge, `03-followup` between merge and plan archival).
- `CLAUDE.md` — new "Skill extensions" subsection under Key Patterns with paths, hook table,
  layering rule, markdown-only constraint, terse-files note, JIRA worked example. ≤30 lines.
- `docs/boris-workflow.md` — one-line callout pointing to CLAUDE.md.
- `PROJECT_PLAN.md` — new plan for this work.

**Shipped to PR:**
- Branch `skill-extensions` pushed, PR #20 opened against main:
  https://github.com/noah-goodrich/borg-collective/pull/20
- Single commit, 6 files, +242 / -48.

## 3. Ready to Commit

Nothing further on `skill-extensions` — everything from this work is committed and pushed in
PR #20.

Untracked on `main` and untouched by this session (pre-existing, unrelated):
- `.claude/` (settings.json, settings.local.json, scheduled_tasks.lock)
- `templates/supabase/.borg/` (empty `debriefs/` dir from prior work)

`/simplify` was not run because there's no executable code in this PR — only prompt edits and
docs. Skipping is appropriate.

## 4. Blockers

- **PR #20 awaiting CI + merge.** No blocker on author side; just needs the merge button.
- **Live verification of the protocol is deferred until ship.** The modified SKILL.md files only
  reach `~/.claude/skills/` via `borg setup`, which runs after `brew upgrade`. Until then, the
  protocol is "edited but unproven."
- **The JIRA extension itself doesn't exist yet** — that's acceptance criterion #4 and the only
  proof the protocol carries weight. It lives on the work machine, not in this repo.

## 5. Next Session

In order:

1. **Merge PR #20** — `gh pr merge 20 --squash --delete-branch` (or via UI). Confirm CI green
   first.
2. **Cut release v0.7.12.** Follow the existing release pattern (look at v0.7.11 commit
   `208c97e` for the exact recipe — likely a tag + brew tap update). Suggested message:
   `release: v0.7.12 — skill extension protocol`.
3. **On the work machine:** `brew upgrade borg-collective && borg setup`. Verify
   `~/.claude/skills/borg-plan/SKILL.md` now contains the three "Local Extensions" blocks
   (`grep -c "Local Extensions:" ~/.claude/skills/borg-plan/SKILL.md` should return `3`).
4. **Write the JIRA extension** at
   `~/.config/borg/extensions/skill-extensions/borg-plan/01-context.md`. Content (rough):
   "Ask the developer for the JIRA ticket key. Run `acli jira workitem view <KEY>` (already
   installed on this machine — confirm path with `which acli`). Use the ticket title +
   description + acceptance criteria as the source material for the planning conversation."
5. **Run `/borg-plan` against a real ticket** in a real work repo. This satisfies acceptance
   criterion #4 — the protocol's only live validation.
6. **Then run `/borg-assimilate` PR-merge flow** for PR #20 itself to dogfood the assimilate
   load points (will write a `02-output` extension to test, then remove it).

Once #4–#5 confirm the protocol works end-to-end, the project ships.
