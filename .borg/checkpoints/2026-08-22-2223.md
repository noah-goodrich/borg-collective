**tl;dr** — The entire infoviz/program-manifests stack merged to main tonight (four-round blind review,
findings 13→8→5→0) and the personal-machine smoke sweep is 7/7 green — the declared 4-repo program renders as
one workstream on live data. Next: work machine's smoke half + its stamps on noah-goodrich/borg-collective#161
and #163, then S4/S1/deriver.

## 1. Goal

Drive noah-goodrich/borg-collective#158/#159/#160/#162 (+#161/#163) through cross-machine approval, blind
different-model review, merge, deploy, and e2e smoke, per Noah's mandate.

## 2. Accomplished

- **Merged to main (7a48a27)**: noah-goodrich/borg-collective#159 (spine regrouping fix + comms program +
  review), #158 (programs.py + PM6 writer + coordinator; backfill extracted to `hold/backfill-rework`),
  #160 (cross-source dedup + offset-aware recency), #162 (infoviz status).
- **Blind gate cleared**: opus, 4 rounds, 13→8→5→0; I authored fixes through the rounds (timefmt relocation,
  doctor gh-mock hermeticity on #163, head-state corrections) and verified every work-machine claim before
  each of my 11 STACK-APPROVAL stamps.
- **Deployed**: main pulled, `borg setup` redeploy, gh 2.96.0→2.98.0 (SA1; CVE-2026-64652 cleared).
- **Smoke 7/7 green** (posted to the noah-goodrich/borg-collective#159 hub): `borg program list/plan` (PM9
  path), live recon (83 items), gather (14 declared edges, regrouping=10, unmapped_gates=3, out-of-window=7
  loud), spine (**ingle-t1-cutover: 6 items / 4 repos / ONE workstream**), chains.md render, `borg link`
  regression.
- Watcher (`bmhdy7z9f`) live all session; smoke manifests placed at `.borg/programs/` (carve-out verified
  committable).

## 3. Ready to Commit

- `skills/borg-link-up/SKILL.md` (tracked mod, this template's tl;dr mandate) — commit after
  noah-goodrich/borg-collective#163 merges; it edits the same file. /simplify not needed (docs).
- `.borg/programs/viz-program.json` (+ `ingle-t1-cutover.json` in stillpoint) — S4's PR.
- Checkpoint backlog (12+ untracked files) — standing drift item.

## 4. Blockers

- noah-goodrich/borg-collective#161 @ 3b1d0c5 and #163 @ d00a859: my stamps posted; work machine's pending —
  then they merge.
- Work machine's smoke half (coordinator sync + adapters) not yet reported.

## 5. Next Session

Read the noah-goodrich/borg-collective#159 hub first — the work machine's smoke results and #161/#163 stamps
land there (watcher will have caught them). Then: (1) merge #161/#163 once stamped, reconcile + commit
`skills/borg-link-up/SKILL.md`; (2) file S4's PR landing the two manifests in their owning repos; (3) start
S1 `borg show` (spec incl. AC1b and `$TMUX_PANE` resolution in
`docs/plans/directives/2026-08-20-comms-delivery-surfaces.md`) and deriver D4 (recon relative-`--since`,
`borg_core/recon/core.py:resolve_since`); (4) fold the opus NITs (`cmd_program` via `_borg_py` et al.) into
the next `borg.zsh` change.
