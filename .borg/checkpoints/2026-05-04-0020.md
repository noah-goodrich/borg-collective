## 1. Goal

Ship ingle-site marketing site scaffold at ingle.kitchen, correctly using devcontainers
for all npm/build work (not the host). Also: continue from the previous session's reveal,
ingle, and borg-collective orchestration work.

## 2. Accomplished

**ingle-site (ingle.kitchen) — SHIPPED**
- `.devcontainer/` written (Dockerfile, docker-compose.yml, devcontainer.json) mirroring
  reveal-site pattern. Port 4322 (4321 was taken).
- Registered with borg, started with `drone up ingle-site`.
- `npm install` now runs inside the container via `postCreateCommand` — no host pollution.
- Tailwind v4 + `@tailwindcss/vite` (correct for Astro 6; `@astrojs/tailwind@5` requires Astro ≤5).
- All source files written: `src/styles/tokens.css` (brand palette matching ingle PWA),
  `src/layouts/Base.astro`, `src/components/{HearthIcon,Header,Footer}.astro`,
  `src/pages/index.astro` (landing: HearthIcon + "The household grocery brain." + CTA),
  `src/pages/privacy.astro` (plain-English privacy policy).
- Build verified inside container: 2 pages, clean output, 970ms.
- GitHub repo `stillpoint-labs/ingle-site` created (private).
- CF Pages secrets set (CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID).
- Deploy workflow fixed: wrangler-action@v3 doesn't reliably pass `apiToken`; switched to
  `npx wrangler pages deploy` with `env:` block (matches ingle's deploy-pwa.yml pattern).
- GitHub Actions deploy passed (40s). Site is live at ingle.kitchen via CF Pages `ingle-holding`.
- `2026-05-03-marketing-site.md` directive assimilated in ingle repo.

**Feedback saved to memory**
- `feedback_use_devcontainer.md`: never run npm/build on host; always `drone exec`.

## 3. Ready to Commit

**borg-collective**: Untracked checkpoint files only.
```
git add .borg/checkpoints/ && git commit -m "chore: session checkpoint 2026-05-04"
```

**ingle-site**: All committed and pushed (eacd3ac, 2b8a09e). Clean.

**ingle**: `fff5eaf` committed (assimilated directive). Needs push.

## 4. Blockers

- **node_modules on host**: `/Users/noah/dev/ingle-site/node_modules` exists from the bad
  pre-devcontainer npm install. bash-guard blocks `rm -rf` on home subdirectories. Not urgent
  (`.gitignore` covers it) but worth cleaning: `! rm -rf /Users/noah/dev/ingle-site/node_modules`.
- **Reveal-site domain**: Workers deploy is live but domain may still point at old Pages project.
  User action needed in Cloudflare dashboard (see previous checkpoint).
- **Cairn**: Still not in PATH.
- **Borg agentic directive**: Written but not activated as a plan.
- **Reveal worker**: ~199 photos were pending as of last checkpoint; check if queue drained.

## 5. Next Session

1. **Check ingle.kitchen live** — verify brand renders correctly (Lora, ember button, hearth icon).
2. **Clean host node_modules**: `! rm -rf /Users/noah/dev/ingle-site/node_modules`
3. **Push ingle**: `git -C /Users/noah/dev/ingle push`
4. **Reveal worker drain**: `flyctl status --app reveal-worker` — if queue empty, scale back to 1.
5. **Borg agentic plan**: Convert `docs/plans/directives/2026-05-03-agentic-orchestrator.md`
   into an active `PROJECT_PLAN.md` (or run /borg-plan on it).
6. **ingle-site: holding page cleanup** — delete `holding/` from the main ingle repo once DNS
   confirmed pointing at ingle-site's CF Pages deploy.
