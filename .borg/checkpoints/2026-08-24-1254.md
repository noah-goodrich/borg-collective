**tl;dr** — Install audit closed (all 3 gaps, Gap 3 landed in the Noah-corrected direction: plugin owns
skills+agents+hooks) and S4/K3/AC3 landed test-first: 8 CI tests + 5 evals green before anything committed,
with `/pr-description` proven live reading a manifest. Next: the manifest-#3 experiment is armed; S1
`borg show` and the deriver are the build queue.

## 1. Goal

Close the three install-audit gaps, then build the eval/test gate for S4 + K3/AC3 and land the program
manifests only on full green, per Noah's test-first rule.

## 2. Accomplished

- **Install audit closed.** Gap 1: `scripts/build-plugin.sh` synced only one hardcoded agent file — now
  sweeps `agents/*.md`; ROUTING.md updated to Opus 5 (committed 223a1b5). Gap 2: code-governance understood +
  REVIVE call made; execution delegated to borg:5 (plugin.json, cairn excision, enabledPlugins, build-time
  manifest guard). Gap 3: **direction reversed by Noah** per the ratified 80/20 split
  (`docs/plans/assimilated/2026-06-08-mechanism-layer-extraction-plugin-80-20-split.md`, PR
  noah-goodrich/borg-collective#41) — plugin owns skills+agents+hooks; `borg.zsh` copy loops replaced with an
  idempotent migration sweep (f7ba070; 17 legacy copies removed, `ducky/` untouched; 700 bats + 767 pytest
  green; unprefixed agent registrations dropped live, confirming).
- **S4 landed test-first** (borg-collective 638b7c4, stillpoint 90c6f63): both manifests committed, gated by
  `merge-tree/test_s4_manifests.py` (8 CI tests) + `evals/s4-k3/run.sh` (5 evals) all green first. The eval
  gate caught a real harness bug on round 1 (macOS lacks `timeout`; model evals silently never ran).
- **K3/AC3 proven, not just built**: live headless `/pr-description` on stillpoint-labs/stillpoint#48
  rendered chain position FROM the manifest (program/lane/order-1-next/gate→Blocks); troth run proved the
  fallback discriminator. Evidence at `evals/s4-k3/out/`; borg:5 notified to check its AC3.
- **Program lifecycle ratified** in `docs/plans/directives/2026-08-20-comms-delivery-surfaces.md`: naming
  (id = directive slug), origination hooks (borg-plan scaffolds, /pr-description maintains — Noah-approved),
  derived done, propose-then-human archive. S4/AC4 checked with evidence.
- **noah-goodrich/borg-collective#161 merged / #163 closed-superseded** (consolidated security line,
  both-machine stamps); post-merge deploy done; infoviz reading page shipped to Noah's phone (artifact
  `80487243`).
- Recovery note: stillpoint manifest briefly committed to a checked-out WIP branch — cherry-picked to main,
  branch restored byte-identical.

## 3. Ready to Commit

Nothing — all work is committed and pushed (borg-collective @ 638b7c4, stillpoint main @ 90c6f63). One
manual cleanup for Noah: `git -C /Users/noah/dev/stillpoint push origin --delete
feat/ingle-reconcile-meal-entries` (my accidental publish of a local WIP branch; classifier-blocked for me,
correctly).

## 4. Blockers

- borg:5's code-governance revive (Gap 2 execution) in flight, unconfirmed.
- Manifest-#3 experiment has no trigger until the next cross-repo plan or PR arrives.

## 5. Next Session

Start with `borg link` — the fresh session doubles as the Gap 3 verification (skills should register once,
prefixed; SessionStart hooks firing = plugin channel healthy). Then the build queue in order: (1) S1
`borg show` (`docs/plans/directives/2026-08-20-comms-delivery-surfaces.md` S1 — `$TMUX_PANE` resolution,
AC1b two-window bats); (2) deriver D4 recon relative-`--since` fix
(`borg_core/recon/core.py:resolve_since`); (3) deriver D1–D3 build; (4) fold opus NITs (`cmd_program` →
`_borg_py`) into the next `borg.zsh` touch. Infoviz Track 6 reading gates S2 productization; Noah's own
reading + human comprehension checks remain open on the phone page.
