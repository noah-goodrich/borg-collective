**tl;dr** — noah-goodrich/borg-collective#222 merged and turned #220 DIRTY; rebased onto `209a95a`, the manifest row
rode the rename into `.borg/chains/`, head is `282043d` with CI running again. Next: the other machine's stamp at the
current head, or round 2. Ontra-ai/ai-data-engineer#48 unchanged, waiting on a data-eng review.

## 1. Goal

Noah said "check again" on my two PRs eighteen minutes after the round-1 push.

## 2. Accomplished

- **#220 was DIRTY.** #222 (`.borg/programs` → `.borg/chains`) merged at `209a95a`. `git rebase origin/main` went
  through with zero conflicts; rename detection carried the #220 row to `.borg/chains/viz-program.json` and no
  `.borg/programs/` survives on the branch. Round-1 fix commit is now `007efdb`; head `282043d`.
- **Suites on the rebased head:** pytest 1262 passed at 93% coverage; lint 10.00/10; bats 880 ok, 0 not ok. Both
  counts grew by what #222 added.
- Force-pushed with lease. PR body refreshed (chains path, base SHA, counts). One-line comment posted saying the head
  moved and why, so the reviewer knows the `cdd06dd` review is void by SHA but the content is unchanged.
- **Diagnosed the missing CI.** No `pull_request` run fired for `cdd06dd` or `e555d4f` because GitHub skips
  `pull_request` workflows on a PR with no merge commit, and the PR went DIRTY the moment #222 landed. After the
  rebased push, checks are running: lint and viz pass, test / python / contract-macos pending at the time of writing.
- **#48:** green, `REVIEW_REQUIRED`, zero reviews, zero comments, even with main. Nothing to do.

## 3. Ready to Commit

Only this checkpoint, committed and pushed after it is written.

## 4. Blockers

No blockers. Waiting on: the other machine's stamp at `282043d` (or its successor if this checkpoint commit lands
first — the stamp SHA is whatever head it reads), and a data-eng approval on #48.

## 5. Next Session

1. `gh pr view 220 --comments`; `gh pr checks 220`. If a stamp arrives, do not touch the head afterwards.
2. #223 is `UNSTABLE` on main's side and shares nothing with #220; #226 shares nothing either. No rebase expected
   unless something else merges.
3. The follow-up directive to file, not now: wire `borg reconcile` (a `borg_core/reconcile/cli.py`, a `reconcile)`
   arm in `borg.zsh`, `borg help` line, `cli_contract.bats` case).
