## 1. Goal

Orchestration session: drive reveal three-directive sweep (Blocks 1–5) to completion, import 13
experiment photos into prod Supabase, plan ingle-auth PWA, monitor reveal-site SSR deploy and
ingle JWT work.

## 2. Accomplished

**Reveal (app.reveal.photo) — THREE-DIRECTIVE SWEEP COMPLETE**
- Block 1 ✓ — User submission context (Python pipeline fixes)
- Block 2 ✓ — Migration + forced_archetype pipeline
- Block 3 ◼ — Example image generation (running in background, not blocking)
- Block 4 ✓ — TS/UI sweep: ArchetypePicker, RenderHistoryStrip, RenderHistoryLightbox, VoteFlow,
  SubmitForm all updated. rootedIn field wired. Photographer surnames removed from all UI.
- Block 5 ✓ — Worker v20 deployed to Fly.io. 16/16 Supabase migrations applied. All 9 smoke
  assertions passed (Gemini 503 retry added 7min delay; smoke timeout bumped to 900s).
- 13 experiment photos imported to prod Supabase (19 total complete plates). SHA256 dedup added
  to import_experiment_plates.py. Committed.
- 1 DB row fixed: burtynsky artist_statement had a photographer surname leak.
- CLAUDE.md clarified: supabase-py SDK scripts run via `drone exec`, not host .venv.
- /borg-assimilate running at session end.

**Reveal-site (reveal.photo)**
- SSR bug fixed: `output: 'static'` → `output: 'hybrid'` (commit f50c050). CF Pages was serving
  a stale build; `prerender=false` was silently ignored. Pushed — CF Pages rebuilding.
- Logo (logo-mark-nav.png), archetype example JPEGs, and gallery will appear after deploy.

**Ingle (mcp.ingle.kitchen) — COMPLETE**
- Supabase JWT extended to 604800s (7-day platform max).
- `ingle-refresh-jwt` shell function added to docs/INSTALL_CLAUDE_DESKTOP.md (ab8fe07).
- **ingle-auth PWA planned**: PROJECT_PLAN.md written. Single `/install` page at
  `app.ingle.kitchen`. Auth via Supabase magic link → pre-filled `claude mcp add ingle` command
  with JWT, one-click copy. ~3hr implementation. Step 1: validate `npm run pages:build`.
- Upgrade path noted: implement OAuth 2.1 on the Worker for native `claude mcp add` auth (zero
  copy-paste, same as Notion/Jira MCPs).

**Snowfort** — standing by, awaiting user "apply the plan" decision.

## 3. Ready to Commit

**borg-collective**: Untracked checkpoint files + decisions log. No code changes.
```
git add .borg/checkpoints/ .borg/decisions/
git commit -m "chore: session checkpoints + decision log 2026-05-01/02"
```

**reveal-site**: 4fc420d pushed (Workers deploy). One manual Cloudflare step pending (see Blockers).

**reveal**: All pushed — e27c8ec (CLAUDE.md), 2296c55 (smoke timeout), 4431854 (Block 4+5). Clean.

## 4. Blockers

- **Reveal-site domain migration (USER ACTION)**: Worker deployed successfully but domain still
  points at old Pages project. In Cloudflare dashboard:
  1. Workers & Pages → find Worker `reveal-site`
  2. Pages `reveal-site` → Settings → Custom Domains → Remove `reveal.photo`
  3. Worker `reveal-site` → Settings → Triggers → Custom Domains → Add `reveal.photo`
  4. Optional: add `LOOPS_API_KEY` encrypted env var on the Worker
  Once domain propagates, logo + /seen gallery + SSR homepage all go live.
- **Reveal Block 3**: Example image generation still running in background (non-blocking).
- **Snowfort**: 13 APPLY NOW items waiting on user. MFA, password policy, network policy,
  AUTO_SUSPEND. Say "apply the plan" in Cortex session (%40).
- **Reveal next directives**: google-oauth + vertex-ai-migration gate on GCP setup (user action).

## 5. Next Session

1. **Confirm reveal-site**: Check reveal.photo — logo should be in nav, gallery should show 19
   plates, SSR pages should be live.
2. **Snowfort**: Say "apply the plan" in Cortex session (%40) — 13 APPLY NOW items.
3. **Reveal /borg-assimilate**: Verify it completed, check the assimilated plan file.
4. **Ingle-auth PWA**: Implement PROJECT_PLAN.md — start with `npm run pages:build` validation
   before writing any code. ~3hr session.
5. **Scoring granularity**: Add `rubric_score NUMERIC(4,2)` to triage_results, populate from
   scoring.py rubric output. Current 0-5 integers insufficient to rank top images.
