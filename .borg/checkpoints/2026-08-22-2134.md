**tl;dr** — The whole infoviz/program-manifests stack merged to main tonight after a four-round blind review
(findings 13→8→5→0) and my post-merge smoke sweep is all green: the declared 4-repo program renders as one
workstream on live data. Remaining: work machine's smoke half + its stamps on #161/#163.

## 1. Goal

Drive the PR stack (noah-goodrich/borg-collective#158/#159/#160/#162 + #161/#163) to full cross-machine
approval, blind-model approval, merge, deploy, and e2e smoke — per Noah's standing mandate.

## 2. Accomplished

- **Merged to main**: noah-goodrich/borg-collective#159 (rider: spine regrouping fix + comms program +
  independent review), #158 (programs.py + PM6 writer + PM6/PM7 coordinator, backfill extracted to
  hold/backfill-rework), #160 (recon cross-source dedup + offset-aware recency), #162 (infoviz status).
  Main is at 7a48a27.
- **Coordination protocol proven**: STACK-APPROVAL comment stamps pinned to SHAs (same-account blocks formal
  reviews), void-on-push, blind opus gate over four rounds. I authored/verified fixes through the rounds:
  timefmt relocation for the demeter lint, doctor gh-mock hermeticity (#163), head-state corrections.
- **Deployed here**: pulled main, borg setup redeploy, gh 2.96.0→2.98.0 (SA1, clears CVE-2026-64652).
- **Smoke sweep all green** (posted to the #159 hub): borg program list/plan (PM9 path), live recon (83
  items), gather (14 declared edges, regrouping, unmapped_gates=3, out-of-window=7 counted loudly), spine
  (**ingle-t1-cutover: 6 items / 4 repos / ONE workstream**), chains.md render, borg link regression.
- Smoke manifests placed locally at .borg/programs/ (carve-out verified committable); K1 design-doc skill
  shipped by borg:5 mid-smoke.

## 3. Ready to Commit

- `skills/borg-link-up/SKILL.md` (tracked mod): my checkpoint-tl;dr mandate — commit after
  noah-goodrich/borg-collective#163 merges (it edits the same file; reconcile then, one PR).
- `.borg/programs/viz-program.json` here + `ingle-t1-cutover.json` in stillpoint: S4's PR when it runs.
- Checkpoint backlog (12+ untracked .borg/checkpoints files) — standing drift item.

## 4. Blockers

- noah-goodrich/borg-collective#161 @ 3b1d0c5 and #163 @ d00a859 await the WORK machine's stamps (mine are
  posted); then they merge.
- Work machine's smoke half (coordinator sync against its shim + adapters) not yet reported.

## 5. Next Session

Read the #159 hub thread first — the work machine's smoke results and #161/#163 stamps land there. Then:
1. Merge #161/#163 if stamped (or nudge); commit the borg-link-up skill reconcile after #163.
2. File S4's PR (land the two manifests in their owning repos) — also the will-anyone-write-manifest-#3 test.
3. Start S1 (`borg show`) from the frozen spec (AC1b two-window bats; $TMUX_PANE resolution) and the
   deriver directive's D4 recon-since fix.
4. Opus residual NITs (cmd_program via _borg_py, plan CAVEAT capsys, bats title) fold into the next
   borg.zsh change.
