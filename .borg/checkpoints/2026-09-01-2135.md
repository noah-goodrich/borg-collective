**tl;dr** — Session-closing checkpoint. AC7's expand phase shipped in twelve commits on PR
[noah-goodrich/borg-collective#186](https://github.com/noah-goodrich/borg-collective/pull/186), all gates green. Next
session starts at E3 (refusal semantics), then the 57-ref rewrite, then the two repoints.

## 1. Goal

Move AC5/AC6/AC7 of the One Front Door plan forward from "what's next?" — which resolved into AC7's merge-tree
retirement, then twice widened when validation turned out to be a design question and chains turned out to need more
than GitHub.

## 2. Accomplished

**Twelve commits on `feat/ac7-manifest-writer-port`.** `make lint` rc=0 · `make test` 1004 passed, 99% vs the 90
floor · `make test-viz` 344 passed, 95% vs 85 · `bats tests/` 774 ok, 0 not ok.

- **`34a11e2`** the manifest writer — the package's first write path (all nine prior functions read). Validates,
  refuses whole, writes nothing. No `program` backfill, all `_`-keys stripped via a shared `core.declared_body`, and
  `mkstemp` over a racing fixed `.tmp`. Suggests but never repairs a shorthand ref.
- **`ad571b4`** the typed ref vocabulary — `github` | `jira` | `link` | `""`. Chains can hold a ticket and a doc, not
  only PRs. Typed, not loosened: every pattern anchored, five near-miss negatives.
- **`41b6143`** + **`7093a9c`** two seam splits: `refs.py` and `errors.py`, each deleting a `too-many-lines`
  suppression rather than carrying it.
- **`d7767ee`** a fourth `▸ CHAINS` diagnosis — a refused manifest no longer renders as "no manifests in the
  registry".
- **`7b12dd7`** `test_proc`'s wall-clock ceiling replaced with sibling-visibility. Threshold deleted, not widened.
- **`fff42d4`** + **`9d151ce`** Architecture Rule: expand → migrate → contract, plus the correction that you migrate
  only what the *surviving* validator rejects.
- **`22c41ca`** + **`819c029`** two directives filed (structured storage; reverse the 08-27 row-salvage decision).
- **`7093a9c`** the `/simplify` pass, which caught two defects: the tracked/reference split was enforced nowhere, and
  enforcing it needed **three** predicates because `not is_tracked` also skips unknown-kind refs and would have
  stopped a malformed `after` from wedging its row.
- **`uv.lock` deleted** after eight checkpoints of "track or delete" — CI installs via `pip install --group dev`.

**Supersedes `.borg/checkpoints/2026-09-01-1711.md`**, the mid-session checkpoint (written before `7093a9c`, amended
after). Prefer this file where they differ.

## 3. Ready to Commit

Nothing. Tree clean, all twelve commits pushed. `/simplify` ran as `7093a9c` — four agents over the whole diff; don't
re-run it on this branch expecting findings.

## 4. Blockers

No blockers. Three carried forward:

- **PR #186 is unreviewed** at twelve commits. If review stalls, split the two directives and the Architecture Rule
  into a docs-only PR that merges immediately, leaving the code behind it.
- **D3 is settled but unimplemented** — `shell.py`'s `_drop_invalid_rows` is still live.
- **A trap in code written this session:** `shell.refused_manifest_paths` deliberately excludes the salvage arm, so
  the obvious cleanup of `coordinator.py:398`'s `".json" in w` sniff re-opens the row deletion.

## 5. Next Session

Order is fixed by expand → migrate → contract. Expand is **done**.

1. **E3, not M1** — refusal semantics from
   `docs/plans/directives/2026-09-01-refuse-the-manifest-stop-salvaging-rows.md`. Write the acceptance test first; it
   must be RED against today's tree (measured: 3 rows in, 2 written, `warnings == []`).
2. **M1** — rewrite all **57** shorthand ref occurrences in `merge-tree/test_coordinator.py` (53 lines; `r#1`×47,
   `r#2`×4, `z#1`×2, `r#3`×2, `a#1`×2). **Whole-file, uniform owner (`o/`), never targeted at `_row(...)`** — 41 of
   the 57 are join keys, so a targeted rewrite turns joins into silent no-matches. Verify against the
   *unmodified* `programs.py`: 49/49 must pass.
3. **NEVER run that rewrite outside `test_coordinator.py`.** The 14 shorthand literals in
   `borg_core/manifest/test_core.py` and `test_shell.py` are negative guards; a blanket sed leaves them green and
   asserting nothing.
4. **C1/C2** — repoint `coordinator.py` and `gather.py`, each one commit, never split. A simulated repoint measured
   12 failed / 37 passed of 49. One of the 12 *passes* afterward while testing an unreachable state: its helper calls
   `discover` once per directory, `cmd_sync` calls it once with the whole list, and `shell.discover` dedups on content
   identity with zero warnings. Write a batched-discover test first.
5. **`merge-tree/test_s4_manifests.py` is a fifth importer** the directive never names, and the only test running a
   validator against committed on-disk manifests.

**Process:** run mutations **one at a time** — a batched loop timed out mid-run and left a mutation in the working
tree.

**Pre-existing flake, not branch damage:** `test_deep_is_accepted_and_ignored` renders the page twice and compares
while `relative_activity` is clock-derived. Passes 3/3 alone.

**Open decision:** AC6's `make eval --skip-model` is not a valid make invocation (rc=2), and two of its three
"deterministic" cases need live GitHub — so "deterministic cases green in CI" has zero members.

No external-origin text is quoted in this checkpoint beyond the PR number.
