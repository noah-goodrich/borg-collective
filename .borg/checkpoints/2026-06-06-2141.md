## 1. Goal

Started as "what's next" and expanded to: fix the borg `next`/capacity session-tracking bugs, ship the meta-research
next-gen evolution of the research-tools plugin (deep-research + brainstorm, directives 01–06), and produce EPUB briefs
of all recent research + reveal go-to-market materials for offline review.

## 2. Accomplished

**borg-collective (branch `fix/borg-next-reaper`, PR #39):**
- Bug A: `cmd_next` + `_borg_print_briefing` now read `borg_registry_with_state` (was reading the raw, post-v0.8.0
  vestigial registry).
- Bug B: durable reaper — active/waiting treated as idle when no live tmux window AND `last_activity` older than
  `BORG_REAP_STALE_HOURS` (default 12); new `borg reap` command persists; dual-defined in `lib/registry.zsh` +
  `lib/borg-hooks.sh`; `borg-link-down.sh` capacity scan matches the CLI; `borg-next`/`borg-link` skills updated.
- Deployed the two copied hook files to `~/.claude` (CLI + lib/*.zsh already run from source).

**claude-plugins research-tools:**
- Directive 01 fail-closed ground gate — PR #7 (branch `meta-research-review`) + the meta-review corpus/6 directives.
- Directives 02–06 implemented, each adversarially verified, no Dir 01 regression — PR #8 (branch `directives-02-06`,
  stacked on #7). Plus fixes for the two Dir-01 hook bugs that surfaced on real plugin load (hook schema wrap +
  global-Stop-hook blast-radius scope guard) and **transcript-detect arming** (`afee9e9`) so the gate auto-fires only
  on a real `/deep-research` run.

**EPUBs (11 in `~/Documents/Claude/`, all valid):** 8 research deliverables, the full meta-review report, the
consolidated `reveal-founder-gtm-brand` bundle (12 chapters), and the `meta-research-brief`.

**Memories added:** `project_borg_deploy_topology`, `reference_cc_plugin_hooks_authoring`.

## 3. Ready to Commit

Nothing uncommitted. Both working trees are clean; all work is committed and pushed across PRs #39, #7, #8.
`/simplify` is effectively covered — every code change went through an adversarial verifier this session, and the
deliverables are bash/markdown/skill files rather than app code. No pending changed files to review.

## 4. Blockers

No blockers. The one open design item (gate arming) was resolved this session via transcript-detect. All three PRs are
pushed and awaiting review/merge.

## 5. Next Session

1. **Merge the claude-plugins stack in order:** PR #7 (Dir 01 + corpus) → PR #8 (02–06 + hook fixes + arming), or
   consolidate into one PR if preferred. Then redeploy the research-tools plugin, `/reload-plugins`, confirm `/doctor`
   is clean, and smoke-test that the gate actually fires on a real `/deep-research` run (transcript-detect arming).
2. **Merge borg-collective PR #39** (reaper); `borg setup` is a no-op now (hook libs already synced this session), but
   re-run it on any other machine. After merge, switch borg-collective off `fix/borg-next-reaper` back to `main`.
3. Optional: review the 11 EPUBs in `~/Documents/Claude/` at leisure.
