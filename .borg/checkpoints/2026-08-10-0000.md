## 1. Goal
Update the work-machine setup guide to reflect recent changes and ensure all borg-collective work is merged
to main and pushed to GitHub so the work machine can pull it.

## 2. Accomplished
- **Merged PR #106** (infoviz learning-program docs import — 40 md + 2 html files, all CI checks green).
- **Updated `docs/work-machine-setup.md`**: added a "What's new" entry documenting the 2026-08-08 cairn
  decommission (DB/service/hooks/CLI removal, corpus now in per-project `.borg/knowledge/*.md`), the
  stale-Stop-hook-after-pull gotcha (a session started before the decommission commit landed can still
  reference the deleted `borg-cairn-heartbeat.sh` — harmless, clears on Claude Code restart, reproduced live
  this session), and the `borg sever` shared-Supabase-stop fix (#110). Marked the old #94 cairn-heartbeat
  entry as superseded rather than deleting it (keeps the changelog trail honest).
- **Committed the stray `.borg/checkpoints/2026-08-08-2201.md`** that was left untracked from the prior
  session.
- **Rebased and pushed to `origin/main`** (`44983f7`) — local main is clean and fully in sync with origin.

## 3. Ready to Commit
Nothing — working tree is clean on `main`, 0 ahead / 0 behind origin. `/simplify` was not needed: the only
change was a documentation edit (no code), plus a straight `git add` of an already-authored checkpoint file.

## 4. Blockers
No blockers.

## 5. Next Session
1. **PR #104** (PR-control-hub renderer graduation, closes #97) is still a **draft**, ~2176 lines across 8
   files. Its own PR description references cairn ("renders fully with cairn down") — confirm that assumption
   still holds post-decommission before readying it. Recommend `/borg-verify` once readied, then
   `gh pr ready 104` → review → merge.
2. **dotfiles repo has untracked functional scripts** (`~/.config/dotfiles/zsh/bin/ducky`,
   `~/.config/dotfiles/zsh/ducky.zsh`) that appear to conflict with the standing "dotfiles = config only, no
   functional scripts" rule. Flagged but not acted on — ask Noah whether these should move into a project
   repo or whether the rule has an exception for the `ducky` skill.
3. **Decommission directive forward work** (unchanged from last checkpoint,
   `docs/plans/directives/2026-08-08-cairn-decommission-and-unconditional-block.md`): Phase 1.6 (one
   unattended verdict gate) and Phase 3 (hookify lifecycle layer + hard session-length mechanism) are still
   genuinely open.
4. **#98** checkpoint-freshness clock-skew bug + **#99** clock-skew check — still cluster naturally into one
   session (carried over, untouched this session).
