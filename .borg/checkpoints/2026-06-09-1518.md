## 1. Goal
Ship the borg-collective session — assimilate the stale Research Gap Close plan, diagnose the cairn
write failures, and establish the mechanism-layer pattern (plugin = 80%, CLI = 20%) proven on the
reaper slice.

## 2. Accomplished

**Assimilation / bug fixes:**
- `tests/state.bats` — fixed 2 stale fixtures broken by reaper centralization; added `tests/reap.bats`
  (11 new cases covering `_borg_should_reap` + `borg_reap_overlay`)
- PR #39 merged → Research Gap Close plan archived to
  `docs/plans/assimilated/2026-05-27-research-gap-close-cairn-evidence-watch.md`
- PR #40 merged — `cairn status` (nonexistent subcommand) → `cairn health` in the failure nudge;
  `borg-link-up.sh` now captures cairn write stderr instead of discarding it
- Filed `docs/plans/directives/2026-06-06-reaper-utc-timezone-offset.md` (the `date -j -f` without
  `-u` bug)

**Mechanism-layer extraction (PR #41 — open, not yet merged):**
- `lib/reaper.sh` — new POSIX-compatible single home for `_borg_should_reap` +
  `BORG_REAP_STALE_HOURS`; collapses the triple-mirror across `lib/registry.zsh`,
  `lib/borg-hooks.sh`, and `borg-link/SKILL.md`
- `lib/registry.zsh` + `lib/borg-hooks.sh` — now source `reaper.sh`; duplicate predicate bodies
  removed
- `skills/borg-link/SKILL.md` — inline prose replaced with pointer to `lib/reaper.sh`
- `scripts/sync-plugin.sh` — new script to sync skills from source → plugin distribution; prevents
  hand-copy drift
- All 5 plan acceptance criteria verified; `bats tests/` 222/0, shellcheck clean

**In `claude-plugins` `directives-02-06` (committed, not pushed):**
- `hooks/notify.sh` — inlined `_borg_osa_notify`, removed `source $HOME/.claude/lib/borg-hooks.sh`
- `borg-link` + `borg-next` SKILL.md synced with source

## 3. Ready to Commit

`feat/mechanism-layer-reaper` branch has 2 commits — PR #41 open, ready to merge. Nothing
additional to stage in borg-collective.

`claude-plugins/directives-02-06` has 2 commits needing push + PR to that repo.

`PROJECT_PLAN.md` is the active plan for this work (untracked intentionally, not committed to feat
branch).

## 4. Blockers

No blockers. PR #41 is green and all criteria verified.

## 5. Next Session

**Start here:** merge PR #41 (`feat/mechanism-layer-reaper`) — green, all 5 criteria met. Then run
`/borg-assimilate` to archive `PROJECT_PLAN.md` (mechanism-layer plan, established 2026-06-08).

**After assimilation:** file 3–4 follow-on directives parented to the archived plan for the remaining
verbs: `scan/scoring` (reaper-scoring duplicated in `borg.zsh`), `cairn-client` (shared adapter
layer), and `search` (skill-callable vs CLI). These are the natural next slices of the 80/20
extraction.

**Don't forget:**
- `claude-plugins/directives-02-06` has 2 unpushed commits — push + PR when next in that project
- Ingle shopping smoke test is still outstanding (blocks Item Canonicalization)
