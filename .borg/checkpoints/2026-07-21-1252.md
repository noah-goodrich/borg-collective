## 1. Goal
Build the Usage Guardian Phase-2 **checkpoint-sweep mechanism** — deliver `/borg-link-up` to active Claude panes
before the 5-hour cap — shipping it default-OFF with the threshold as config (not hard-tuned), per the directive.

## 2. Accomplished
- **Locked the plan** (`PROJECT_PLAN.md`) via `/borg-plan` + a Collective Review: 6 verifiable acceptance criteria,
  scope boundaries excluding the 92% veto hook and live-cap validation.
- **Built the sweep in `bin/borg-usage-watch`** (TDD — RED→GREEN): `_claude_panes`, `_send_checkpoint` (two-step
  separate-Enter delivery), `_guardian_get`/`_guardian_set_swept` (idempotence state at
  `~/.config/borg/usage-guardian.json`), `_run_sweep`. Default-OFF master switch `BORG_USAGE_SWEEP_ENABLED`;
  threshold via `BORG_USAGE_CHECKPOINT_PCT` (default 85, inert); fail-safe per pane (reaper stance); halt>=92 is
  signal-only.
- **12 new bats** in `tests/usage_watch.bats` (replaced the blanket "no send-keys" test with a behavioural default-OFF
  test; added the directive-mandated plist-USER assertion). Full `usage_watch` suite green (34/34), shellcheck clean,
  fast pure-bash suites green (222/222).
- **Shipped as PR #88** — CI green (lint + test on ubuntu + macOS), `mergeStateStatus: CLEAN`. Directive updated with a
  dated "Sweep-mechanism result" section; `/simplify` run (no fixes needed).

## 3. Ready to Commit
Nothing pending — all work is committed on `feat/usage-guardian-sweep` (HEAD `207e3d0`) and pushed. `/simplify` already
run on the one code file (`bin/borg-usage-watch`); the rest are docs/tests. `PROJECT_PLAN.md`, the directive, the
script, and the test are all in the PR. Working tree clean.

## 4. Blockers
No blockers. (Note: the full local `bats tests/*.bats` run hangs on container/tmux-dependent suites in this stateful
host env — an environment quirk, not a code issue; CI runs the complete suite green on clean runners.)

## 5. Next Session
**PR #88 is green and awaiting a human merge** (left unmerged deliberately — first capability that types into live
panes). After merge, three things remain to fully close `docs/plans/directives/2026-07-08-usage-guardian-build.md`:
1. **Live-cap validation** — arm the sweep against a real 5-hour cap approach once, end-to-end (the directive's "Done
   when" clause). This is the gate before enabling in production.
2. **The `>=92%` dispatch hard-stop veto** — a separate `PreToolUse` hook reading guardian state (the sweep only writes
   the signal today).
3. **Arm the sweep** in the plist (`BORG_USAGE_SWEEP_ENABLED=1`) — only after (1) and once more near-cap episodes
   accrue to tune `BORG_USAGE_CHECKPOINT_PCT`. Ships OFF on purpose.
   Then assimilate: archive `PROJECT_PLAN.md` -> `docs/plans/assimilated/`.
