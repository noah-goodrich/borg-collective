**tl;dr** — Seed checkpoint written by the Queen, 2026-09-18 22:00 MDT. This worktree is the **shim drone**: it owns
[noah-goodrich/borg-collective#220](https://github.com/noah-goodrich/borg-collective/pull/220) (the employer-shim
and adapter layer plan, branch `plan/employer-shim-and-adapter-layer`) and nothing else. The One Front Door chain
(PROJECT_PLAN in the main tree, AC5, AC7, #221, #222) has its own drone in window `borg-collective`.

## 1. Where this came from

This worktree used to live at `~/.local/state/borg/worktrees/borg-collective/shim-adapter`; the Queen moved it to
`/Users/noahgoodrich/dev/borg-collective-shim` so `drone feature` could adopt it. Same branch, same commits. The
untracked `.venv` moved with it and may have stale absolute paths; recreate it if anything imports oddly.

The full record is the repo-level checkpoint
`/Users/noahgoodrich/dev/borg-collective/.borg/checkpoints/2026-09-18-1251.md`. Read it, take only the #220 items.

## 2. State of #220 at handoff

- Head `e2e4421`. Three of five criteria shipped (AC1 to AC3): CLAUDE.md names the shim layer's two tiers with a gate
  that can fail; `borg_core/reconcile/core.py` reports contradictions and writes nothing; resolution is
  adapter-driven with no source name on the path. 14 mutations killed.
- This branch replaces `PROJECT_PLAN.md`. That is why it must never be checked out in the shared tree: Step 0.75
  reported 0 children instead of 9 when it was.
- Opened early and deliberately incomplete so the other machine could see it.

## 3. Queue, in order

1. Check whether [#222](https://github.com/noah-goodrich/borg-collective/pull/222) (`.borg/programs` to
   `.borg/chains`) has merged. If yes, rebase this branch onto main and fix anything the rename breaks here. If not,
   do not wait on it; note it and continue.
2. Finish AC4 and AC5 of the shim plan as the branch's own PROJECT_PLAN defines them. Each criterion lands with its
   tests in the same commit, never trailing.
3. Keep the PR body's criteria table current after each push.

## 4. Do not touch

- The main tree `/Users/noahgoodrich/dev/borg-collective` (on `feat/short-window-names`, ahead 2), the `ac5-evals`
  worktree and #221, `#222`, `#223`, `#224`: those belong to the One Front Door drone.
- `docs/plans/directives/2026-08-31-project-to-repository-rename.md`: parked by design until One Front Door
  assimilates.

## 5. Standing facts

- Bash-tool rules from `~/.claude/CLAUDE.md` apply: `bash -c` for pipelines, `run-in` instead of `cd &&`, absolute
  paths, no `$()`.
- Finish every session with a checkpoint in this worktree's `.borg/checkpoints/`, #220-only.
