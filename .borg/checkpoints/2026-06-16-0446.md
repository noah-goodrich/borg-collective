## 1. Goal

Started as "watch ingle/troth and validate cairn is receiving/saving its data correctly," then expanded into an
overnight push to get **troth + ingle fully working in production by morning** (import flows + unblocked migrations),
**complete the cairn knowledge-graph backfill**, and **decide the Supabase shared-project architecture**.

## 2. Accomplished

**Cairn knowledge graph — validated + backfill completed (ship):**
- Adversarially validated the graph (embeddings 100% present/384-dim, completeness, no dup inflation) — found it sound;
  diagnosed the "ANTHROPIC_API_KEY expired" failure as a **context/wiring issue** (the key is valid inside the
  container; extraction had run without it in scope), not a bad credential.
- Hardened extraction: streaming + 4-retry + `max_tokens` 4096→16384 — committed `e70ff3b` on cairn branch
  `fix/backfill-extraction-robustness` (331 tests, 83% cov). **Not merged/pushed.**
- Ran the only-missing extraction sweep (153/154 files; 1 reveal-site file quarantined — deterministically malformed
  YAML), QA'd, normalized `stillpoint-labs`→`stillpointlabs-site`, **committed 1,535 new records** (121 idempotent
  skips), re-embedded (chunked 50/batch to beat an OOM). Graph ~doubled: **966 dec / 588 pat / 1,394 obs, 0 NULL
  embeddings**; semantic search verified on the new records.

**troth + ingle — deadline-critical, all merged + deployed + live:**
- **ingle PR #100** (merged `fd766e0`, deployed): fixed **Amazon CSV silently importing 0 orders** + the opaque
  multi-MB body-size failure + canonicalization hardening. 498 PWA tests green.
- **troth PR #18** (merged, deployed): import-UX fixes (quarantine state surfaced, empty-category dead-end guarded) +
  fixed a legitimately-broken `plaid-landing` crash-replay assertion (Sandbox non-determinism).
- **troth migration UNSTICK + live bug fix:** `db push` was blocked. Added 2 no-op placeholders for reveal's newest
  shared-ledger migrations (**PR #19**, merged), then applied troth's BLOCKED `20260615010001` to prod — it drops the
  partial plaid index → adds a plain UNIQUE constraint, which **fixed the live Plaid Link 500** (the partial index
  couldn't serve `ON CONFLICT`). `db push --dry-run` now "up to date". (ingle was never stuck — verified.)
- Verified ingle's morning meal-plan→grocery flow end-to-end against the **live MCP**.

**Supabase architecture (decided + researched, not executed):**
- Inventoried both projects: `main` (jmgjznimsrlcgyevefqm) is **shared** by reveal(public)/troth/stillpoint; `ingle`
  (muyxsjvgybpyaapccmaz) is its own clean project. Target = **one `stillpoint-platform` project, schema-per-app, shared
  `auth.users`**, reveal as a schema (not split).
- Verified via fact-checked research: a Supabase project as a **cross-project IdP is Team/Enterprise-gated** on Pro;
  WorkOS↔Supabase RLS is broken by open bug `supabase/auth#2476`; **no Alembic in prod** (Supabase is canonical).
- Captured a schema snapshot/backup: `~/dev/_db-snapshots-2026-06-16/main-shared-schema.sql`.
- Security: `item_aliases` (ingle) is an intentional RLS-free global catalog — do NOT bare-enable; `magic_link_rate_limit`
  (reveal) is safe to enable (SECURITY DEFINER RPC). Leaked-password protection + MFA are OFF on both (flagged).

## 3. Ready to Commit

Working trees for troth + ingle are clean (everything shipped via merged PRs). **One unmerged item:**

- **cairn** is on branch `fix/backfill-extraction-robustness` (`e70ff3b`) — committed, **not merged to main, not pushed.**
  This is the permanent fix for the extraction OOM/disconnect issue. `/simplify` was NOT run on it this session —
  review `src/cairn/boardroom.py` (streaming `call_anthropic`) + `src/cairn/cli.py` (`backfill` summary/exit) before
  merging.

Not in any repo (intentional artifacts): `~/dev/_db-snapshots-2026-06-16/` (schema dump) and cairn's gitignored
`backfill-input/_sweep/` candidate scratch (audit trail).

## 4. Blockers

No blockers to the morning work — troth + ingle are live and functional. Open items that need **Noah's decision/hands**
(not blockers):
- The **manual Plaid browser Link smoke** for troth (only Noah can do it — needs the browser + a session).
- The **consolidation direction** — "who owns the shared project's migrations going forward" — gates the full migration
  rebaseline.
- reveal's `supabase db push` is still blocked by the contaminated shared ledger (parked reconciliation directive);
  reveal isn't shipping schema, so low urgency.

## 5. Next Session

1. **troth Plaid final gate (Noah):** `drone exec troth -- npm run dev` → open **`/accounts/connect`** → Link
   **"First Platypus"** with **`user_good` / `pass_good`** → confirm accounts + transactions appear. The cloud
   migration prerequisite is already applied, so smoke against the deployed app works. Then the Plaid-sandbox plan is
   fully shipped → consider `/borg-assimilate` to archive `troth/PROJECT_PLAN.md`.
2. **Merge cairn `fix/backfill-extraction-robustness`** (`e70ff3b`) to main after a quick `/simplify` review.
3. **Supabase consolidation** — make the call on the target, then execute the **supervised** migration rebaseline using
   the snapshot at `~/dev/_db-snapshots-2026-06-16/main-shared-schema.sql` (baseline via `supabase db pull`, not blind
   repair). reveal's reconciliation directive (`reveal/docs/plans/directives/2026-06-15-migration-history-reconciliation.md`)
   is the playbook. Then fold ingle in (additive + cutover-gated).
4. **Precise security fixes** via proper migrations (not MCP onto the contaminated ledger): `item_aliases` permissive
   `SELECT` policy, `magic_link_rate_limit` RLS, enable leaked-password protection + TOTP MFA on both projects.
