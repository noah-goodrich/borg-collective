**tl;dr** — Measured before building, and the measurement inverted the plan: AC3's targeted fetch is free, while a serial
`gh` loop nobody was looking at was the real 12.6s. Landed S1 (context scoping) and the adapter rewrite (`borg recon`
12.6s → 1.93s); next session starts at S2, the manifest reader.

## 1. Goal

Start work on the approved `PROJECT_PLAN.md` ("One Front Door: `borg link` as the Derived-Fact Surface") by executing its
own stated first step — measure AC3's targeted-fetch cost before building anything.

## 2. Accomplished

- **Measured AC3 and retired the plan's top risk.** One batched `gh api graphql` call resolves all 14 declared refs of
  `stillpoint/.borg/programs/ingle-t1-cutover.json` in ~0.77s standalone, and **+0.087s marginal** when folded into the
  sweep query. Sequential `gh pr view` was 8.9s — that was the version that would have sunk the plan. Cost is flat at
  **1 rate-limit point** from 14 to 112 aliased nodes.
- **Found the real cost.** `lib/recon/adapters/recon-adapter-github:64` was a serial `while read` loop issuing one REST
  `gh pr list` per repository (line 82). 14 repos × ~0.9s = the 12.6s.
- **Ran a 7-surface mapping workflow + 3-lens blind adversarial review** of a first-pass AC1/AC2/AC3 design. Two lenses
  returned `unsound`; 12 blockers, three found independently by all three reviewers.
- **Filed `docs/plans/directives/2026-08-25-link-front-door-hardened-spec.md`** — the corrected design with every blocker
  bound to a mechanical test, parented to the active plan.
- **Landed S1 — context scoping.** New pure `core.scope_for()` (boundary-safe longest-prefix, positional dominates cwd);
  `shell.orchestrator_root()`, `cwd()`, `resolved_project_paths()` (realpath-normalized both sides); `--local` flag;
  additive `scope` document key with `DOCUMENT_VERSION` deliberately held at 2; `_borg_py` now exports
  `BORG_ORCHESTRATOR_ROOT`. All three hot call sites opt down: fzf preview (`borg.zsh:266`), `cmd_watch`'s 5s redraw
  (`borg.zsh:2222` — a third loop the reviewers missed), `drone status`'s per-window loop (`drone.zsh:964`).
- **Ran a 16-agent adversarial verification of S1.** 6 of 12 findings survived refutation. Fixed all: one real regression
  I introduced (`shell.cwd()`'s unguarded `os.getcwd()` killed `borg link` in all four modes from a deleted directory)
  and three of my own tests that passed without testing anything — including the one guarding the single wire S1 exists
  to land. **Mutation-tested every fix**: cut the `_borg_py` bridge → red; cut deep-arm forwarding → red; cut
  overview-arm forwarding → red; unguard `cwd()` → red.
- **Rewrote `recon-adapter-github` to one batched GraphQL call.** Adapter 12.72s → 1.73s; `borg recon` end-to-end
  **12.6s → 1.93s**; 15 `gh` invocations → 1. Equivalence proved against the live registry: same 13 repos, same 77 items,
  identical per-project counts. The two prelude calls are gone (`viewer { login }` rides along, `gh auth status` dropped).
- **Added `tests/recon_adapter_github.bats`** — the adapter previously had zero executable coverage, only a grep of one
  regex out of its source. 13 tests, all `gh`-mocked, including one that counts invocations and fails if the sweep
  regresses to N calls.
- **Hardened the adapter**: owner/name validated (not escaped) before GraphQL interpolation; `gh`'s non-zero exit with a
  populated `data` no longer discards a good sweep.

## 3. Ready to Commit

Changed and passing, but **`/simplify` has NOT been run this session — do that before committing.** Review these:

```
borg.zsh  drone.zsh  lib/recon/adapters/recon-adapter-github
borg_core/link/{cli,core,shell}.py
borg_core/link/test_{cli,core,shell}.py
tests/cli_contract.bats  tests/recon_adapter_github.bats  (new)
skills/borg-link/SKILL.md
docs/plans/directives/2026-08-25-link-front-door-hardened-spec.md  (new)
```

Green: 462 pytest / 97% coverage (floor 90) · `make lint` 10.00/10 · 720 bats, exit 0.

Two natural commits: S1 (scoping) and the adapter rewrite — they're independent.

## 4. Blockers

No blockers. Two open items that are decisions, not obstacles:

- `skills/borg-link/SKILL.md` changed, and since `f7ba070` skills ship via the plugin only — it needs
  `scripts/build-plugin.sh` to reach a live session. That writes to another repo, so it wasn't done unasked.
- One deliberate behavior change shipped in the adapter: the 30-item cap now keeps the **most recently updated** 30
  rather than an arbitrary 30. Documented in the adapter; flagging it because it is a real output difference.

## 5. Next Session

**Start at S2 — the manifest reader** (`borg_core/manifest/`, a new package). It is the last piece before the sweep can
fold into `link`.

Why a new package rather than an import: `merge-tree/programs.py` is unreachable from `borg_core` — the directory name
has a hyphen so it can never be a Python package, and `pyproject.toml:76` sets
`allowed_prefixes = ["borg_core", "__future__"]`, so importing it fails `make lint`. The read side gets **ported**, not
imported: `looks_like_manifest`, `validate`, `lanes`, `derive_edges`, `GATE_KINDS` from `merge-tree/programs.py:44-278`.

Then add the four new pure functions the hardened spec specifies: `declared_refs()`, `parse_ref()` (must NOT normalize —
`dedup_cross_source` keys on the exact `owner/repo#num` string), `levels()` (Kahn longest-path, rank = ROW index —
deliberately the transpose of `render_graph.py:659`'s JS `layout()`, which puts rank on X and is the wrong axis for
AC2's "time flows down"), and `ready_set()`.

Read §3 of `docs/plans/directives/2026-08-25-link-front-door-hardened-spec.md` first — B6 in particular. Manifest
discovery must glob **every** registered repository's `.borg/programs`, then filter by ref prefix. Scoping discovery to
the in-scope repository renders an empty grid for 3 of the 4 repositories in the only real multi-repo manifest, because
`ingle-t1-cutover.json` lives only under `stillpoint`.

Vocabulary stays ratified: *repository* / *project*. "Program" is retired — including in the new package's names, even
though the manifest files and their `program` key still use it (that's AC7's problem, not S2's).
