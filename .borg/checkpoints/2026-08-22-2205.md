## 1. Goal

Drive the infoviz/program-manifests PR stack (#158/#159/#160/#162) through cross-machine approval, an
independent different-model review, ordered merges into main with all gates, and post-merge e2e smoke — plus
implement the security-audit directive (SA1–SA6) along the way.

## 2. Accomplished

- **The full stack is merged to main.** Order: #159 → `feat/program-manifests` (`86983b5`), #158 → main
  (`b4ec080`), #160 → main (`b73010a`), #162 → main (`7a48a27`). Every merge gated on green CI + local suites;
  all four chain blocks re-stamped to merged.
- **Independent review loop (opus, blind, 4 rounds): 13 → 8 → 5 → 0 findings, VERDICT: APPROVED.** Notable
  fixes shipped through the loop: the coordinator cross-project AND same-project manifest-overwrite blocker
  (`write_manifest` gained `filename=`, sync rewrites in place), PM4's `unmapped_gates` gained its production
  caller (gather `meta.unmapped_gates`), curate preserves edge `source` (PM5 — provenance verified live in
  `data.json`), discovery names missing/unreadable dirs, non-dict rows fail validation, declared-vs-declared
  reversal attribution, **`borg program list|plan|sync` wired into borg.zsh** + 5 cli_contract bats,
  offset-aware dedup timestamps (relocated to `timefmt.iso_in_prose_to_utc` by the personal machine — verified
  sound) + `test_timefmt.py`, project-divergence contradictions, plan's stdout parse-artifact caveat, three
  zsh arg-crash guards (live-verified).
- **Backfill extracted** to `hold/backfill-rework` (`278c484`) per the agreed merge unit.
- **SA1–SA6 implemented as PR #163** (gh 2.96→2.98 + doctor version floors with CVE-named failures,
  bash-guard RO subcommand allowlists replacing the blanket gh/docker/git approvals — 123 bats,
  untrusted-quotation labeling at 3 points, cache-dir counter, stdin keychain write live-verified, exact pins)
  + the doctor no-docker crash fix.
- **Post-merge e2e smoke on main, all green**: borg_core 432/97%/mypy, merge-tree 335/96%, bats 277,
  live recon→gather→spine→curate→chains with the new features exercised, `borg program` all three actions
  live, existing CLI surface intact. Recorded on the hub
  ([#159 comment](https://github.com/noah-goodrich/borg-collective/pull/159#issuecomment-5384030659)).
- Cross-machine coordination worked as designed throughout: the personal machine's stamps, fixes
  (timefmt relocation, S1 mechanics), and re-verifications were all reviewed and incorporated here.

## 3. Ready to Commit

Nothing staged; all work is merged or pushed on its branch. Untracked: three checkpoint files and **`uv.lock`
(my test artifact — `rm` it or track deliberately)**. Currently on the throwaway `main-check` branch —
`git checkout main && git pull` to normalize. /simplify not run; if you want a pass, the densest new code is
`merge-tree/coordinator.py` (`sync_borg`) and `hooks/bash-guard.sh` (the SA2 allowlists, on #163).

## 4. Blockers

No blockers for the shipped stack. Open decisions that are yours: #161 (SA directive) and #163 (SA
implementation) await your review — #163 changes permission-prompt behavior for gh/docker/git, which you
should feel in daily use before merging.

## 5. Next Session

1. Review/merge **#161 then #163** (directive before implementation). After #163: `borg setup` to reinstall
   the changed hooks, and expect prompts on `gh pr create`/`git push` that were previously silent.
2. **Backfill rework** on `hold/backfill-rework`: findings 1–3 + `programs.edges_from`'s `order: "–"`
   inversion (all documented in the #159 review thread).
3. Round-4 residual NITs, chief: route `cmd_program` through `_borg_py` (borg.zsh ~2500) — the documented
   env-inheritance bug class; plus the CAVEAT capsys assertion and the fifth bats test's title.
4. One-line docs refresh: `docs/plans/directives/2026-08-18-program-manifests-stack.json` statuses → merged
   (the PR bodies are already re-stamped; the committed manifest lags).
5. S1+S4 split from comms-delivery-surfaces is now unblocked — the manifests dir carve-out and `borg program`
   are on main, so S4 (landing the two real manifests in their owning repos) is a quick win.

Saved to: `/Users/noahgoodrich/dev/borg-collective/.borg/checkpoints/2026-08-22-2205.md`
