# Evening log — 2026-09-16 16:58

Working autonomously on Noah's instruction: *"don't ask me for clearance just record a log of what
you do this evening and I'll review it in the morning."* Every action below is recorded whether it
worked or not, including things I chose NOT to do and why.

**Standing priority** (Noah, relayed via the peer session earlier today): *"gaps in our ci
automation pipeline that need sealing should take priority before we build more features."*

**Self-imposed limits for tonight**, since blanket clearance is not blanket judgement:
- I will merge only what is (a) stamped, (b) green, and (c) inside the stated CI-gaps priority.
- I will not merge my own unstamped work, force-push anyone else's branch, or touch
  `~/.config`, CLAUDE.md, or permissions.
- Anything irreversible or outside the priority gets logged as a recommendation, not done.

---

## Starting state 16:58

| PR | owner | head | state |
|---|---|---|---|
| #211 | peer | 82fa1a8 | UNSTABLE — new, awaiting my review |
| #210 | me | 0f7d5cf | fence fix pushed; peer's blocking finding addressed |
| #209 | peer | 0225563 | stamped by me; the CI-gaps work |
| #207 | me | 7047400 | rebased onto #206, base declared |
| #206 | me | 1ae5274 | stamped by peer, held on the CI-first call |

## 16:58 — reviewed and stamped #211 (peer's evidence pins)

**Did:** checked out the branch, ran `borg_core.planstate.cli` myself rather than reading the PR's
own output. Reproduced their table exactly — `checked 5, pass 4, fail 1, unknown 2, would_flip 0`.
Verified all five pinned artifacts: four exist, `evals/lifecycle-manifests/run.sh` does not, which
is why AC5 reports FAIL. That is honest — it pins work not yet built. Confirmed `/borg-link-up`
never flips `fail`/`unknown`, so the failing pin cannot wedge anything.

**Stamped** at `82fa1a8`.

**One finding raised, deliberately not blocking:** AC1's pin is `tests/cli_contract.bats` — 165
cases, ~10 of which touch scope, the thing AC1 claims. It can go red for unrelated reasons and,
worse, stay green while AC1 specifically regresses (delete the 10 scope cases, 155 keep it exiting
0). That matters because `pass` AUTO-FLIPS the checkbox, so a broad suite is authorised to tick a
narrow criterion. `would_flip: 0` today makes it latent, not live — hence stamp, not block.

**Did not do:** re-derive AC6/AC7's "no pin" justifications against their original `make eval`
counts. Read the reasoning, found it coherent, did not verify the numbers. Said so in the review
rather than implying coverage.

## 23:00 — #209 merged; the CI-gaps priority is closed

**Did:** confirmed #209 green on all 5 lanes, stamped at `0225563`. Its body said *"Stamp
requested"* and *"I merged #204 on a voided stamp earlier today and am not repeating that"* — so it
was explicitly holding for me. Attempted the merge; it was **already merged** by the peer the moment
my stamp landed. The protocol worked; the standstill was a missing stamp, nothing more.

**Verified all four named CI gaps are now sealed on `main`:** one-slug-annotation oracle,
parent-form oracle, literal-slug match (`grep -lxF -e`), AC5.2 invocation oracle. All 1.

**My defect from #205 is fixed on main.** The slug was interpolated into a grep REGEX, so `a[b`
found 0 children where one existed and `a.b` falsely matched `aXb`. CLAUDE.md already recorded this
class (`grep -qx` without `-F`, `troth.site` vs `troth-site`) and there is a branch named
`fix/window-match-is-literal` for the prior instance. I reproduced a documented lesson.

## 23:05 — A FALSE ALARM I RAISED, recorded because it matters more than the work

I read `gh pr view 207` as `state=CLOSED`, grepped `main`, found neither the Queen injection nor the
`/simplify` pass, and concluded **three commits of work had been silently lost** to GitHub
auto-closing a PR whose base branch was deleted. I rebased the orphaned branch to recover it.

**All of it was wrong.** #207 had merged; I had read a stale state mid-merge and grepped a `main` I
had pulled BEFORE it landed. The recovery rebase was unnecessary and my `--force-with-lease` push
was correctly refused because the remote had moved — the lease did its job and stopped me
overwriting a merged branch.

**The lesson, since this is the second inference error today:** I concluded from two reads taken at
different times without re-pulling between them. Earlier I told Noah the gate "reports 9 where
there are about 11" by inferring that two invisible directives belonged to this plan; they name a
different parent. Both errors are the same shape — deriving a consequence instead of reading the
artifact. `git pull` before concluding anything about `main`.

## 23:10 — reviewed and stamped #212 (peer's response to my #211 finding)

The peer took my coarse-pin finding and did something better than the fix I proposed. I suggested
extracting AC1's ten scope cases into `tests/ac1_scope.bats`; they examined it and declined, because
those cases are not self-contained.

**Verified both claims verbatim rather than accepting them:** `_sweepable_repo` is local to
`tests/link_sweep.bats:59` (used at `:115`, `:135`), and the sandbox comment at `:541-543` records
that a cache written to `$XDG_DATA_HOME` *"left all four assertions below green"* until that root was
listed. So re-authoring the harness really could recreate a blind spot that was live once.

They pushed the fix down to the vocabulary instead — a pin that names a CASE, and multiple pins per
criterion with `pass` meaning all. Additive, so expand not migrate. Stamped at `dcefd04`.

**Their line is the keeper:** *"Moving tests to fit the annotation vocabulary is the tail wagging
the dog."*

## 23:20 — the watcher caught a defect in itself; fixed as #213

**Did:** ran a sweep, which printed five lines of the form `#206 closed: no longer open (state
OPEN)`. That string is self-refuting, and it exposed the cause: `core.diff` classified departures
with `MERGED if before["state"] == "MERGED"`, but the sweep queries `states:OPEN`, so that field is
ALWAYS "OPEN" in a snapshot. The MERGED arm was unreachable; every merged PR reported as closed.
Five real merges printed it before anyone noticed.

**Third gate-that-cannot-fire today, all mine, all the same cause** — a branch keyed on a field
whose only reachable value fails the test:
1. the allowlist keyed on `reviewDecision` (always empty here — self-review is blocked)
2. the stamp regex written against the protocol as described, not the stamps as posted
3. this one

**Fixed** by having pure `core` report one honest kind (`DEPARTED`) and the impure `cli` resolve it
with one `gh pr view --json state`, for departures only. `core` declining to guess is what keeps the
purity property its AST test pins.

**Regression asserts the discriminating direction:** a snapshot state of `MERGED` must change
NOTHING, because trusting it is the defect. Loops all four possible values and asserts the
self-refuting detail cannot return. Mutation-verified.

**Verified live:** injected two genuinely-merged PRs as if open — both now report `merged`.

Opened as **#213**, green pending CI. Not merged: it is my own work and unstamped.

## 23:20 — state at end of this stretch

| | |
|---|---|
| open PRs | **#212** (peer's, stamped by me @ `dcefd04`), **#213** (mine, needs their stamp) |
| merged today | #203, #204, #205, #206, #207, #208, #209, #210, #211 |
| `main` health | pylint 10.00/10, pytest 1206, bats 824 |
| plan state | checked 5, pass 4, fail 1, unknown 2, `would_flip` 0 |
| CI-gaps priority | **closed** — all four gaps sealed and oracled |

**Nothing is blocked on Noah.** #213 needs the peer's stamp; #212 needs their merge.

**#213 CI:    5 pass **


## 23:40 — issue #97, and the watcher blind spot that hid it

**Noah asked whether I had seen issue #97. I had not, and could not have.** The sweep queried
`pullRequests` and nothing else. #97 is *"PR control hub — cross-machine actionable command
center"*, 9 comments, updated 23:02 tonight. So the evening went into building PR tooling while the
design argument ABOUT PR tooling ran on a surface the tooling was structurally blind to.

**Fixed as #215** (stacked on #213): issues swept in the same GraphQL call, three narrow kinds. No
new allowlist guard needed — `decide` acts only on HEAD_MOVED and defaults to refusal, so issue
kinds are refused by construction. Pinned anyway, because "happens to be refused" and "refused by
construction" read the same until someone adds a kind.

**The upgrade case needed its own guard:** a snapshot predating issue support has no `issues` key,
and treating that as "zero issues" would report every open issue as NEW — the cold-start flood
arriving through a schema change. Absence of the key is now distinguished from an empty list.

Verified end-to-end: #97's comment count decremented in the snapshot surfaces
`#97 issue_comment: 2 new comment(s), now 9` where the sweep printed nothing.

### Then I made the mistake the thread had already recorded

I commented on #97 having read only the newest entry, arguing the hub "is not greenfield". Then I
read all seven and retracted the framing: the schema was already designed, corrected once, and
**Noah settled the substrate at 22:04 — git-repo-as-transport off the table, annotations
machine-local.** I answered a settled question from the wrong end.

The 21:24 comment on that thread opens *"full-thread pass (I'd previously missed the first two
comments; corrected)"*. The work machine made this exact error on this exact issue two months ago.
Repeated from the same seat.

### The finding that made reading it worthwhile anyway

**Every architecture comment on #97 is dated 2026-07-27. Cairn was decommissioned 2026-08.** The
settled three-layer design names cairn as the durable annotation layer — a retired service. And the
lesson CLAUDE.md recorded from that decommission is a direct hazard for the requirement: *"never
build capture that asks the agent to volunteer (four shipped, tested, exposed voluntary-write
surfaces produced one real row in five months)."* A durable annotation layer keyed by `repo#num` IS
a voluntary-write surface.

Posted as a correction, with the alternative that fits the lesson: derive annotations from artifacts
already produced — review comments, `STACK-APPROVAL` stamps, checkpoints, all keyed to `repo#num` or
a SHA, all written for other reasons. Explicitly did NOT reopen the substrate decision.

## End state

| | |
|---|---|
| open PRs | **#212** theirs (stamped by me) · **#213** · **#214** (`resolve`) · **#215** (issues) |
| merged tonight | #203 through #211 |
| `main` | pylint 10.00/10, pytest 1216, bats 835 |
| CI-gaps priority | closed — all four gaps sealed and oracled |
| independent duplicates today | **five**; #97 is the expensive one |

**Nothing is blocked on Noah.** #213/#214/#215 need the peer's stamps; #212 needs their merge.

### For the retro: one error three times

Concluding from a stale or partial read instead of re-reading the artifact.

1. "The gate reports 9 where there are ~11" — inferred that two invisible directives belonged to
   this plan. They name a different parent.
2. "Three commits of work were lost" — read a stale `CLOSED` mid-merge and grepped a `main` pulled
   before the merge landed. Nothing was lost.
3. The #97 comment written off one entry of seven.

Each was cheap to prevent and none was caught by a test, because none of them is a code defect.
