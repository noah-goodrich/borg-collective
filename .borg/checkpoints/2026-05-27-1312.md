## 1. Goal

Clear the backlog left from the previous session: merge PR #27, archive `PROJECT_PLAN.md`, route
all deferred untracked files, and run the ~/dev audit directive.

## 2. Accomplished

- **PR #27 merged** — `fix/capacity-warning-hook-and-skill` (capacity warning in SessionStart hook
  + adhd-guardrails).

- **Local main divergence fixed** — 5 commits had been made directly to local main without being
  pushed. Rescued them to `feat/orchestrator-mode-session-separation`, then reset local main to
  `origin/main`. (Branch later cleaned up as redundant with PR #21.)

- **PR #28 merged** — Archived `PROJECT_PLAN.md` →
  `docs/plans/assimilated/2026-05-04-skill-extension-protocol.md` with `Shipped: 2026-05-27`.

- **PR #29 merged** — Routed all deferred untracked files:
  - `hooks/borg-plan-promote.sh` + `tests/plan_promote.bats` committed
  - Fixed erroneous `.borg/` gitignore line (was breaking `!.borg/checkpoints/` negation)
  - Added `.claude/` and `templates/supabase/.borg/` to `.gitignore`
  - Session checkpoints `2026-05-27-1038.md` + `2026-05-27-1141.md` committed
  - `CLAUDE.md`, `README.md`, `docs/architecture.md`, `borg.zsh` doc updates landed

- **~/dev audit directive complete:**
  - `claude-plugins/`: committed all stray content (deep-research skill, INSTALL.md,
    writing-voice validation corpus), pushed, deleted local clone
  - `cortex-handoffs/`: April 2026 eval notes moved to `~/Documents/old-repos/dev-strays/`
  - `reveal-design-system/`: directive stale — already registered in borg registry, no action
  - `snowflake_assets/`: 102M of brand assets moved to `~/Documents/snowflake_assets`
  - 5 stray session markdown files removed from `~/dev/` root

## 3. Ready to Commit

Nothing uncommitted. `docs/research/` remains untracked in the worktree — it belongs with
PR #22 (`docs/borg-next-level-research`) and will land when that branch merges.

## 4. Blockers

**PR #21 (`feat/auto-promote-plan-hook`) needs rebase.** Its last 3 commits (borg-plan-promote
docs + tests) now conflict with main since the same content landed via PR #29. The first 5
commits (orchestrator-mode session separation) are still unmerged and valid. Needs a rebase
that drops the conflicting commits before it can merge.

## 5. Next Session

1. **Handle PR #21** — Two options:
   - Rebase onto main, drop the 3 borg-plan-promote commits (already in main), force-push, merge.
   - Or close PR #21 and open a focused `feat/orchestrator-mode-session-separation` PR with just
     the 5 orchestrator-mode commits. Commits are at `08fb429..4f899ba` in the branch history.
2. **Archive the ~/dev audit directive** — it's done. Run:
   `git mv docs/plans/directives/2026-05-11-dev-dir-final-audit.md docs/plans/assimilated/`
   and prepend `Shipped: 2026-05-27`.
3. **Review PR #22** (`docs/borg-next-level-research`) — likely has conflicts with main now
   (many overlapping files were updated via #29). Check and rebase if needed.
