**tl;dr** — Review round 1 on noah-goodrich/borg-collective#220 addressed at head `cdd06dd`: AC2 now promises the
`borg_core/reconcile` package rather than a `borg reconcile` verb that never existed, the three code points are
taken as-is, the branch is rebased onto main, and the reply is posted. Next: the other machine re-reviews and stamps
at `cdd06dd`, or sends round 2. Ontra-ai/ai-data-engineer#48 still waits on a data-eng approval.

## 1. Goal

Noah asked whether my PRs needed action. #220 had a same-day review refusing to stamp; this session executed its
asks and re-pushed.

## 2. Accomplished

- **The review's central finding was correct.** AC2 was ticked as "`borg reconcile` ships READ-ONLY" and there is
  no dispatch arm, no `cmd_reconcile`, no `cli.py`, and no importer outside the package's tests; the verb exits 1
  with `unknown command`. The verify clause tested the library against the library, which is the `borg watch` /
  `borg sync` / `borg recon` shape CLAUDE.md already records three times.
- **Chose rewording over wiring.** The directive's own scope rule is "if done early: ship, don't expand", and a new
  CLI surface with a `cli_contract.bats` case is a session, not a fix. AC2 in
  `docs/plans/directives/2026-09-19-employer-shim-and-adapter-layer.md` now promises the package and names the verb
  as a follow-up directive. The objective line and the ship definition are reworded to match.
- **Ship-definition smoke run done for the first time.** `shell.manifest_rows` on this repo's `viz-program`, live
  states from `gh`, `core.report` + `core.summary`: 4 rows, kinds `{github}`, 0 unresolvable, 0 findings. Pasted
  into the PR body. Zero is correct: three rows declare `merged` and are merged; #220 declares nothing.
- **Prose gate ceiling.** `tests/prose_contracts.bats` case 12 gains `[ "$n" -le 40 ]`. Reproduced the reviewer's
  mutation (rename only the `prefer-tool` end anchor): the extracted section went 28 → 308 lines and the case now
  fails naming the anchor. Reverted; 15/15.
- **`resolvable_kinds()` guarded.** `except OSError` → `set()`, the narrower degrade so every ref is reported
  unresolvable rather than quietly resolved; commented with the pointer to `manifest/cli.py`, where the same call
  degrades in the opposite safe direction. New test monkeypatches the raise. Did NOT add the mirror comment on the
  `manifest/cli.py` side: AC5 surface, out of this directive's scope, and it already says "must not WIDEN".
- **`summary()`** drops the unreachable `or []`.
- **Rebased onto `f551bba`** (#221 and #224 merged under the branch); clean.
- **Verification on the head:** pytest 1259 passed at 93% coverage; lint 10.00/10; bats 878 ok, 0 not ok.
- **Commit `cdd06dd`**, force-pushed with lease. PR body updated (AC2 row, smoke output, "Review round 1" section,
  counts). Reply comment posted on #220 at `cdd06dd` listing each point and the one deliberate omission.
- **PR 48 checked:** all checks green, `REVIEW_REQUIRED`, zero reviews, zero comments, even with main. Nothing to do
  but wait for a data-eng teammate.

## 3. Ready to Commit

Only this checkpoint and `.borg/programs/viz-program.json` (the row's `why` refreshed to say "package" during
link-up); both committed right after this file is written. `/simplify` was not run; the Python delta is a
try/except, a comment, one test, and one dropped `or []`.

## 4. Blockers

- No blockers. The stamp is the other machine's to give, at `cdd06dd`. Per the protocol, do not touch the head for
  post-verdict nits; file them.
- The planstate engine, run read-only against the restored One Front Door plan, would flip its AC5 on
  `path:evals/lifecycle-manifests/run.sh exists`. Not applied from this worktree: that plan is the One Front Door
  drone's, and the AC2–AC4 `fail` verdicts are the same `PYTHONPATH`-at-the-main-tree resolver artifact as before,
  so a half-trusted run should not write into someone else's plan.

## 5. Next Session

1. Read new comments on noah-goodrich/borg-collective#220 (`gh pr view 220 --comments`). If a stamp at `cdd06dd`
   arrives, the merge condition is CI plus the second stamp; if round 2 arrives, address it the same way — fix,
   rebase if main moved, reply at the new head.
2. If noah-goodrich/borg-collective#222 or #223 merges first, rebase; #222's rename carries the manifest row to
   `.borg/chains/` by rename detection (verified by both machines), #223 has no overlap with #220.
3. Follow-up directive to file, not now: wire `borg reconcile` (a `borg_core/reconcile/cli.py`, a `reconcile)` arm
   in `borg.zsh`, `borg help` line, and a `cli_contract.bats` case asserting the arm exists).

## Criteria Reconciled

Engine run read-only (no `--apply`) against `PROJECT_PLAN.md`, which is the One Front Door plan on this branch.

**Proposed, not flipped** (would be `pass`; not applied because the plan belongs to the One Front Door drone):
- AC5 — Lifecycle skills author project manifests by default — pass: `path:evals/lifecycle-manifests/run.sh exists`
  - Apply with: `python3 -m borg_core.planstate.cli PROJECT_PLAN.md --json --apply`, from that drone's worktree.

**Proposed, not flipped** (`fail` / `unknown`):
- AC2, AC3, AC4 — fail: `pytest:borg_core/link/test_picture.py`, `test_across.py`, `test_grid.py` exited 1 under a
  `PYTHONPATH` pointing at the main tree on `feat/short-window-names`; suites pass in this worktree.
- AC6, AC7 — unknown: `no evidence annotation`.
- AC1 — pass, already `[x]`.

The shim directive's five ticks are hand-made; `/borg-assimilate` remains the gate.

## Manifest Row

**Wrote:** `noah-goodrich/borg-collective#220` → `viz-program` (lane `_default`, order 1), `why` refreshed in place.
