---

# Session Debrief: borg-collective
*Date: 2026-04-23 02:33*

## Objective
Reduce permission-prompt friction by allowlisting read-only Supabase MCP tools that were interrupting Claude sessions in the `reveal` and `ingle` projects.

## Outcome
- **Modified:** `~/.claude/settings.json` — added 17 read-only Supabase MCP tools to the global allowlist (inserted alphabetically between `Grep` and `Read`): `generate_typescript_types`, all `get_*` / `list_*` ops, and `search_docs`
- **Created:** `.borg/checkpoints/2026-04-23-0233.md`
- Mutating ops (`execute_sql`, `apply_migration`, branch/project lifecycle) intentionally left prompt-gated
- JSON validity of `settings.json` confirmed via `jq`
- No `borg-collective` repo files were modified; nothing to commit

## Decisions Made
- **Read-only ops allowlisted, mutating ops kept behind prompt.** Audited 26 actual Supabase MCP `tool_use` calls across `reveal` and `ingle` sessions; dominant callers were `execute_sql` (7) and `apply_migration` (4), but those carry write risk so they stay gated. The 17 read-only tools were the clear safe cut.
- **Global allowlist, not per-project.** Change went into `~/.claude/settings.json` rather than a project-scoped config, so the relief applies everywhere.

## Blockers and Observations
- No blockers this session.
- `ingle` shows as idle in `borg ls` because it runs inside a drone container under `/workspace` — its Claude session history never lands on the host, so the registry sees no activity.
- Post-write hook flagged line-length violations (>120 chars) in the checkpoint file — reflow not performed, just a warning.

## Next Steps
1. **If Supabase prompts resurface:** identify the offending tool with `jq -r '..|.name? // empty' ~/.claude/projects/<slug>/*.jsonl | grep -i supabase` — it's almost certainly `execute_sql` or `apply_migration`; decide at that point whether to promote them to the allowlist.
2. **Commit the pre-existing untracked files** (when ready, separately from this session's work): `.borg/checkpoints/2026-04-14-1600.md`, `.borg/checkpoints/2026-04-20-1100.md`, `.claude/`, `docs/plans/directives/2026-04-14-portfolio-mvp-pivot.md`, `templates/supabase/.borg/`.
3. **Reflow `.borg/checkpoints/2026-04-23-0233.md`** to satisfy the 120-char line-length hook if that warning is enforced as an error in future runs.
