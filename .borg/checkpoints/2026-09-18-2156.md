**tl;dr** — The shim drone finished noah-goodrich/borg-collective#220: AC4 (the employer leak) and AC5
(nothing breaks) landed, so all five criteria are ticked, the branch is rebased onto main and pushed, and the PR
body carries the full table. Next: Noah decides whether to open the employer PR from the pushed branch and rules
on the one-plan-slot conflict; nothing else in this worktree is pending.

## 1. Goal

Work queue items 1 to 3 of the Queen's 2026-09-18 22:00 seed for noah-goodrich/borg-collective#220 only: check
whether noah-goodrich/borg-collective#222 merged, finish AC4 and AC5 of the branch's own `PROJECT_PLAN.md`, and
keep the PR body's criteria table current.

## 2. Accomplished

- **Queue 1.** noah-goodrich/borg-collective#222 is still OPEN, so not waited on. Rebased this branch onto main
  at `921278c` (noah-goodrich/borg-collective#225 was the one new commit); zero file overlap, clean rebase.
- **AC4, employer side.** `Ontra-ai/ai-data-engineer` branch `fix/close-borg-leak-in-plugins` @ `cc71779`,
  pushed, **PR deliberately not opened** (posts to teammates under Noah's account). Three changes: `strike.md`
  line 334 says "do a design pass before editing: write down the change, its blast radius and the rollback, and
  have someone else read it" with no borg word; `tests/run-tests.sh`'s portability grep covers the whole
  `plugins/` tree instead of two paths; `plugin.json` bumped 1.12.1 to 1.12.2. Suite green, two mutations red
  (the shipped line back in `strike.md`; a borg string in `airflow-debugger/`, which the old grep never looked
  at), `shellcheck --severity=warning tests/run-tests.sh` clean.
- **AC4, machine side.** `~/.config/borg/extensions/skill-extensions/borg-plan/01-context.md` written: when a
  Type C DAG/pipeline fix arrives via `/strike`, run `/borg-collective-review` before locking criteria, DE-1727
  precedent. Machine-local, unversioned by design; an absent file everywhere else.
- **Found on the way.** The employer suite on unmodified main failed its own narrow borg grep when the checkout
  lived under `~/.local/state/borg/worktrees/`, a path Noah already uses for that repo (`alert-watch-installer`).
  Traced with `bash -x` to `skills/stacked-pr-program/scripts/__pycache__/stamp_stack.cpython-314.pyc`: the
  DATA LOSS check imports `stamp_stack.py`, Python 3.14 embeds the checkout's absolute path in bytecode, and the
  path contains "borg". The widened grep adds `--exclude-dir=__pycache__`; reproduced failing before and passing
  after under that path. The employer worktree itself was moved to `~/dev/ai-data-engineer-wt-close-leak`, the
  naming that repo's other worktrees use.
- **AC5.** On the rebased head: `make test` 1258 passed at 93% coverage (floor 90), `make lint` 10.00/10,
  `bats tests/` 877 ok exit 0. This branch adds no shell file, so shellcheck applies only to the employer-side
  `run-tests.sh` above.
- **Borg side commit `87d6681`**, pushed with `--force-with-lease`: all five plan boxes ticked with evidence
  lines under AC4 and AC5; the plan's risk "AC5 requires work in a private repo" corrected to AC4; the CLAUDE.md
  shim section records the closure and the bytecode finding. `bats tests/prose_contracts.bats` cases 12 to 15
  still pass on the edited section.
- **Queue 3.** noah-goodrich/borg-collective#220 body rewritten with a five-row criteria table, the AC4 mutation
  transcript, and the unchanged merge condition; title now reads "(AC1–AC5 of 5)".
- `/simplify` was NOT run this session. Nothing new was written in Python; the only code edits were prose and one
  shell `if` block in the employer repo.

## 3. Ready to Commit

Two files in this worktree, both from the link-up itself, committed together right after this file is written:
`.borg/checkpoints/2026-09-18-2200.md` (the Queen's seed, untracked until now) and
`.borg/programs/viz-program.json` (the manifest row below, plus a key-order rewrite the writer performs).
`.venv` stays untracked; it is a symlink into the main tree's venv and works.

## 4. Blockers

- **Clock skew makes this file sort BEFORE the seed.** This machine's `date` returned `2156` while the Queen
  stamped her seed `2200` and her kickoff "22:08 MDT". `hooks/borg-link-down.sh` picks the latest checkpoint by
  `sort -r | head -1` on the filename, so the NEXT session in this worktree will be handed the seed, not this
  file. Whoever reads the seed next: this checkpoint supersedes it. The skill forbids composing a timestamp by
  hand, so the filename was not adjusted.
- The planstate engine's AC3 verdict is a false negative caused by the resolver, not the code. The skill resolves
  the borg source dir from `command -v borg`, which is the main tree on `feat/short-window-names`, and that tree
  has no `borg_core/reconcile/`. With `PYTHONPATH` pointing there, `pytest borg_core/reconcile/test_shell.py`
  cannot import and exits 1. The same suite passes in this worktree's own tree (1258 passed). Not re-run, per
  the one-invocation rule.
- The engine's verdicts for AC1 and AC2 were not captured: the output was piped through `tail -40` and those two
  entries scrolled off. Not re-run for the same reason.

## 5. Next Session

Nothing in this worktree is owed. Two decisions are Noah's, in this order:

1. Open the employer PR, or not: `gh pr create --repo Ontra-ai/ai-data-engineer --base main --head
   fix/close-borg-leak-in-plugins`. The commit message on `cc71779` is the body draft. Check the Python 3.14
   pycache note lands in the PR text; a reviewer will otherwise ask why `--exclude-dir` is there.
2. Rule on the one-plan-slot conflict in `PROJECT_PLAN.md`'s Ship Definition. Until then
   noah-goodrich/borg-collective#220 does not merge. If noah-goodrich/borg-collective#222 merges first, rebase
   and move the `_default` lane row from `.borg/programs/viz-program.json` to wherever `.borg/chains` puts it.

If picking this up as a drone: `git -C /Users/noahgoodrich/dev/borg-collective-shim status` should show only
`.venv` untracked and the branch even with origin.

## Criteria Reconciled

**Proposed, not flipped** (`fail` / `unknown` — evidence is not yet real to the engine):
- AC3 — Resolution is adapter-driven, with no hardcoded source name on the path — fail:
  `pytest:borg_core/reconcile/test_shell.py exited 1` (resolver pointed at a tree without the package; see §4)
  - Apply with: `python3 -m borg_core.planstate.cli PROJECT_PLAN.md --json --apply` with `PYTHONPATH` set to
    this worktree, once the resolver points at a tree that has `borg_core/reconcile/`.
- AC4 — The employer leak is closed as an absence, and the grep that missed it can fail — unknown: `no evidence
  annotation` (the evidence lives in a private repo; the plan records it as prose, not as a runnable pin)
- AC5 — Nothing breaks — unknown: `no evidence annotation`
- AC1, AC2 — verdicts not captured this run (see §4).

Disclosure: all five boxes were ticked by hand in commit `87d6681` as queue item 3 work, before this link-up ran.
The engine flipped nothing (`flips: []`); no box carries a `flipped by link-up` mark. `/borg-assimilate` remains
the gate and may un-tick any of them.

## Manifest Row

**Wrote:** `noah-goodrich/borg-collective#220` → `viz-program` (lane `_default`, order 1). A fresh lane, so the
row is a root and not a child of the viz chain's tail, which matches Noah's 2026-09-17 ruling that this plan
stands to the side of One Front Door.
